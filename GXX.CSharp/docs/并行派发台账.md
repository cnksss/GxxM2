# 并行派发台账（Parallel Dispatch Ledger）

> 建立日期：2026-09-19 ｜ 建立者：并行调度会话（会话 B）
> 目的：让 **顺序干线会话（会话 A）** 与 **并行分支车道（会话 B 派发的多个子车道）** 同时推进
> Delphi 7 → C# 全量翻译，**互不干扰、各自提交、最后合并**。

---

## 0.0 给"顺序干线会话（会话 A）"的一段话（可直接粘贴）

```
并行派发已启动。请先读 GXX.CSharp/docs/并行派发台账.md，然后遵守三条：
1) 不要碰 .worktrees/ 目录，不要创建/切换/删除 par/* 分支。
2) 新批次只在你原有区域内开工：src/GXX.M2Server/**、src/GXX.Client/Scenes/**、src/GXX.Client/HGE/**、
   tests/GXX.M2Server.Tests/**、tests/GXX.Client.Tests/FormJ*.cs。
   以下路径已被车道独占，请勿新建/修改：
     src/GXX.Client/GUI/**、src/GXX.Client/DxComponent/**、src/GXX.Client/ReadResources/**（Wil.cs 除外）、
     src/GXX.RunGate/**、src/GXX.DBServer/**、src/GXX.LoginSrv/**、src/GXX.LogDataServer/**、
     tests/GXX.RunGate.Tests/**、tests/GXX.DBServer.Tests/**、tests/GXX.LoginSrv.Tests/**、tests/GXX.LogDataServer.Tests/**
3) 继续用 "批次J<n>：…" 提交；不要改 GXX.slnx / Directory.Build.props / docs/Checklist.md 之外别人认领的文件。
   若想让车道接手某块区域，只在台账 §5「待认领池」加一行，不要改动别人的行。
```
---

## 0. 结论：可以并行，机制如下

单一工作树里跑多个 agent 必然互相踩（共用一个 git index、一次 `git add` 会把别人的半成品一起提交）。
本工程采用的方案是 **git worktree + 独占文件分区 + 单向合并**：

```
main (会话A，串行干线，持续提交)
  │
  ├─ par/lane-client-gui-mir    → .worktrees/lane-client-gui-mir    （车道1  Client GUI/Mir 窗口族）
  ├─ par/lane-resources         → .worktrees/lane-resources         （车道2  Client 资源格式族）
  ├─ par/lane-rungate-utils     → .worktrees/lane-rungate-utils     （车道3  RunGate 工具族）
  ├─ par/lane-dbserver-utils    → .worktrees/lane-dbserver-utils    （车道4  DBServer 工具族）
  ├─ par/lane-dxcomponent       → .worktrees/lane-dxcomponent       （车道5  DxComponent 基础控件族）
  ├─ par/lane-login-logdata     → .worktrees/lane-login-logdata     （车道6  LoginSrv/LogDataServer 余部）
  ├─ par/lane-client-guiconfig  → .worktrees/lane-client-guiconfig  （车道7  Client GameConfig 对话框族）
  └─ par/lane-client-statewin   → .worktrees/lane-client-statewin   （车道8  Client StateWindows.pas 分片）
```

- 每个车道 = 一个独立目录 + 一条独立分支 + 一份独立 `index`，因此**可以真正并行提交**，不会互相锁。
- 冲突面被压到零的做法有二：
  1. **文件独占分区**（见 §2）：任何两个写入者不得拥有同一个文件路径。
  2. **车道只新建文件**：SDK 风格 csproj 自动 glob 全部 `*.cs`，新增文件**不需要改 csproj / slnx**，所以车道永远不碰共享文件。
- 合并方向是**单向的**：`par/* → main`，由会话 B 在集成时执行；车道永不 merge/rebase。
- 因子工程的项目引用不跨车道，`dotnet build` 在各自目录内独立可跑（已验证：全量 build 约 3 秒）。

---

## 1. 给会话 A（顺序干线）的硬性约束

会话 A 请遵守（这是"两个会话不干同一件事"的唯一保证）：

1. **不要**创建、切换、删除任何 `par/*` 分支，**不要**碰 `.worktrees/` 目录下的任何文件。
2. **不要**修改 `docs/并行派发台账.md`、`docs/并行覆盖审计.md`、`docs/并行报告-*.md`、`tools/**`（这些是会话 B 的登记文件，会话 A 只读）。
3. 开工新批次前，先看 §2 的**车道独占区**：`src/GXX.Client/GUI/{Mir,GameConfig,NewStateWin}/**`、`src/GXX.Client/DxComponent/**`、
   `src/GXX.Client/ReadResources/**`、`src/GXX.RunGate/**`、`src/GXX.DBServer/**`、`src/GXX.LoginSrv/**`、`src/GXX.LogDataServer/**`
   **已被车道认领**，不要在这些路径下新建/修改文件，否则合并时必冲突。车道未认领的区域（`src/GXX.M2Server/**`、
   `src/GXX.Client/Scenes/**`、`src/GXX.Client/HGE/**`、`src/GXX.Client/ReadResources/Wil.cs`、`tests/GXX.M2Server.Tests/**`、
   `tests/GXX.Client.Tests/FormJ*.cs` 等）全部归会话 A。
4. 会话 A 的提交信息继续沿用 `批次J<n>：…`，车道用 `并行批次P<n>：…`，**编号空间不重叠**，便于审计。
5. 会话 A 若想让车道接手某块区域，在本文件 §5 的"待认领池"里**加一行**即可（只加行，不改别人的行）。

---

## 2. 文件独占分区（唯一真源）

| 分区 | 写入者 | 独占路径（**只有**该写入者可创建/修改） |
|---|---|---|
| 串行干线 | 会话 A | `GXX.CSharp/src/GXX.M2Server/**`；`GXX.CSharp/src/GXX.Client/Scenes/**`；`GXX.CSharp/src/GXX.Client/HGE/**`；`GXX.CSharp/src/GXX.Client/ReadResources/Wil.cs`；`GXX.CSharp/tests/GXX.M2Server.Tests/**`；`GXX.CSharp/tests/GXX.Client.Tests/` 下除 `GuiMir*Tests.cs`、`GuiCfg*Tests.cs`、`GuiSw*Tests.cs`、`Resource*Tests.cs`、`Dx*Tests.cs` 以外的全部文件；`GXX.CSharp/docs/Checklist.md` |
| 车道1 | 会话 B | `GXX.CSharp/src/GXX.Client/GUI/Mir/**`（新建）；`GXX.CSharp/tests/GXX.Client.Tests/GuiMir*Tests.cs`（新建） |
| 车道2 | 会话 B | `GXX.CSharp/src/GXX.Client/ReadResources/**`（**除 `Wil.cs`**）；`GXX.CSharp/tests/GXX.Client.Tests/Resource*Tests.cs`（新建） |
| 车道3 | 会话 B | `GXX.CSharp/src/GXX.RunGate/**`（**除 `Program.cs`、`RunGateService.cs`、`DesNew2.cs`、`BagItemList.cs`**）；`GXX.CSharp/tests/GXX.RunGate.Tests/**`（新建工程） |
| 车道4 | 会话 B | `GXX.CSharp/src/GXX.DBServer/**`（**除 `Program.cs`、`DBServerService.cs`、`RoleDatabase.cs`**）；`GXX.CSharp/tests/GXX.DBServer.Tests/**`（新建工程） |
| 车道5 | 会话 B | `GXX.CSharp/src/GXX.Client/DxComponent/**`（新建）；`GXX.CSharp/tests/GXX.Client.Tests/Dx*Tests.cs`（新建） |
| 车道6 | 会话 B | `GXX.CSharp/src/GXX.LoginSrv/**`（**除 `Program.cs`、`LoginSrvService.cs`、`AccountDatabase.cs`**）、`GXX.CSharp/src/GXX.LogDataServer/**`（**除 `Program.cs`、`LogDataService.cs`**）；`GXX.CSharp/tests/GXX.LoginSrv.Tests/**`、`GXX.CSharp/tests/GXX.LogDataServer.Tests/**`（新建工程） |
| 车道7 | 会话 B | `GXX.CSharp/src/GXX.Client/GUI/GameConfig/**`（新建）；`GXX.CSharp/tests/GXX.Client.Tests/GuiCfg*Tests.cs`（新建） |
| 车道8 | 会话 B | `GXX.CSharp/src/GXX.Client/GUI/NewStateWin/**`（新建）；`GXX.CSharp/tests/GXX.Client.Tests/GuiSw*Tests.cs`（新建） |
| 车道9 | 会话 B | `GXX.CSharp/src/GXX.GameCenter/**`（**除 `Program.cs`、`GameCenterService.cs`**）；`GXX.CSharp/tests/GXX.GameCenter.Tests/**`（新建工程） |
| 车道10 | 会话 B | `GXX.CSharp/src/GXX.SelGate/**`（**除 `Program.cs`、`SelGateService.cs`**）；`GXX.CSharp/tests/GXX.SelGate.Tests/**`（新建工程） |
| 车道11（调度会话自己开） | 会话 B | `GXX.CSharp/src/GXX.Core/Data/**`（新建）；`GXX.CSharp/tests/GXX.Core.Tests/SqlCreateTable*`（新建） |
| 白名单特例 | 会话 B | 车道1 额外持有 `GXX.CSharp/src/GXX.Client/GUI/DxComponent/{DxControls,DxComponents}.cs`（**上限这两个文件名**）；集成时由会话 B 迁移到 `src/GXX.Client/DxComponent/` |
| 共享（**仅集成时由会话 B 串行改**） | 会话 B（独占） | `GXX.CSharp/GXX.slnx`、`GXX.CSharp/Directory.Build.props`、`GXX.CSharp/docs/并行*.md`、`GXX.CSharp/tools/**` |

> 违反分区的唯一后果就是合并冲突。**宁可换个文件写，也不要跨区。**

---

## 3. 车道使用规程（派发子 agent 时逐条复制）

```
工作树（唯一可写目录）：D:\chuanqi\daima\GXX原版_Delphi7\.worktrees\<lane>
分支：par/<lane>
绝对禁止：在 D:\chuanqi\daima\GXX原版_Delphi7 根目录执行任何 git 写命令（会话 A 正在那里工作）
绝对禁止：修改 GXX.slnx / *.csproj / Directory.Build.props / docs/Checklist.md / docs/并行派发台账.md
绝对禁止：git merge / rebase / checkout / switch / push / worktree
提交：在自己的工作树内 git add -A && git commit -m "并行批次P<n>：…"
禁止：在工作树里留下并提交临时探查目录（如 _tmp_src/、_probe/）；提交前删除，或写入 .git\info\exclude
门禁：dotnet build GXX.slnx -c Debug --nologo  → 0 error
      dotnet test <本车道测试工程>              → 0 failed
```

---

## 4. 合并（最后一步，由会话 B 串行执行）

前提：各车道测试全绿、车道分支已提交。

```powershell
cd D:\chuanqi\daima\GXX原版_Delphi7
# 1) 先确认干线工作树干净（会话 A 不能有未提交改动，否则暂停）
git status --short

# 2) 逐个合并车道（分区互斥 ⇒ 应无冲突）
git merge --no-ff par/lane-client-gui-mir -m "合并并行车道：Client GUI/Mir 窗口族"
git merge --no-ff par/lane-resources      -m "合并并行车道：Client 资源格式族"
git merge --no-ff par/lane-rungate-utils  -m "合并并行车道：RunGate 工具族"
git merge --no-ff par/lane-dbserver-utils -m "合并并行车道：DBServer 工具族"

# 3) 集成动作（只有这一步会改共享文件）
#    a) 把新测试工程登记进 GXX.slnx
#    b) dotnet build GXX.slnx -c Release  → 0 error
#    c) dotnet test 全解决方案            → 0 failed
```

合并后车道工作树可保留复用（下一波换个分支名继续），或 `git worktree remove .worktrees/<lane>`。

---

## 5. 待认领池（会话 A 想退掉的区域 / 会话 B 的下一波）

| 序 | 目标区域 | 规模（Delphi 行） | 状态 |
|---|---|---|---|
| 1 | `Source/Client-HGE/GUI/Share/FState.pas` | 22,503 | **下一波（P2）首抢**，需分片（须先定 `GUI/Share/**` 归属） |
| 2 | `Source/Client-HGE/GUI/NewStateWin/StateWindows.pas` | 12,481 | ✅ 车道8（P1） |
| 3 | `Source/Client-HGE/GUI/Mir/SerialWindowsDlg.pas` | 34,266 | 待认领（巨片，必须再分片，须先定 `GUI/Mir/**` 归属交接） |
| 4 | `Source/Client-HGE/DxComponent/**`（37 个自绘控件） | ≈ 33,000 | 车道5（P1）已取 6 个；余 31 个（DxMemo 5,139 / DIB 8,087 / DxControls 3,516 / DxImageEdit 1,729 / DxMainBottomForm 1,352 / DxEdit 1,354 …）待 P2 |
| 5 | `Source/Client-HGE/ReadResources/Pak.pas` | 2,798 | 待认领（P2，车道2 已占该目录） |
| 6 | `Source/M2Engine/ObjHero.pas` / `ObjMon.pas` / `ObjSmartMon.pas`(3,469) | 12k+8k+3.5k | 待认领（**须先与会话 A 协商**，属其 `src/GXX.M2Server/**` 区） |
| 7 | `Source/DBServer/MySqlRoleDB.pas`（SqliteRoleDB 的镜像实现） | 5,539 | 待认领（P2，车道4 已占该目录） |
| 8 | `Source/LoginSrv/**` 余部 / `Source/LogDataServer/**` 余部 | — | ✅ 车道6（P1） |
| 9 | `Source/RunGate/RunGateUtils.pas`(4,393) / `RunGate/Common/IocpWinsock2.pas`(3,101) / `uBuffer.pas`(2,271) / `IocpUtils.pas`(1,383) / `uFrm*.pas`(8 个配置窗体) | ≈ 20,000 | 待认领（P2，车道3 已占该目录+测试工程） |
| 10 | `Source/Client-HGE/AsyncCalls.pas`（M2Engine / Client-HGE / RunGate **三份同源，2,959 行 ×3**） | 8,877 | **高价值 P2**：移植一次到 `src/GXX.Core/Async/`，三处共用（须新增 `src/GXX.Core/**` 归属） |
| 11 | `Source/Client-HGE/GameConfig/{Mir,MirJSY,MirReturn}/*.pas` | 7,442 + 5,615 + 5,339 | 待认领（P2，车道7 已占 `GUI/GameConfig/**`） |
| 12 | `Source/GameCenter/**` 余部（GMain 6,382 / ParadoxDataSet 1,240 / ParadoxConv 1,049 …） | ≈ 15,000 | ✅ 车道9（P1）取 GMain + 4 个小单元 |
| 13 | `Source/SelGate/**` 余部 | ≈ 5,000 | ✅ 车道10（P1，先做对照审计） |
| 14 | `Source/Common/MySqlCreateTableSql.pas`(2,786) / `MsSqlCreateTableSql.pas`(883) | 3,669 | ✅ **已完成并合并**（车道11，`657cbe15`）。生成器 `tools/gen/GenSqlCreateTable.ps1` 可复用：`SqliteCreateTableSql.pas`(1,097) 建议同法处理（先确认是否已被 `GXX.DBServer/RoleDatabase.cs` 覆盖） |
| 16 | `Source/Common/AESUtils.pas`(1,218) | 1,218 | 待认领（P2，须新增 `src/GXX.Core/Crypto/**` 归属） |
| 15 | `Source/{GameCenter,RunGate}/ParadoxConv.pas` + `ParadoxDataSet.pas`（**两份同源副本**） | 2,289 ×2 | ⚠️ 车道3 正在移植 RunGate 份；GameCenter 份**暂不重复移植**，建议 P2 把公共 Paradox 层上移到 `src/GXX.Core/**` 后两边共用（已通知车道9 登记该缺口） |
| 11 | `Source/GameCenter/**` 余部（GMain/GLoginServer/GHeroDB/DataBackUp/ParadoxConv…） | ≈ 15,000 | 待认领 |

---

## 6. 车道运行台账

| 车道 | 分支 | 工作树 | Delphi 源 | 目标 | 状态 |
|---|---|---|---|---|---|
| 车道1 | `par/lane-client-gui-mir` | `.worktrees/lane-client-gui-mir` | `GUI/Mir/{Mir176WindowsDlg,Mir185WindowsDlg,MirSequelDlg,HeroWindowsDlg,MirNewUI205Dlg}.pas` | `src/GXX.Client/GUI/Mir/**` | 进行中（P1） |
| 车道2 | `par/lane-resources` | `.worktrees/lane-resources` | `ReadResources/{Wzl,Uib,Wis}.pas` | `src/GXX.Client/ReadResources/**` | 进行中（P1） |
| 车道3 | `par/lane-rungate-utils` | `.worktrees/lane-rungate-utils` | `RunGate/{IniFilesEx,ParadoxConv,RunGatePluginInterface,VerifyCodeUtils,EncryptUnit_LF,uIPDownThread}.pas` | `src/GXX.RunGate/**` + `tests/GXX.RunGate.Tests/**` | 进行中（P1） |
| 车道4 | `par/lane-dbserver-utils` | `.worktrees/lane-dbserver-utils` | `DBServer/{Ranking,RouteEdit,FDBexpl,AddrEdit,uRunGateList,RouteManage,Setting,CreateId,TestSelGate}.pas` | `src/GXX.DBServer/**` + `tests/GXX.DBServer.Tests/**` | 进行中（P1） |
| 车道5 | `par/lane-dxcomponent` | `.worktrees/lane-dxcomponent` | `DxComponent/{DXConsts,DxCanvas,DxLabel,DxLine,DxImageProgress,DXTrackBar}.pas` | `src/GXX.Client/DxComponent/**` | 进行中（P1） |
| 车道6 | `par/lane-login-logdata` | `.worktrees/lane-login-logdata` | `LoginSrv/{BasicSet,MySqlAccountDB,uFrmDataManager,uFrmBatchEditAccountInfo,FrmFindId,EditUserInfo,MonSoc,FAccountView}.pas` + `LogDataServer/{LogManage,uFastFileStream,uFrmRemoteQuerySetting}.pas` | `src/GXX.LoginSrv/**`、`src/GXX.LogDataServer/**` + 两个新测试工程 | 进行中（P1） |
| 车道7 | `par/lane-client-guiconfig` | `.worktrees/lane-client-guiconfig` | `GameConfig/GameConfigDlgs.pas` + `GameConfig/Common/{ConfigShare,FilterItems,GameConfigDlg}.pas` + `GameConfig/Mirs/MirsConfigDlg.pas` | `src/GXX.Client/GUI/GameConfig/**` | 进行中（P1） |
| 车道8 | `par/lane-client-statewin` | `.worktrees/lane-client-statewin` | `GUI/NewStateWin/StateWindows.pas`（12,481 行，分片交付） | `src/GXX.Client/GUI/NewStateWin/**` | 进行中（P1） |
| 车道9 | `par/lane-gamecenter` | `.worktrees/lane-gamecenter` | `GameCenter/{GMain(6382),GCertServerSet,GLoginServer,GLoginServerRouteSet,CheckPrevious,uSqliteDB}.pas` | `src/GXX.GameCenter/**` + 新测试工程 | 进行中（P1） |
| 车道10 | `par/lane-selgate` | `.worktrees/lane-selgate` | `SelGate/**`（22 单元，先做对照审计，只补 GatewayKit 未覆盖的特有逻辑） | `src/GXX.SelGate/**` + 新测试工程 | 进行中（P1） |
| 车道11 | `par/lane-core-sql` | `.worktrees/lane-core-sql` | `Common/{MySqlCreateTableSql,MsSqlCreateTableSql}.pas` | `src/GXX.Core/Data/**` | ✅ **已合并 main（`657cbe15`）**：17 常量 / 79,755 字符 SQL，157/157 测试绿 |

**基线**：全部车道从 `main @ 96f0f646`（批次 J183）切出；`GXX.slnx` Debug 全量 build 已验证 0 error。

---

## 7. 工具与基线快照

### 7.1 `GXX.CSharp/tools/`

| 文件 | 作用 |
|---|---|
| `audit-coverage.ps1` | Delphi `.pas` → C# 映射审计（DoD「无未映射单元」的可执行形式）。证据：E1 同名 `.cs` / E2 某 `.cs` 头部提到 `<unit>.pas` / E3 Checklist.md 提及 / E4 `unit-map.tsv` 显式登记。判定：`MAPPED` / `ASSIGNED`（车道在飞）/ `CHECKLIST_ONLY` / `UNMAPPED` / `VENDOR`（第三方，不移植） |
| `unit-map.tsv` | 显式「单元 → 车道」登记表。车道认领某单元时在此加一行；该单元落地后被 E1/E2 识别为 `MAPPED`，即可删除该行 |
| `Merge-ParallelLanes.ps1` | 集成合并：校验主工作树干净 → 列出合并计划 → **冲突预检**（两条车道不得改同一文件）→ `git merge --no-ff` 逐条合并 → Release build + 全解决方案 test 门禁。`-DryRun` 只预览 |
| `lane-zones.tsv` | 车道文件分区（`docs/并行派发台账.md` §2 的机器可读镜像） |
| `verify-lanes.ps1` | **分区机械执行器**：对每条车道同时检查「已提交改动」与「工作树未提交改动」，报出 `OUT-OF-ZONE`（越区写）/`MODIFIED-EXISTING`（改了别人已有的文件，车道只许新增）/`SCRATCH`（`_tmp_src`、`.tmp-src`、`_recon` 等临时目录残留）。**派发后与合并前各跑一次** |
| `Register-TestProjects.ps1` | 把车道新建、但尚未登记进 `GXX.slnx` 的测试工程补进去（幂等；默认只报告，`-Apply` 才写）。合并时跑一次 |
| `Integrate-Wave.ps1` | **一键波次集成**（§12.3）：硬门禁 → 吸收主干 → 自动发现待合车道 → 跨车道碰撞预检 → 逐个 `--no-ff` 合并 → 登记+构建+逐工程测试（Integration.Tests 视为已知 flaky）→ `--ff-only` 提升 main 并比对 `^{tree}` 证明一致 |
| `gen/GenSqlCreateTable.ps1` | 建表 SQL 常量族生成器：从 `.pas` 抽取常量 → 生成 `src/GXX.Core/Data/*.cs` + 指纹测试，并跑 V1 往返 / V2 CRLF 普查 / V3 分号普查，任一不过就整体失败 |

用法：
```powershell
cd D:\chuanqi\daima\GXX原版_Delphi7
& .\GXX.CSharp\tools\verify-lanes.ps1                    # 分区体检（越区/改他人文件/临时文件残留）
& .\GXX.CSharp\tools\audit-coverage.ps1 -Report -ReportPath 'docs\并行覆盖审计.md'
& .\GXX.CSharp\tools\Merge-ParallelLanes.ps1 -DryRun
& .\GXX.CSharp\tools\Merge-ParallelLanes.ps1
```

> 注意：这两个 `.ps1` 刻意写成 **纯 ASCII**。Windows PowerShell 5.1 以 ANSI 解析 `.ps1`，
> 脚本里出现中文会导致解析失败（已踩过）。中文输出路径请从命令行参数传入（命令行通道是 UTF-8 安全的）。

### 7.2 基线覆盖快照（2026-09-19，车道开工前，`main @ 96f0f646`）

| 模块 | 单元 | 已映射 | 车道在飞 | 仅清单提及 | 未映射 | 第三方 |
|---|---|---|---|---|---|---|
| Client-HGE | 110 | 22 | 14 | 13 | 54 | 7 |
| M2Engine | 142 | 84 | 0 | 11 | 38 | 8 |
| RunGate | 43 | 7 | 5 | 3 | 25 | 3 |
| DBServer | 21 | 6 | 8 | 3 | 4 | 0 |
| Common | 190 | 25 | 0 | 7 | 12 | 145 |
| LoginSrv | 17 | 7 | 9 | 0 | 0 | 0 |
| LogDataServer | 7 | 0 | 3 | 3 | 0 | 0 |
| SelGate | 22 | 10 | 0 | 10 | 2 | 0 |
| GameCenter | 14 | 7 | 0 | 2 | 4 | 0 |
| **合计** | **566** | **169** | **42** | **53** | **139** | **163** |

> 判读：`VENDOR` 163 个属 `docs/转换开发文档.md` §2.3 的不移植项（VMProtect/VEH/FastMM/第三方库），
> 需在最终 DoD 里显式登记而非补翻译。真正待办 = `UNMAPPED 139 + CHECKLIST_ONLY 53`（后者是"清单里提过但没有直接产物"，多半是部分移植）。
> 未完的 `ASSIGNED 42` 即本波 8 条车道的在飞单元。

---

## 8. 经验与坑（踩过就记下来，别再踩）

### 8.1 PowerShell 5.1 按 ANSI 解析 `.ps1` —— 工具脚本一律纯 ASCII

- 含中文的 `.ps1` 必须带 UTF-8 BOM；但**编辑器保存时会丢 BOM**，于是"能跑→改一行→报语法错"反复出现（本项目已踩 4 次）。
- 结论：`tools/**.ps1` **全部写成纯 ASCII**（英文注释与英文输出）。中文路径/文本**作为命令行参数传入**（命令行通道是 UTF-8 安全的），不要在脚本里写中文**字面量**——包括 `$PSScriptRoot` 拼接时也要避免中文常量。
- 反过来，**生成的 C# 文件可以是 UTF-8**（C# 编译器默认按 UTF-8 读，无 BOM 也没问题）。

### 8.2 合并期"干线有未提交改动"不一定是阻塞

车道只**新增**文件，所以只要「干线脏文件集合」与「车道改动文件集合」**交集为空**，合并就是安全的
—— `Merge-ParallelLanes.ps1` 已按这个精确判据实现（早期版本是"干线必须完全干净"，过于保守，会让并行停摆）。
`-SkipBuild` 时脚本会明确提示"集成门禁已跳过"，并给出在**车道工作树**里跑门禁的命令：
主工作树跑 `dotnet build` 会与会话 A 抢 `obj/`，**不要在主工作树构建**。

### 8.3 车道自述必须与仓库实际对照

车道6 报告：任务书里写的"`LogDataMain`/`ThreadPool`/`LDShare`/`FileSearchPool` 已移植" **与实际不符**
（`src/GXX.LogDataServer` 只有 `Program.cs` + `LogDataService.cs`）。这说明 **Checklist.md 的"已完成"不能当作事实**，
`tools/audit-coverage.ps1`（同名 `.cs` 或 `.cs` 头部引用 `<unit>.pas`）才是可核验的证据。
派发下一波前，凡"据清单说已完成"的单元都要先 grep 复核。

### 8.4 待确认的跨模块不一致（记入待办，不要擅自"统一"）

- 车道6 发现：`BasicSet` 布尔 INI 写入用 `-1`/`0`（`TIniFile.WriteBool` → `BoolToStr`），
  而 `GXX.Core/Util/FastIniFile.cs` 的 `WriteBool` 写 `1`/`0`。两者语义不同——
  需要确认原版 `FastIniFile.pas` 的真实行为后再决定是否修正，**不得凭直觉统一**。

### 8.5 未认领的两份重复副本（P2 处理）

- `ParadoxConv.pas` / `ParadoxDataSet.pas` 在 `Source/RunGate` 与 `Source/GameCenter` 各一份（同源）。
  车道3 在移植 RunGate 份；GameCenter 份暂不重复，建议 P2 把公共 Paradox 层上移到 `src/GXX.Core/**` 后共用。
- `AsyncCalls.pas` 在 `M2Engine` / `Client-HGE` / `RunGate` **三份同源**（各 2,959 行）：移植一次到
  `src/GXX.Core/Async/` 三处共用，是性价比最高的一块 P2 目标。

### 8.6 实际发生过的越区事故与处置（必读）

- **事故**：车道3 把辅助脚本 `extract_paradox_tables.ps1` 写进了**主工作树**的 `GXX.CSharp/tools/`
  （不只是自己的 worktree）。会话 A 的 `git add -A` 随即把它扫进了 J185 提交，于是 main 上出现了这份文件；
  车道3 的分支又提交了同一路径 → 合并时会是 add/add。
  **处置**：确认两份 SHA256 完全一致（已核）；已要求车道3 用 `git rm --cached` 把该路径从分支移出。
  **教训**：`tools/**` 是集成者独占区；车道写文件必须**只用 `.worktrees/<lane>` 前缀的绝对路径**，
  写临时脚本时尤其容易图省事直接落到主工作树。
- **为什么这类事故不一定立刻暴露**：`Merge-ParallelLanes.ps1` 的脏文件过滤把 `GXX.CSharp/(docs|tools)/`
  整段屏蔽了（本意是忽略调度会话自己的登记文件），所以越区文件在干线视角是"隐形"的。
  `verify-lanes.ps1` 才是兜底 —— **派发后、合并前都必须跑**。
- **白名单特例的迁移**：车道1 的 `GUI/DxComponent/{DxControls,DxComponents}.cs` 合并时要迁到
  `src/GXX.Client/DxComponent/`，并统一命名空间 `GXX.Client.DxComponent`；这是唯一的路径正规化动作。

---

## 9. 集成分支与 P1 集成结果（2026-09-19/20）

### 9.1 集成分支 `par/integration`（本轮新增的关键机制）

**问题**：直接往 `main` 合车道，一旦某条车道与别的车道/干线冲突，`main` 就会处于编译不过的状态，
而 `main` 是会话 A 正在干活的分支 —— 这会直接打断它。

**做法**：新增一条**集成分支** `par/integration`（工作树 `.worktrees/integration`）作为暂存区：

```
par/lane-*  ──merge──►  par/integration  ──构建+全量测试通过后──►  main（fast-forward）
                              ▲
                       main 的新提交也先合进来（merge main）
```

于是 `main` **只会收到已经验证过的状态**，任何冲突/编译错误都止步于集成分支。

### 9.2 P1 集成结果

已并入集成分支并通过门禁的车道（40+ 车道提交）：

| 车道 | 车道提交数 | 集成分支状态 |
|---|---|---|
| lane-client-gui-mir（GUI/Mir 5 窗口 + DxComponent 接缝） | 4 | ✅ 已集成 |
| lane-client-statewin（StateWindows.pas 分片：字段表/方法面/经脉/称号/宠物/文本） | 4 | ✅ 已集成 |
| lane-client-guiconfig（GameConfig 族：ConfigShare/FilterItems/GameConfigDlg(s)） | 2 | ✅ 已集成 |
| lane-dxcomponent（DXConsts/DxCanvas/DxLabel/DxLine/DxImageProgress/DXTrackBar） | 2 | ✅ 已集成 |
| lane-dbserver-utils（DBServer 9 单元全量） | 7 | ✅ 已集成 |
| lane-login-logdata（LoginSrv 8 单元 + LogDataServer 3 单元） | 4 | ✅ 已集成 |
| lane-rungate-utils（IniFilesEx/ParadoxConv…） | 2 | ✅ 已集成 |
| lane-core-sql（建表 SQL 常量族） | 1 | ✅ 已并入 main（`657cbe15`） |
| lane-resources / lane-gamecenter / lane-selgate | — | 进行中 |

**集成门禁结果**（`par/integration`，Debug）：

```
GXX.Core.Tests 162 ✅   GXX.M2Server.Tests 5025 ✅   GXX.Client.Tests 2241 ✅
GXX.DBServer.Tests 199 ✅   GXX.LoginSrv.Tests 229 ✅   GXX.LogDataServer.Tests 142 ✅
GXX.GatewayKit.Tests 7 ✅                       合计 8005 passed / 0 failed
```

**已提升到 main**：`git merge --ff-only par/integration` → `48bc480f`；提升后用
`git rev-parse main^{tree}` 与集成分支 `HEAD^{tree}` 比对**完全相同**，故门禁结论对 main 直接成立
（这样就不必在主工作树跑构建，避免与会话 A 抢 `obj/`）。

### 9.3 集成期解决的两类真实冲突

1. **同一 Delphi 单元被两条车道各自造了接缝（重复劳动）**
   车道1 与车道5 都手写了一套 DxComponent 基础类型，**9 个同名类型**（`TClientVersion`、`TImageType`、
   `TDxFont`、`TDxCaptionColor`、`TDxImageIndex`、`TDxControl`、`TDxImageButton`、`TDxLabel`、`TClickSound`）
   同处 `namespace GXX.Client.DxComponent` → CS0101 编译失败。
   **裁决**：车道5 的 `src/GXX.Client/DxComponent/**` 为正式归属；车道1 的两个接缝文件改为
   独立命名空间 `GXX.Client.GUI.DxComponent`（服务 GUI/Mir 对话框族），
   并把引用它们的 9 个文件（8 源 + 1 测试）的 using 一并切换。车道1 已在自己的分支上照此提交
   （`b1bf8734`），集成分支无需再人工修补。
   **教训**：「只新建文件」不足以避免重复 —— **同一 Delphi 单元只能由一条车道负责**，接缝层也要登记归属。
   两者都只是"接缝"而非 1:1 全量移植，`DxControls.pas`(3,516) / `DxComponents.pas` 的完整移植仍是 P2 待办。
2. **一个集成测试偶发失败（不是回归）**
   `GXX.Integration.Tests.LoginSrvIntegrationTests.Client_Through_Gate_ToLoginSrv_RegisterAndLogin`
   在集成分支上 30s 超时失败。做了 A/B 对照：**trunk 在同一时间窗口也会失败**
   （round1 通过 188ms、round2 失败 30s），说明它是**固定端口 + 并发测试负载**下的既有 flaky 用例，
   与本轮集成无关。**判读规程**：集成门禁若只有这一条失败可放行，但必须在机器空闲时复跑确认。

### 9.4 根因修复：`GXX.Core.HUtil32.GetValidStr3`（跨车道共性缺陷）

车道4（DBServer）与车道7（GameConfig）**各自独立**发现并绕开了同一基线缺陷，故在集成期做了根因修复。

原文 `Source/Common/HUtil32.pas:1243-1341`（Delphi 7 走 `{$ELSE}` ANSI 分支）语义：
`Dest := Str` 起步；`Len=0` 或分隔符表为空直接退出；**「丢掉最前面的分隔符，不管多少个」**；
字段内遇到第一个分隔符 → `Dest` = 字段本体（不含分隔符）、`Result` = 分隔符**之后**的剩余串；
若只有前导分隔符 → `Dest` = 去掉前导后的剩余串、`Result` = 空。

旧 C# 实现有两处误读：① 只跳前导**空格**（不跳分隔符）；② 返回的剩余串**仍以分隔符开头**
（`Copy(str, strPos, strLen)` 而 strPos 正指向分隔符）。后果：

- 以分隔符开头的行（CSV/Tab 落盘格式常见）被整行解析为空 → `FilterItems.LoadFormList` 永远加载不出物品，`DBShare`/`AddrEdit` 同类；
- `while ((s = GetValidStr3(s, ref d, div)) != "")` 这类链式切割**原地打转**。

修复后全量回归：`GXX.M2Server.Tests` 5001 项、`GXX.Client.Tests` 1870 项**全部仍通过**，
说明旧行为没有被其它模块依赖；`GXX.Core.Tests` 中 2 处**误读原文**的期望
（`GetValidStr3_Basic` 断言 `rest == ",def"` 且注释"分隔符本身保留在剩余串中"）已按原文改写，
并新增 6 条语义/链式回归守卫。
**待办**：车道4 的 `GXX.DBServer/HUtil32Seam.cs` 与车道7 的 `FilterItems.cs` 本地复刻应改为转调本方法（去重）。

---

## 10. 车道交回的新增待办（P2 认领池补充）

> 全部来自车道交付报告，均已在其代码里注释留档；此处汇总以便下波派发时逐条消化。

| 序 | 待办 | 来源 | 规模/影响 |
|---|---|---|---|
| 1 | **去重**：`GXX.DBServer/HUtil32Seam.cs`、`GUI/GameConfig/FilterItems.cs` 的本地 `GetValidStr3` 复刻改为转调已修好的 `HUtil32.GetValidStr3` | 车道4 / 车道7 | 小；根因已修（§9.4） |
| 2 | `TFastIniFile.WriteBool` 写 `1/0` vs 原版 `TIniFile.WriteBool` 写 `-1/0` —— 需确认 `FastIniFile.pas` 真实行为后再决定，**禁止凭直觉统一** | 车道6 | 小；影响所有 INI 布尔落盘 |
| 3 | `Source/LogDataServer` 的 `LogDataMain / ThreadPool / LDShare / FileSearchPool` **并未移植**（现只有简化合并版 `LogDataService.cs`）—— 任务书曾误以为已完成 | 车道6 | 中；已有最小接缝，接入时替换接缝类型即可 |
| 4 | `LoginSrv` 侧 `LSShare / AccountDB / MasSock / GrobalSession / Parse / GateSet`、`DBServer` 侧 `RoleDB / SqliteRoleDB / MySqlRoleDB / uFrmRoleDataEdit` 仍未移植（已有接缝） | 车道6 / 车道4 | 中 |
| 5 | `MirsConfigDlg.pas` 用了 6 个 `GameConfigDlg.pas` 的 `TConfigChecked` **不存在的成员**，且基类声明的 `RefKeyboardConfig/AddToBossList/…` 在 Mirs 单元无实现 → 该单元针对**另一版公共头**。当前用显式映射表 + 合成槽位兜住；若能找到权威版 `GameConfigDlg.pas` 应替换 `MirsConfigCheckedMap` | 车道7 | 中；映射语义风险 |
| 6 | `MirsConfigDlg.pas` 未覆盖：`Initialize`/`LoadClientConfig`/`LoadConfigFile`+`SaveConfigFile`(约 460 行 INI)/自动用药全流程/`RefUseItemConfig`/UI 类 —— 均与未移植的 DxComponent 控件树深度耦合 | 车道7 | 大；等 DxComponent 全量后补 |
| 7 | `DxControls.pas`(3,516) / `DxComponents.pas` 的**完整**移植（车道1/车道5 目前都只做了接缝面）；`DIB.pas`(8,087) / `DxMemo.pas`(5,139) / `DxImageEdit` / `DxMainBottomForm` / `DxImageForm` / `DxMagicBall` / `GfxFonts` / `Magnetic` 等控件族 | 车道1 / 车道5 | 大；P1 未认领的主力 |
| 8 | `CreateId.pas` **只有窗体骨架**，没有任何 ID 生成规则（真正的分配在 RoleDB 侧）—— 任务书里的"0/最大值/已存在边界测试"在本单元无对应代码 | 车道4 | 小；已加结构断言防"顺手补全" |
| 9 | `FDBexpl.pas` 依赖的 `HumDB/Main/NewChr/frmcpyrcd` 四个单元**在源码树中不存在**，原文本身不可单独编译（`THumDataInfo`/`HumAddItems` 长度 30 待核对） | 车道4 | 中；需先定位或判定为缺失 |
| 10 | `StateWindows.pas` 真实行数 **14,027**（非旧统计 12,481）；**无 .dfm**，布局来自 `LoadDxControlEx.LoadCompressedUIData('STATE_WIN_UI','ZDAT')` 压缩 UI 流；`TStateWindows` 真类待 DxComponent 全量后才能建 | 车道8 | 大；批次2-9 已按行号分组待续 |
| 11 | `GUI/NewStateWin`、`GUI/Mir/*`、`DxComponent/DxLabel|DxLine|DxImageProgress|DXTrackBar` 等**都没有同名 .dfm**，设计期取值落在各 `.GUI` 二进制文件的 `Tgui*` 记录里 —— "按 .dfm 逐条对齐"这类要求对这些单元不适用，已在各文件注释中登记偏差 | 车道1 / 车道5 / 车道8 | 规程性；后续派发时应改为"以 .GUI 记录为布局真源" |
| 12 | `GXX.Integration.Tests` 的 `Client_Through_Gate_ToLoginSrv_RegisterAndLogin` 是**固定端口 + 并发负载下的 flaky 用例**（trunk 同样偶发失败） | 集成期诊断 | 小；建议改为随机端口或加串行集合 |

---

## 11. P1 波次收官（11 条车道全部并入 main）

### 11.1 结果

`main = ab8b51d7`，与集成分支 `HEAD^{tree}` **完全相同**（故门禁结论直接对 main 成立）。
**11 条车道 / 40+ 车道提交全部吸收**（`main..par/*` 全为 0）：

```
GXX.Core.Tests 162 ✅   GXX.M2Server.Tests 5053 ✅   GXX.Client.Tests 2278 ✅
GXX.SelGate.Tests 163 ✅  GXX.RunGate.Tests 248 ✅    GXX.GameCenter.Tests 219 ✅
GXX.LoginSrv.Tests 229 ✅ GXX.DBServer.Tests 199 ✅   GXX.LogDataServer.Tests 142 ✅
GXX.GatewayKit.Tests 7 ✅                       合计 8700 passed / 0 failed
```

期间 `main` 被会话 A 推进了 **3 次**（J185/J186/J187）——集成分支每次先 `merge main` 再重跑门禁，
`main` **一次都没有收到过编译不过的状态**。机制成立。

### 11.2 覆盖审计的**误判**被车道抓出（已修工具）

车道9 报告：审计把 GameCenter 的 `GShare/DataBackUp/uSqliteDB/CheckPrevious` 标为"已映射"，
而证据只是 `GameCenterService.cs` / `Program.cs` 里的**顺带注释提及**，实际并不存在移植产物。

根因是我的 E2 证据规则太宽（"某个 src .cs 里任意位置提到 `<unit>.pas`"）。**已收紧**：

- E2 现在只认 **`.cs` 文件头 40 行内**的提及（真正的移植会在文件头写明源单元）；
- 文件后段的提及降级为 **`WEAK`** —— 单独统计，**永不计入已映射**；
- 于是"已映射"数字**变诚实了**：mapped 从虚高的 239 降到 **214**，同时多出 `WEAK 42`。

**收紧后的基线**：`mapped 214 / weak 42 / checklist-only 39 / unmapped 107 / vendor 163`。

> 教训：覆盖率审计的**假阳性**比假阴性危险得多——它会让人误以为已经翻完。
> 任何"证据来自某处提及"的判定，都必须限定**位置**（文件头）而不是"存在"。

### 11.3 "改了源码但测试结果不变" —— 是**陈旧构建产物**，不是编译器缺陷

- 车道10 报告称 .NET SDK 11.0.100-rc.1 会"错译三元表达式/局部变量/元组解构"，并建议全车道改用 if/else 规避；
- 车道3 独立报告同一现象，但给出的原因是**工作树内增量编译偶发未刷新产物**，建议清 `bin/obj` 重跑；
- 我在处理一个 SelGate 用例时**亲自复现**：`Share.MakeIPToInt("127.0.0.1")` 在测试里读回 -1，而在同程序集的探针里读回 `0x0100007F`——**清掉该测试工程的 bin/obj 重建后一切正常**。

**结论：按车道3 的解释处理**（陈旧产物），**不要**采用"回避三元/元组"的写法——那会把本来正确的代码改坏。
**操作规则**：测试结果与源码明显矛盾时，先 `Remove-Item -Recurse -Force <项目>\bin,<项目>\obj` 重建，
再怀疑代码；**最后**才怀疑编译器。

### 11.4 一个真实陷阱：`MakeIPToInt` 无法区分"失败"与 `255.255.255.255`

`Share.MakeIPToInt` 失败时返回 `-1`（INADDR_NONE），而 `255.255.255.255` 按主机序也正好是 `0xFFFFFFFF` → 同为 `-1`。
**不要用 `!= -1` 当成功判据**（我在修这个测试时先踩了一次：实现是对的、错的是我的期望值——
又一次印证"测试失败 ≠ 实现有错"）。该不可区分性源自原设计，已登记，**不得顺手修正**。

### 11.5 生成物文件会造成合并冲突

`docs/并行覆盖审计.md` 是**生成物**，而主工作树与集成分支各生成过一份 → 合并冲突。
**处置规程**：生成物冲突一律**取集成侧再重新生成**，不要手工合并文本。
（同类：`*.g.cs`、`Checklist.md` 的机器段落。）

### 11.6 车道9 新报的两处 `GXX.Core` 一致性缺陷（P2，已用本地包装绕开，未改基线）

1. `Rtl/DelphiRTL.cs` 的 `FormatOne` 对**布尔实参**按 `Convert.ToInt64` 渲染成 `1/0`，
   而 Delphi `Format` 对 Boolean 走 `BoolToStr` → `-1/0`。车道9 用 `GameCenterFormat` 包装规避。
2. `HUtil32.BoolToStr` 返回 `"True"/"False"`，而 `SysUtils.BoolToStr` 是 `"-1"/"0"`。
   车道9 用 `DelphiSystem.BoolToStr` 规避；车道6 也报过 `TFastIniFile.WriteBool` 写 `1/0` 的同类问题。

> 三处都指向同一个根因：**Delphi 的布尔字符串化是 `-1/0`**。建议 P2 派一条 `GXX.Core` 车道统一收口，
> 并把这批本地包装逐个改回调用（与 §9.4 的 `GetValidStr3` 同一套路：**根因修一次胜过 N 处本地绕开**）。

### 11.7 事故：`git worktree remove --force` 中途失败，留下"能写主干"的残骸

**经过**：P1 收官后我批量清理已合并的车道工作树，其中 `.worktrees/lane-resources` 的 `git worktree remove --force`
报 `Directory not empty` 失败，**但它已经把该目录的 `.git` 链接文件删掉了**。后果：那个目录从"独立工作树"退化成
**普通目录**，在里面跑任何 git 命令都会解析到**主工作树 / main** —— 车道2 当时还在里面工作，
它一旦 `git add/commit` 就会直接写主干。车道2 发现异常后**立即停止 git 写操作并上报**，处置正确。

**处置**：抢先把 `.lane-backup/` 抢救到 `.worktrees/_salvage/`，然后**整目录删除**，并为该车道
在新路径 `.worktrees/p2-resources-uib` 重建干净工作树。

**规程（新增）**
1. **绝不在车道 agent 还在运行时删除它的工作树**；先确认该车道已结束。
2. `git worktree remove` **必须检查结果**：确认目录已消失；若失败，**不要留在那里**——
   立刻 `Remove-Item -Recurse -Force <目录>` + `git worktree prune`。
3. 判定"某目录是不是真工作树"：`Test-Path <目录>\.git`（文件或目录）必须为真；
   否则里面的 git 命令会落到别处，**这是最危险的状态**。
4. 删工作树前先看有没有**未提交/未跟踪**的成果（本例 UIB 实现就是在这里丢的，只有测试文件被抢救出来）。

### 11.8 一起"main 编译不过"的误报（复核规程再次生效）

车道2 报告 `GXX.Client.Tests` 在 main 上 `error CS0103: TMagicPlusLevel` 不存在、0 tests 可跑。
**在干净工作树（pristine `ab8b51d7`）实测：`dotnet build tests\GXX.Client.Tests` → 0 error，
`dotnet test` → 2308 passed / 0 failed。** 该报错来自那个**被破坏目录里的旧树**（基线 `96f0f646`），不是 main。

**复核规程**：任何"main/别人编译不过"的结论，**必须先在干净工作树里复现**再上报；
被破坏/半删除的工作树会给出极具误导性的结果。

### 11.9 一起"漏合并"的发现（靠分支对账抓出）

同一轮对账发现 `main..par/lane-resources` 仍是 **1**：车道2 的 **WIS** 提交（`f0f3edce`，`Wis.cs` 1136 行 + 测试）
当时只合了 WZL、**漏了 WIS**（此前我误判为"fully integrated"）。已补合并，main 现为 `acc683eb`，Client.Tests 2308 全绿。

**规程**：收官时必须逐分支跑 `git rev-list --count main..par/<lane>` 对账，**不能只看"看起来合过了"**。

---

## 12. P2 波次（2026-09-20 派发，9 条车道）

基线 `main = acc683eb`（含 P1 全部 11 条车道 + 会话A J185–J187）。
本轮起 **`tools/lane-zones.tsv` 支持 `!` 前缀 = ALLOW-MODIFY 授权**（允许车道修改既有文件），
`verify-lanes.ps1` 已实现；只有"修既有共享文件"这类车道才会拿到该授权。

| 车道 | 工作树 / 分支 | 目标 | 独占区 |
|---|---|---|---|
| `p2-core-rtl` | `par/p2-core-rtl` | **唯一带 `!` 授权的车道**：Delphi 布尔字符串化（`-1/0`）在 GXX.Core 收口 + 消除两处 `GetValidStr3` 本地复刻 | `!DelphiRTL.cs`、`!HUtil32.cs`、`!FastIniFile.cs`、`!DBServer/HUtil32Seam.cs`、`!GameConfig/FilterItems.cs` + 新测试 |
| `p2-core-async` | `par/p2-core-async` | `AsyncCalls.pas` **三份同源副本**（M2Engine/Client-HGE/RunGate，各 2,959 行）移植一次到 `GXX.Core/Async/` | `src/GXX.Core/Async/**` |
| `p2-dbserver-mysql` | `par/p2-dbserver-mysql` | `MySqlRoleDB.pas`(5,539) —— SqliteRoleDB 的 MySQL 镜像 | `src/GXX.DBServer/MySqlRoleDB*` |
| `p2-rungate-impl` | `par/p2-rungate-impl` | `RunGateUtils.pas`(4,393) + `Common/{IocpWinsock2,uBuffer,IocpUtils}.pas` + `uFrm*.pas`；先做对照审计，已由 GatewayKit 覆盖的不复制 | `src/GXX.RunGate/{RunGateUtils,Iocp,uBuffer,uFrm}*` |
| `p2-resources-pak` | `par/p2-resources-pak` | `Pak.pas`(2,798) | `ReadResources/Pak*.cs` |
| `p2-resources-uib` | `par/p2-resources-uib` | `Uib.pas`（车道2 接手；其旧工作树已在 §11.7 事故中损毁，UIB 实现需重建） | `ReadResources/Uib.cs` |
| `p2b-m2-dblayer` | `par/p2b-m2-dblayer` | M2Engine DB 层 6 单元 ≈9,244 行（`MySql/SqliteUserShopDB`、`Sqlite/M2DataDB`、`MySql/SqliteAuctionDB`）→ **只在新建 `DbLayer/` 子目录写文件**（`src/GXX.M2Server/**` 是会话A 常驻区） | `src/GXX.M2Server/DbLayer/**` |
| `p2b-m2-plugins` | `par/p2b-m2-plugins` | `PluginInterface.pas`(2,484) + `PluginImplement.pas`(5,472) → **只在新建 `Plugins/` 子目录写文件**；重点是接口成员齐全性反射测试与托管侧映射 | `src/GXX.M2Server/Plugins/**` |
| `p2c-common-crypto` | `par/p2c-common-crypto` | `Common` 尾部：`AESUtils`(1,218)/`DesUnit`(824)/`HashUnit`+`HashObjList`(464)/`CheckUnit`(127)/`ZipUnit`+`CompressUnit`(73)/`UpdateCommon`(56) + `MemoryModule*` 按 §2.3 登记为 Stub | `src/GXX.Core/{Crypto,Util,Compress,Stubs}/…`（逐个文件精确授权） |
| `p2c-client-tail` | `par/p2c-client-tail` | Client-HGE 的"小单元尾巴"扫尾：`WinSock2`/`HerbActor`/`UpdateEngine`/`GlobalString`×2/`IECache`/`StringHashMap`/`DxImageButtonEx`/`GuiManage`/… 约 20 个单元 ≈8,000 行（**覆盖率收尾**型，切片极易） | `src/GXX.Client/Tail/**` |
| `p2-dxcontrols-rest` | `par/p2-dxcontrols-rest` | DxComponent 余部：`DxControls.pas`(3,516) 全量 + `DxImageForm`/`DxImageButton`/`GfxFonts`/`Magnetic`/`DxMagicBall` | `DxComponent/{DxControls,DxImageForm,DxImageButton,DxMagicBall,GfxFonts,Magnetic,DxControlsExt*}.cs` |
| `p2-client-loaddx` | `par/p2-client-loaddx` | `LoadDxControl.pas`(1,550) + `LoadDxControlEx.pas`(2,125) —— **客户端所有窗口布局的真源**（GUI 窗口没有 .dfm，布局来自这里的压缩 UI 流） | `src/GXX.Client/LoadDx/**` |
| `p2-client-fstate` | `par/p2-client-fstate` | `FState.pas`（**22,503 行，全工程最大**）分片移植：声明段 100% → 纯数据/纯函数 → 消息/状态机 | `src/GXX.Client/GUI/Share/**` |

**遗留给 P3**：`MySqlRoleDB` 之外的 M2Engine DB 层（`MySql/SqliteUserShopDB`、`SqliteM2DataDB`、`MySql/SqliteAuctionDB`、`MySqlM2DataDB`）、`PluginInterface/PulginImplement`、`DIB.pas`(8,087)、`DxMemo.pas`(5,139)、`StateWindows.pas` 批次2-9(约13,000行)、`FState.pas` 余部、`SerialWindowsDlg`(34,266) 分片、`ParadoxConv/DataSet` 公共层上移、`GXX.Integration.Tests` flaky 用例串行化。

**清理待办**：`.worktrees/_salvage/`（车道2 抢救出来的 3 个测试文件）在 `p2-resources-uib` 收官后删除。

### 12.1 已完成并并入 main

| 车道 | commit | 产出 | 门禁 |
|---|---|---|---|
| `p2-resources-uib` | `a840dabe` | `ReadResources/Uib.cs`(668) + `ResourceUibTests.cs`(22) + `ResourceUibExtraTests.cs`(15) | Client.Tests **2345 passed / 0 failed**，已 FF 进 main（`5bd12133`） |

> 该车道的 `Uib.cs` 是在 §11.7 事故中**重建**的：先按 `Uib.pas` 逐块写实现，再用事故前抢救出来的 22 条测试
> 全绿作为**交叉验证**（那些测试锁死了 `GetCachedBright` 守卫位置、`IndexList.Add` 时机、`GetCachedImageSize` 恒 0 等细节），
> 因此"重建与原实现行为一致"是有证据的，不是凭空猜测。这条做法值得后续复用：
> **当实现对丢失但测试还在时，测试就是最强的行为规格。**

### 12.2 工具修复：`verify-lanes.ps1` 读分区表必须按 UTF-8

分区表 `lane-zones.tsv` 里含中文报告路径（`docs/并行报告-*.md`）。脚本原先用 `Get-Content`（PS 5.1 默认 **ANSI**）
读取，导致中文 glob 被解码成乱码 → 车道**合法**的报告文件被判成 `OUT-OF-ZONE`（本轮真实误报了一次）。
已改为 `Get-Content -Encoding UTF8`。
**教训**：凡是"配置/清单里含非 ASCII"的脚本，读文件必须显式指定编码——否则得到的是一组**看起来像 bug 的假阳性**。

### 12.3 一键波次集成：`tools/Integrate-Wave.ps1`（本轮新增）

前几轮的集成是**手工八步**（合车道 → 构建 → 合主干 → 再构建 → FF main → 比对树），
而且 §11.9 那次"漏合并"正是手工流程漏一步造成的。现在收敛成一条命令：

```
.\GXX.CSharp\tools\Integrate-Wave.ps1 -DryRun      # 只预览
.\GXX.CSharp\tools\Integrate-Wave.ps1             # 全流程
.\GXX.CSharp\tools\Integrate-Wave.ps1 -Lanes p2-resources-uib -SkipTests
```

它按序做：**① 必须在主工作树运行 → ② 硬门禁 `verify-lanes.ps1` 零违规才继续 → ③ 先把 main 合进集成分支
→ ④ 自动发现所有"尚未并入集成分支"的 `par/*` 车道（或 -Lanes 指定）并做**跨车道同路径碰撞预检**
→ ⑤ 逐个 `--no-ff` 合并，冲突即中止并提示"生成物冲突要取集成侧再重新生成" → ⑥ 登记新测试工程 + 构建 +
**逐个测试工程跑**（`GXX.Integration.Tests` 标记为已知 flaky：报告但不致命） → ⑦ `--ff-only` 提升 main
并用 `^{tree}` 比对证明 main 与集成分支**完全一致**。**

设计要点：**主工作树全程不跑构建**（避免与会话 A 抢 `obj/`），构建只在集成分支工作树里发生；
每一步失败都有独立退出码，不会"半途成功"。

### 12.4 事故：车道在 `src/` 下造出 `obj_s/`、`obj_subB/`，`.gitignore` 拦不住

`p2-rungate-impl` 为了并行跑构建，用 `-p:BaseIntermediateOutputPath=obj_s`（另有 `obj_subB`）
把 MSBuild 中间产物生成在 `src/GXX.Core/obj_s/` 等目录。问题：`.gitignore` 只忽略**恰好**的 `obj/` 与 `bin/`，
这些自定义名**不在忽略范围**，`git add -A` 会把它们提交进仓库。

**处置（两层，都已落地）**
1. **检测**：`verify-lanes.ps1` 的 SCRATCH 规则扩展为前缀匹配（`_tmp*`、`_recon*`、`_probe*`、`.scratch*`、`obj*`、`bin*` …），
   并改用 **`-cmatch` 大小写敏感**匹配 —— 否则 `Source/.../Objects.pas` 会被误判成 `obj*` 目录。
2. **根因**：把这类目录写进 `.git/info/exclude`（**全局、未跟踪、不进任何提交**）：
   `_tmp_src/`、`.tmp-src/`、`_recon/`、`_probe*/`、`.scratch*/`、`_scratch/`、`_stage/`、`_salvage/`、`obj_*/`、`bin_*/`、`obj-*/`、`bin-*/`、`_tool*.ps1`、`*.patch`。
   效果：车道工作树里的探查/中间产物**再也不可能被 `git add -A` 扫进提交**，
   `verify-lanes` 的噪声从 95 条降到 **0–1 条真实问题**。

> 教训：**"禁止提交临时文件"这条规程，靠反复叮嘱是拦不住的，要靠 ignore 规则让它物理上不可能发生。**

### 12.5 事故：门禁"假绿"——测试汇总行没解析出来，却被当成通过

`Integrate-Wave.ps1` 第一版用 `dotnet test` 的中文汇总行（`已通过!` / `失败!`）做判定。某次运行时
`dotnet` 输出的是**英文**（`Passed! - Failed: 0, Passed: 2618`），于是 11 个工程全部拿不到汇总行、
被判为 `(no summary)`，而代码却**把它当成通过**继续走到"提升 main"——**门禁实际上什么都没验证**。

**修复（两条）**
1. 脚本开头 `$env:DOTNET_CLI_UI_LANGUAGE = 'en'` 固定英文输出；脚本本身保持**纯 ASCII**。
2. **解析不出汇总行 = 失败**（绝不是通过），并把输出尾部打印出来便于诊断。

> 教训：门禁的"未识别状态"必须默认判**失败**；默认判通过的检查等于没有检查。

### 12.6 事故：为了绕开 §8.1 而"无差别剥离非 ASCII"，把正则改坏了

为了修 §12.5，我往脚本里写入了含中文的正则；随后用 `[regex]::Replace($t,'[^\x00-\x7F]','')` **无差别剥离非 ASCII**
（本意是遵守 §8.1 的 ASCII-only 约定）。结果 `失败:\s*[1-9]` 被剥成 `:\s*[1-9]` —— 它会匹配
`Passed! - Failed: 0, Passed: 2618, ... Duration: 2 s` 里的 **`Duration: 2`**，于是**每个通过的工程都被判为失败**。

**修复**：不再做短语匹配，改为**显式解析计数**：`Failed:\s*(\d+),\s*Passed:\s*(\d+)`，只看捕获到的数字。

> 教训：**自动化的"清理/规范化"会静默改变语义**。剥离后必须回读被改动的那几行；
> 能用"显式结构解析"的地方，绝不要用"短语匹配"。

### 12.7 P2/P2b/P2c 波次集成结果（本轮一次提升）

`main` 由 `488b770e` → **`4acf4376`**，一次提升 **5 条车道 / 43,120 行插入**；与集成分支 `^{tree}` 逐字节一致。

| 车道 | 产出 | 规模 |
|---|---|---|
| `p2-client-loaddx` | `LoadDx/**`：压缩 UI 数据（`ZDAT`）逐字段格式 + 52 条 `TGui*` 记录脚本生成 + 两单元共用的 `LoadComponent` 全量 + 24 个控件标签映射 | 10,461 行 |
| `p2-client-fstate` | `GUI/Share/**`：`FState.pas` **声明段 100%**（常量 9/类型 24/字段 201/方法面 538，四张清单带 SHA-256）+ 75 个实现例程 | 10,027 行 |
| `p2-dbserver-mysql` | `MySqlRoleDB` **81/81 例程全覆盖**，SQL 140+31 条脚本抽取并独立回读比对 0 处不一致 | 9,725 行 |
| `p2b-m2-dblayer` | `DbLayer/**` 切片1：`Sqlite/MySqlUserShopDB` 全覆盖 + 接缝层（41/43 条 SQL 保真链） | 7,269 行 |
| `p2c-client-tail` | `Tail/**`：`WinSock2`（463 常量脚本抽取）+ `GlobalString` 等 8 个小单元 | 5,638 行 |

**门禁（修正后的解析）**：`Client 2745 / M2Server 5132 / Core 464 / DBServer 371 / LoginSrv 229 /
GameCenter 219 / SelGate 163 / LogDataServer 142 / RunGate 248 / GatewayKit 7 / Integration 2`
= **9,722 passed / 0 failed**（Integration.Tests 已按已知 flaky 容忍，本轮实际通过）。

**车道交回的重要事实更正（已按"回读原文"采信）**
1. `p2-core-rtl` 推翻了我派发时的三处前提：`HUtil32.BoolToStr` 原文返回的是 **`'TRUE'/'FALSE'`**（不是 `-1/0`；
   `-1/0` 是 **SysUtils.BoolToStr**）；原文**没有** `StrToBoolDef`；`FastIniFile.WriteBool` 继承 RTL
   `TCustomIniFile.WriteBool` = **`'1'/'0'`，C# 原本就对**，真正的缺陷在 **`ReadBool`**（已修）。
   → 台账 §8.4 与 §10-2 的悬案据此**结案**。
2. `p2-client-fstate` 实测：**`FState.pas` 只有一个单元级全局**（`FrmDlg`）。此前多处接缝注释把
   `THintLines`(实为 `DrawScrn.pas:318`)、`GetHintFontSize/Style/Stroke`、`GetRGB`(MShare.pas:2897)、
   `g_MySelf/g_MyHero/g_MagicList/g_DefColorTable/g_AcupointLevels`(MShare.pas) 归给 FState.pas —— **应更正**。
   另：`GetGodBlessItems` 是**复数**且为嵌套函数；原文 23699-23773 用 `(* *)` 注释掉了一整份重复的
   `DNPC_PLAYIMG_PAINT`，**按行号扫例程的脚本必须识别块注释**。
3. `p2-client-loaddx` 确认三个 GUI 家族的布局真源与资源名（`MIR_CONFIG_DLG_UI` / `MIR_UI` / `STATE_WIN_UI`），
   并给出 `IDxGuiResourceProvider` 注入点 —— 后续窗口批次应基于它，而不是去找不存在的 `.dfm`。

**新增待办（P3）**
- `GUI.Mir.TFrmDlg` 与 `GUI.Share.TFrmDlg` **同名同源重复**（已实测可触发 CS0104）→ 应统一到 `Share` 版并删接缝（§9.3 同类问题第三次）。
- `p2b-m2-dblayer` 只剩 1/3：`Sqlite/MySqlM2DataDB` + `Sqlite/MySqlAuctionDB`（≈6,300 行）侦察已完成、可直接开工。
- DxComponent 余部（`DIB` 8,087 / `DxMemo` 5,139 / `DxImageEdit` / `DxMainBottomForm` …）、`StateWindows` 批次2-9、`FState` 余部 300 例程。
- `HUtil32.Str_ToInt` 解析失败吞掉 `Def`（原文保留 `Def`）：改动面广，需连同 `LoginSrv.Tests/BasicSetTests` 的差异断言一起修。

### 12.8 第三、第四次"重复接缝"碰撞，以及新的裁定

`Integrate-Wave.ps1` 的门禁连着抓到两起**真实的跨车道同名类型碰撞**（都是 §9.3 同一模式的复发）：

1. **`GXX.Client.LoadDx` ↔ `GXX.Client.DxComponent`**（本轮把 dxcontrols 车道挡在门外）：
   `TDxControlRef`、`TAlignEx` 同时存在于两个命名空间 → 集成构建 **20+ 处 CS0104**。
   **裁定**：`GXX.Client.DxComponent` 为正式归属（`DxControls.cs` 现已是 `DxControls.pas` 全量移植）；
   `LoadDx` 当初自造它们只因该文件尚不存在 → 已授权 `p2-client-loaddx` 车道**删除自己的重复声明并改引用**。
2. **`TDxImageButton`**：车道5 早先把最小接缝落在**只读的** `DxLabel.cs` 里，导致 `p2-dxcontrols-rest`
   的全量 `DxImageButton.cs` 触发 `CS0101`，它把已写好的实现**撤回**并上报（处置正确）。
   **裁定**：已把 `DxLabel.cs` 加入该车道的 `!` ALLOW-MODIFY 白名单，由**它**删掉那段旧接缝
   （`TDxLabel : TDxControl`）后再重提全量实现。
3. `DxControls.pas` 的**上游接缝远比台账 §9.3 描述的薄**（无 `SetFocus`/`FindActiveControl`/`GetRootCtrl`/
   `Insert`/`Remove`/`Move`/`SetMousePoint` …）。该车道把缺的成员全部实现在静态 `DxControlOps`（首参 Self）。
   **待办 P3**：单独排一个"把 `DxControlOps` 融进 `TDxControl`"的批次（需把 7 个只读文件里的 `TDxControl` 改 `partial`）。

> **第三次复发的教训**：车道被禁止修改既有文件，于是**只能另造一份同名接缝**——这恰恰制造了冲突。
> 处置规程已明确：**只有在"正式归属文件缺失"时**才允许车道造接缝，且必须在文件头写明"待 <单元> 移植后由该类型接管"，
> 并在报告里**显式申请**后续归属；集成者一旦发现正式归属落地，必须立即用 `!` 授权原车道去重复制。

### 12.9 门禁的环境性失败：MSBuild 子节点崩溃（不是代码问题）

本轮门禁报 `MSBUILD : error MSB4166: Child node "3" exited prematurely`，使 `LoginSrv.Tests` 与
`M2Server.Tests` 拿不到汇总行 → 按 §12.5 的新规则**判为失败**（规则本身工作正常）。
**根因是资源耗尽**：当时 6 条车道在同时跑 `dotnet build`。**单独复跑两者均全绿**
（LoginSrv 229 / M2Server 5153），确认与代码无关。

**规程**：门禁出现 `MSB4166` / 子节点崩溃 / 无法解析汇总时，**先单独复跑受影响的工程**；
若单独复跑全绿即判定为环境性，可放行（并在报告里写明"哪几个工程是单独复跑验证的"）。
不要把这类失败当成代码缺陷去改代码。

### 12.10 本轮（P2/P2c 第二批）集成结果

`main` = **`17247af5`**（+`p2c-client-tail` 12 文件 / 7,880 行）。
**全部 11 个测试工程均已验证通过**：Client 2912 / M2Server 5153 / Core 464 / DBServer 371 /
LoginSrv 229 / GameCenter 219 / SelGate 163 / LogDataServer 142 / RunGate 248 / GatewayKit 7 / Integration 2
= **9,910 passed / 0 failed**（M2Server 与 LoginSrv 为单独复跑验证，见 §12.9）。

**`p2-resources-pak` 交回的两条跨车道发现（待转办）**
1. **`GXX.RunGate/DesNew2.cs` 存在真实移植缺陷**：`DesUtils.pas` 与 `DesNew2.pas` 同源，但
   `DesNew2.cs` 的 `JenkinsMix` 尾段**漏掉全部左移、且 `k+4`/`k+5` 用反**（原文 569-572）。
   该车道的 `PakCrypto.PakDesNew.JenkinsMix` 已按原文修正 → **需派 RunGate 车道复核 `DesNew2.cs`**。
2. **"AnsiString 承载裸二进制"陷阱**：Delphi 的密文字节串写回 `ShortString` 是逐字节拷贝；托管侧若
   "Latin-1 承载、GBK 取回"会把 `0x80–0xFF` 重新编码（实测 5 字节 → 7 字节），Pak 头校验全挂。
   建议在 `GXX.Core` 下沉一个"Latin-1 字节透明"工具供 Des/AES/WZL 复用。
   另：`GXX.Core.Compress.ZlibEx.DecodeRLE` 是 **RLE90 变体**，与 `RLEUnit.pas` 的行程编码**不同**，不可互用。

### 12.11 未闭合阻塞：`DxImageButton` 成员契约（两车道接口名不一致）

**现象**：集成 `p2-dxcontrols-rest` 时构建报 `CS0426: 类型 TDxImageButton 中不存在类型名 TClickSound`
（`LoadDx/DxControlSeams.cs:9`、`LoadDx/GuiRecords.g.cs` 多处），以及若把该引用改为顶层 `TClickSound` 后
继续报 `CS1061: TDxImageButton 未包含 ClickSound / Style`。

**成因**：这是一对**互相耦合**的车道，两边各自都对，只是接口名不一致 ——
`p2-dxcontrols-rest` 按授权删掉了 `DxLabel.cs` 里的**嵌套** `TClickSound` 与最小 `TDxImageButton`，
改落**全量** `DxImageButton.cs`；而 `p2-client-loaddx` 的加载器是按**旧的嵌套/最小面**写的。

**裁定（已发给 dxcontrols 车道）**：**以原文属性名为准**，由"正式归属"那侧定名，另一侧跟随：
原文 `DxImageButton.pas` 的 `FClickSound`/`FButtonStyle` 对应哪个公开属性名就保留哪个；
若原文确有 `Style` 别名属性则补别名，否则给出**精确成员契约清单**由集成方转给 `LoadDx` 侧对齐。
**在它交付契约清单前，该车道不并入 main。**

**新增卫生规程**：门禁失败并中止后，**必须把集成分支 `reset --hard main`**，不要留一个"合了一半、编译不过"的暂存态 ——
否则下一次波次会在脏基线上继续叠加，问题越滚越大。本轮即按此把集成分支复位到与 main 完全一致。

## 13. 事故：宿主重启把 8 条在飞车道同时杀死 —— 以及由此确立的抢救规程

### 13.1 现象

P2/P3/P4 波次共 **8 个子代理**（`p2-rungate-impl`、`p2-dxcontrols-rest`、`p3-dx-big`、`p3-m2-dbdata`、
`p3-m2-sweep`、`p4-rungate-mirclient`、`p4-m2-objnpc`，加 1 条只读甄别代理）**全部**以
"failed before it finished"、**无任何收尾消息**结束。同时宿主发生了模型切换。事后证明这是**宿主级联杀**，
与任务本身、与提示词质量都无关（重新派发后同样提示词可正常开工）。

### 13.2 血泪教训：`git log` 看不到车道的在飞工作

第一反应是"8 条车道什么都没产出"（`main..par/*` 全部 ahead=0、`git log --all --not main` 为空）。
**这个结论是错的、且代价极大**：逐工作树 `git status --porcelain` 后发现，车道的产出**都在磁盘上，只是没提交**：

| 工作树 | 遗留未跟踪文件 | 性质 |
|---|---|---|
| `p2-rungate-impl` | **60**（源码 31 + 测试 28 + 报告 1） | **完整且全绿**，可直接固化 |
| `p3-m2-dbdata` | 10（全是 SQL 语句表 + 支撑类） | **一个测试都没有**，属半成品 |
| `p3-dx-big` | 6（`DIB*.cs` ×5 + 1 个测试） | 部分完成 |
| `p3-m2-sweep` | 3（`Sweep/` 下 `Nations.cs`/`M2Locker.cs`/`SweepSeams.cs`）+ 2 个测试 | 起步 |
| `p2-dxcontrols-rest` | 2（`DxMagicBall.cs` + 测试） | 完整度待验 |
| `p4-m2-objnpc` | 0（只有 `.tmp-probe*/` 探查垃圾） | 未开工 |
| `p4-rungate-mirclient` | 0 | 未开工 |

**新规程（写进关闭/接管检查单）**：
> **禁止**用 `git log` 或分支 ahead/behind 判断车道"有没有干活"。
> 接管、关闭、或怀疑车道失联时，**必须逐工作树** `git -C <wt> status --porcelain`。
> 未提交 ≠ 没做；未提交的产出只差一次门禁 + 一次提交。

### 13.3 抢救结果：`p2-rungate-impl` 60 文件并入 main

按 60 个未跟踪文件**原样**验证（调度方一行未改车道产出）：
- `dotnet build`（工程级）→ **0 error / 0 warning**
- `dotnet test tests\GXX.RunGate.Tests` → **1206 passed / 0 failed**（该工程此前只有 6 个测试文件）
- 车道提交 `e06d90c4` → 并入集成分支 `94cd9570` → 吸收 main → 全量门禁 → `767ab38d` **ff-only 提升**，
  `main^{tree}` 与集成分支树**逐字节相同**。
- 全量门禁（11 个测试工程）：**11,946 例通过 / 0 失败**（此前基线 10,659），其中 RunGate 1,206、
  M2Server 5,591（顺序会话同期也在长）。
- 该批覆盖：`RunGateUtils*.cs`(9) / `uBuffer*.cs`(5) / `uFrm*.cs`(12) / `Iocp*Compat|Policy`(2) + 28 个测试文件。

**该车道报告（`docs/并行报告-p2-rungate-impl.md`，674 行）另有三项高价值产出**，后续 RunGate 车道必须读：
1. **条件编译开关分析**：钉死哪一半是活代码（`UseIocpClient=1`、`MultiThreadRunContext=1`、
   `NEED_REGISTER=1`、`REGISTER_TEST=0`、`CLIENT_ANTIPLUG=1`、`RungateLEG_IOCP=6`），
   并据此判定 `RunGateUtils.pas:1538-1786`、`:4233-4270`、`:4331-4476`、`:1983-2069`、`:3816-3899` 等为**死代码**。
   没有这一步就会把死分支当活逻辑移植（或反之）。
2. **接缝清单**（§6.2）：`GateSession`/`RunGateFrameScanner`/`RunGateDataCache`/`RunGateFullServiceMsgQueue`/
   `RunGateTiming`/`RunGateChallengeState`/`IocpUtilsPolicy`/`IocpWinsock2Compat` —— 后续车道**复用，不得另造**。
3. 它**独立地**把 `MirClientContext.pas` 判为"未覆盖，建议单独一条车道"——与调度方的战略判断一致。

### 13.4 本轮修掉的三个审计工具缺陷（都是"少报/假报缺口"方向）

1. **备份文件被当成缺口**：`Source\Client-HGE\GlobalString-加密前备份.pas`（basename 含非 ASCII，
   是"加密前备份"）一直被算作一个 UNMAPPED 单元。**Delphi 单元名是 ASCII 标识符，非 ASCII basename 的
   `.pas` 不可能是可编译单元引用** → 新增 `NONUNIT` 判定与独立报表段落，不计入缺口。
   （脚本必须 ASCII-only，因此用转义 `[^\x00-\x7F]` 表达。）
2. **单条命中时计数渲染为空 —— 会把真实缺口显示成空白**：`$ErrorActionPreference='Stop'` 下
   `(单对象).Count` 不返回值，于是"恰好只有 1 个未映射单元"的模块那一格渲染为**空白**而非 `1`，
   肉眼极难发现（例：`DBServer` 的未映射格为空，实际有 1 个）。全部改为 `@(...).Count`。
   **修正后单模块口径变化**：Client-HGE 未映射 7→6（另 1 个转 NONUNIT）、M2Engine 的 WEAK 由空→7 等。
3. **行数系统性少报 4%~19%**（**由车道 `p2-rungate-impl` 的报告抓出**）：原实现
   `Get-Content | Measure-Object -Line` **跳过空行**，于是每个有大量空行的单元都被低估：
   `MirClientContext.pas` 9,758→**11,125**、`ObjNpc.pas` 10,281→**10,546**、`RunGateUtils.pas` 4,393→**4,925**、
   `IocpWinsock2.pas` 3,101→**3,450**、`uFrmSafeFilter.pas` 1,307→**1,497**。改为**物理 LF 计数**
   （`latin1` 字节透明读入后按 `` `n `` 切分），顺带让计数**不受 GBK 编码与 CRLF/裸 LF 混用影响**。
   > 这条直接影响派发决策：派发前的"规模"判断曾被系统性低估最多五分之一。

### 13.5 `unit-map.tsv` 改为"在飞归属"的单一事实源

原文件仍停留在 P1 波次（那批车道早已并入 main），导致审计的 `assigned` 列恒为 0 ——
**调度方看不到"谁在做什么"，正是重复派发的温床**。已整体重写为当前在飞车道的 64 行归属
（P2/P3/P4 共 7 条车道），审计随之正确报出 `assigned=55`。
`ParadoxDataSet` **有意不登记**（`RunGate` 与 `GameCenter` 各有一份 1,240 行的同名源，
basename 键会同时命中两份，必须由集成方一次性裁定去重方案，不能让某条车道误以为独占了全部副本）。

### 13.6 清掉一个"能写主干"的残骸，并补上 scratch 忽略漏洞

- **残骸**：`.worktrees/lane-resources` 是一个**没有 `.git` 的目录**（§11.7 那次 `worktree remove --force`
  半失败的遗留）。在它里面执行任何 `git` 命令都会**落到主仓库**——这正是最危险的一类残留。
  已确认其分支 `par/lane-resources` 早已并入 main（ahead=0）、内容仅为已合并的 `Wis.cs`/`Wzl.cs`/
  `GameImagesBase.cs` 副本 → **删除**。
- **忽略漏洞**：失联车道在 `p4-m2-objnpc` 里造出 `.tmp-probe/` 与 `GXX.CSharp/.tmp-probe2/`，
  两者**都不匹配** `.git/info/exclude` 里已有的 `_tmp_src/`、`_probe*/`、`.tmp-src/`、`.scratch*/`
  → 一次 `git add -A` 就会把探查垃圾提交进车道。已补 `.tmp-probe*/`、`.tmp-probe*`、`.tmp_probe*/`，
  并清理该工作树的残留。

### 13.7 战略发现：无人认领的四个"巨型单元"

审计的 `CHECKLIST_ONLY`（36 个）里混着**真正未移植、且没有任何车道/会话认领**的四个大单元：

| 单元 | 实测行数 | 现状 |
|---|---|---|
| `RunGate\MirClientContext.pas` | **11,125** | 全仓只有一处 `TClientContext` 插件 ABI 接口，**`TMirClientContext` 完全没实现**；是 RunGate 的"业务大脑" |
| `M2Engine\ObjNpc.pas` | **10,546** | 全仓只有 `PluginInterfaceSeams.cs:257` 一处 `_TNormNpc` 接缝注释 |
| `Client-HGE\MirReturnConfigDlg.pas` | 5,339 | 无任何 .cs 证据 |
| `M2Engine\uFrmCustomMagic.pas` | 4,285 | 只有周边的 `CustomMagicUtils.cs`/`CustomMagicCopySettingForm.cs`，主窗体未做 |

四者合计约 **31,300 行**，比当时"UNMAPPED 清单"里所有单元加起来还大一个量级 ——
**只看 UNMAPPED 会严重误判剩余工作量**。已为前两个建立 P4 波次工作树与分区
（`p4-rungate-mirclient` → `src/GXX.RunGate/MirClientContext*.cs`；`p4-m2-objnpc` → `src/GXX.M2Server/Npc/**`）。

### 13.8 与顺序会话 A 的分工现实：UNMAPPED 已归零，但"归零"不等于做完

推进期间顺序会话 A 一直在主干高速工作（批次 J193→J211，18 个提交约 2,400 行，全部集中在服务端怪物类），
并且**它自己把调度方列在 UNMAPPED 里的 `MySqlStorageDB.pas`/`SqliteStorageDB.pas` 做掉了**（J195/J196）。
结果：审计的 `unmapped` 由 58 降为 **0**。

**结论（对分工策略有直接影响）**：
1. 会话 A 的活动范围**不止 M2Server**（近 100 个提交里 `src/GXX.Client` 62 次、`src/GXX.M2Server` 39 次、
   `src/GXX.Core`/`src/GXX.DBServer` 各 11 次）→ **不存在"按目录互不重叠"的干净分区**。
2. 因此"避免重复劳动"不能靠目录隔离，只能靠：**调度方优先派发会话 A 近端未触及的子系统**
   （RunGate 全族、Client 窗体族、SelGate/DBServer/GameCenter/Common 尾部），并接受偶发重叠。
3. `unmapped=0` 只说明"审计用 basename/文件头证据抓不到的单元已为 0"，**剩余量在 `assigned`(55)
   与 `checklist-only`(36) 里**；真实完成度必须以"派发清单 + 逐单元覆盖行号"为准，不能看这一个数字。

### 13.9 活主干下的提升竞态（本轮实际发生）

集成分支基于 `252afb01` 门禁全绿后，`main` 已被会话 A 推到 `8164e62c`，`--ff-only` 失败。
**处置**：`git -C .worktrees/integration merge --no-ff main`（吸收）→ **重跑全量门禁** → 再 ff-only。
本轮共走两轮（第一轮 11,927 例、第二轮 11,946 例，均 0 失败）。
**规程**：只要 `main` 在门禁后移动过，**必须重新吸收并重新门禁**，不得复用旧结论；
提升窗口尽量压缩（门禁一绿立即 ff-only）。

### 13.10 本轮新增的车道交接纪律（写进后续所有派发提示词）

1. **小切片提交、立刻提交**：每完成一个小方法族（100~200 行）就提交一次。
   本轮 8 条车道之所以损失惨重，根因就是"攒到最后一起提交" —— 被杀死时**已完成的正确工作全部未落库**。
   **先提交骨架，再迭代。**
2. **接管遗留产出时先甄别，不要推倒重来**：重新派发同一车道时，必须告知其工作树里已有上一轮未提交产出
   （附文件清单与完成度线索），要求"先判断完成度，能续作就续作"。
3. **提升前必须确认 `main` 未移动**，否则重吸收 + 重门禁。
4. **禁止用 `git log` 判断车道是否干活**（§13.2）。

## 14. 第 5/6 次跨车道类型重名：两起裁定与根因归属

> 背景：本工程"两条车道各自定义同名 public 类型"已累计 6 次。§12.8 确立的总原则是
> **以原文（.pas）的正式归属为准；正式归属落地后，立即授权原持有方去重复制**。
> 本节两起都按该原则裁定，但**去重的执行者不同**，理由是"一个文件只能有一个写者"。

### 14.1 `TDxScrollControl` / `TDxScrollBox` —— 撤销接缝（已执行）

**车道报告的碰撞面比实际大**。实测全路径后只有一处是真冲突：

| 位置 | 命名空间 | 是否冲突 |
|---|---|---|
| `src/GXX.Client/DxComponent/DxControls.cs:706` `TDxScrollControl` | `GXX.Client.DxComponent` | **是**（唯一真冲突） |
| `src/GXX.Client/GUI/DxComponent/DxControls.cs:216` `TDxScrollBox` | 另一命名空间 | 否 |
| `src/GXX.Client/LoadDx/DxControlSeams.cs:200/221` | `GXX.Client.LoadDx` | 否 |

**裁定**：`DxMemo.pas`（`TDxScrollControl` 在 :44、`TDxScrollBox` 在 :194，全族 8 类、5,877 行）
是正式归属 → 整族划给车道 `p3-dx-big`，产出 `DxComponent/DxMemo*.cs`。
`DxComponent/DxControls.cs` 里那份"最小接缝"（原 `697-713`）**由集成方删除**（保留说明注释），
因为该文件同时被另一条在飞车道（`p2-dxcontrols-rest`，授权 `!DxComponent/**`）持有 ——
**若把删除权也交给 `p3-dx-big`，就会出现两个写者**。为让 `verify-lanes.ps1` 接受这次改动，
给 `p3-dx-big` 补了**极窄**的 `!.../DxComponent/DxControls.cs` 授权（仅此一次改动，不得他用）。

**兼容契约（必须满足，否则同文件与 LoadDx 侧立刻编译不过）**：
- `GXX.Client.DxComponent.TDxScrollControl` 仍须 `: TDxControl`，且提供
  `MouseWheelDown(TDxShiftState, TDxPoint)` / `MouseWheelUp(...)` 两个虚方法。
  依赖方：`DxControls.cs` 内 4 处 `is TDxScrollControl` + `LoadDx/DxControlSeams.cs:200` 的派生。
- 在 `GXX.Client.DxComponent` 内定义 `TDxScrollBox` 是**安全**的（该命名空间此前并无此名）；
  但 `SerialWindowsDlg.cs:48/251` 已在用它，需注意与 `LoadDx` / `GUI.DxComponent` 两个同名类型的
  **CS0104 二义性**（若某文件同时 using 两个命名空间）。

**教训（已发给车道并写进规程）**：撤销接缝会使**该车道工作树立即编译不过**（`is TDxScrollControl` 报 CS0246），
所以必须先落下正式类型、验证并提交，**不能**先去干别的分片 —— 否则车道在补齐前永远拿不到绿色门禁。
本例中集成方先删接缝、又告诉车道"先做 DIB.Fusion"，制造了一个自我阻塞，已发更正。

### 14.2 `TMagicBallType` / `TMagicBallValueAlignment` —— 裁定去重，**根因修复未完成（待办）**

**现象**：`DxMagicBall.pas` 依赖 `DxComponents.pas:46-47` 的这两个枚举，但它们在解决方案内**已有唯一定义**：
`src/GXX.Client/LoadDx/GuiRecords.g.cs:92-106`（`:byte`，成员名/顺序与原文逐字一致）。
若在 `DxComponent` 按 1:1 重新声明，以下 6 处**立即 CS0104**（这些测试同时 using 两个命名空间）：
`LoadDxRecordLayoutTests.cs:362/368`、`LoadDxControlLoaderTests.cs:612/615/647/655`。

**裁定**：正式归属 = **`GXX.Client.DxComponent`**（依原文 `DxComponents.pas`）。但**根因修复由集成方执行**，
因为涉及三处都在车道分区之外：① 生成器 `Tools/GenGuiRecords.ps1`；② 生成物 `GuiRecords.g.cs`；
③ `LoadDx` 侧测试的 6 处引用。
**可行性已确认**：`LoadDx/DxControlSeams.cs:200` 的 `TDxScrollControlSeam : TDxScrollControl` 说明
`LoadDx` 本来就依赖 `DxComponent`，因此给 `GuiRecords.g.cs` 补 `using GXX.Client.DxComponent;` 不引入反向依赖。

**临时处置（已批准车道照用）**：车道在 `DxMagicBall.cs` 与其测试里用
`using TMagicBallType = GXX.Client.LoadDx.TMagicBallType;` 引既有定义 —— 不重复声明、不越区改文件。
`DxGroupAttackProgress.pas` 同样需要 `TMagicBallValueAlignment`，沿用同一别名。
**集成方完成根因修复并通知后，车道再删除本地别名**（通知前不得删，否则其工作树 CS0104）。

**⚠ 待办（勿丢）**：生成器 + 生成物 + 6 处测试引用的这一版去重**尚未执行**。
在它完成前，`DxComponent` 里任何新车道只要用到这两个枚举，都必须走别名 —— 这是**已知的重复摩擦点**，
后续 DxComponent 车道（`DxImageButtonEx`/`DxSwitchButton`/`GuiManage`/…）会反复撞上。

### 14.3 顺带确认的两处"报告与现实不符"

1. 车道 `p3-dx-big` 报告"`TDxScrollBox` 也存在于只读的 `DxComponent/DxControls.cs:706` 附近" —— **不实**，
   那是 `GUI/DxComponent/DxControls.cs` 与 `LoadDx/DxControlSeams.cs`（不同命名空间）。
   **规程**：跨车道重名的报告必须带**完整路径 + 命名空间**，只报 basename 会让集成方误判（本轮即被误导一次）。
2. `DxMemo.pas` 的真实内容与任务书描述不符：它不是"多行文本编辑器"，而是**滚动/列表/树控件家族 8 个类**
   （`TDxScrollControl`/`TDxScrollBox`/`TDxLines`/`TStringLineEx`/`TDxChatMemo`/`TDxTreeNode`/`TDxTreeView`/`TDxListView`）。
   **规程**：派发提示词里的单元描述来自审计的粗判，车道有义务核实并回报（本车道做对了）。

### 14.4 ⚠ 同批待办：生成物里"按原文属于 DxComponent"的枚举（**3 个，根因去重未执行**）

| 枚举 | 原文正式归属 | 当前唯一定义处（生成物） |
|---|---|---|
| `TMagicBallType` | `DxComponents.pas:46` | `src/GXX.Client/LoadDx/GuiRecords.g.cs:92-106` |
| `TMagicBallValueAlignment` | `DxComponents.pas:47` | 同上 |
| `TScrollStyle` | DxComponent 族（原文） | `src/GXX.Client/LoadDx/GuiRecords.g.cs:133` |

**为什么迟迟不做（不是遗漏，是有意推迟）**：这是一次**跨文件的类型搬迁**，必须"在 `DxComponent` 落定义"与
"从生成物 `GuiRecords.g.cs` + 生成器 `Tools/GenGuiRecords.ps1` 移除"**在同一提交内原子完成**，
否则两份定义并存期间，任何同时 `using` 两个命名空间的文件都会 **CS0104**（已实测 6 处）。
当前有 8 条车道在飞、其中 `p2-dxcontrols-rest` 与 `p3-dx-big` 都在 `DxComponent` 树内，
搬迁会让它们的工作树在各自分支上编译不过 —— **飞行中做原子搬迁收益为负**。
**执行时机**：DxComponent 车道家族收敛（不再有车道改这两个目录）后，由集成方一次性做完并门禁。
**在那之前**：`DxComponent` 侧新车道一律以引用/别名的形式消费这三个枚举（已有两处照此办理），**不得再造第三份定义**。

### 14.5 ⚠ 已知形式偏差：`TDxControl.MouseWheelDown/Up` 的 `override` 形式（**有意暂缓**）

- 原文：`DxControls.pas:4036-4044` 在 `TDxControl` 上声明同名**空虚方法**，`DxMemo.pas` 的 `TDxScrollControl` 以 `override` 覆写。
- 托管侧：`TDxControl`（`DxComponentCommon.cs:788`）里没有这两个成员；既有模式是落到
  `DxControlOps.MouseWheelDown/Up(TDxControl self, …)` 静态替身（`DxControls.cs:2299/2302`），
  该模式的文件注释明确写着"属于 `TDxControl` 但无法写进 `DxComponentCommon.cs`（只读独占）的成员"。
- 现状：`DxMemo.cs` 的 `TDxScrollControl.MouseWheelDown/Up` 用 `public virtual` + 转调 `DxControlOps`，
  **行为与原文等价**（车道已验证调用点 `PortMouseWheelDown` 一致），仅"原文是 override"这一**形式**保真度受损。
- **裁定：现在不改**。飞行中修改核心共享类 `TDxControl`（其继承面被 7 条在飞车道与顺序会话共同依赖）
  的风险大于两个关键字的收益。**待 DxComponent 家族收敛后**，由集成方在 `TDxControl` 补两行虚方法并把各子类改回 `override`。
- **注意**：撤销 `DxControls.cs` 的接缝会**顺带**让这两个虚方法失去落点（见 §14.1），
  下次做同类"撤销接缝"操作时，要**先确认接缝是否同时承载了原文基类成员**。

### 14.6 主干卫生：删除误提交在仓库根目录的空文件

`4942a665`（批次 J212）**误把空文件 `SqliteM2DataDB.cs` 提交在仓库根目录**（0 字节，不在 `GXX.CSharp/` 下、
不参与任何 csproj 编译）。已在集成分支 `47dd3eda` 删除后再提升。
**新增提升前检查**：`git ls-files -- '*.cs' | Where-Object { $_ -notmatch '/' }` 必须为空 ——
根目录出现 `.cs` 一律是误提交（本轮即靠这条抓出）。

## 15. 第 7 次跨车道碰撞：**新形态** —— 接缝与真实实现的签名不一致

### 15.1 事故

集成本波（4 条车道）后全量门禁失败，其中一处是：
```
src/GXX.RunGate/MirClientContextSeams.cs(706,90): error CS1503:
  Argument 1: cannot convert from 'int' to 'ushort'
```
**它不是"重复定义"，而是本工程此前没遇到过的新形态**：

| 车道 | 做了什么 |
|---|---|
| `p2-rungate-impl` | 把 `MagicIntervalUtils.pas` **全量移植**到新文件 `GateShareMagicIntervalUtils.cs`（`TMagicIntervalList:62`、`Find(ushort) :139`，对应原文 `Find(MagicID: Word)`），**并把这两个类型从共享接缝层 `uFrmGameSpeedLogic.cs` 移走** |
| `p4-rungate-mirclient` | 在 `MagicIntervalUtils` **尚未移植时**造了接缝 `MagicIntervalUtilsSeam.Find(TMagicIntervalList, int)`；其 `int` 与真实实现的 `ushort` 不匹配 |

两条分支各自都自洽（各自都能编译），**合到一起才炸**。这正是"分片并行 + 集中门禁"必须存在的理由。

**裁定**：按 §12.8"正式归属落地后立即去掉接缝" —— 接缝方删除整个 `MagicIntervalUtilsSeam`，
直接调用真实实现，原文有窄化转换处照 `p2-rungate-impl` 的 `unchecked((ushort)…)` 写法。

**新规程（写进后续所有派发提示词）**：
> **移植某单元之前，先 `grep` 全仓是否已有该单元的"接缝"**（`*Seam*`、`*Seam.cs`、以及与单元同名的 `Seam` 类）。
> 若已有接缝：**不要默默替换**，要在报告里列出"哪些接缝已被真实现取代、需要其归属方删除"。
> 同理，**造接缝的车道**在报告里必须写明"本接缝对应哪个未移植单元"，以便该单元真正落地时能被找到并回收。
> 本轮之所以能快速定位，正是因为 `p2-rungate-impl` 的报告 §6.2 明确列了它自己的接缝清单。

### 15.2 事故二：**"构建绿灯" ≠ "门禁绿灯"**（一次真实的流程误解）

车道 `p3-dx-big` 报告"门禁已恢复绿色"，实际含义是 `dotnet build` 通过；
但其 `GXX.Client.Tests` 是**红的**（`DxCtlDibCoreTests` 34 例遗留测试期望错误尚未修）。
我据此把它并入波次 → 整个波次被它一个人挡住。

**规程**：
1. **车道门禁 = 构建 0 error **且** 该工程测试全绿**。只报"构建通过"不算门禁通过，必须在报告里给**测试汇总数字**。
2. 若确需留存档提交（防宿主杀死丢工作），提交信息必须写 **`WIP-不可合并`**，
   并保证**最后一次提交是全绿的** —— 否则集成方无法区分"可合并"与"存档"。
3. 集成方在并入前，**必须回读车道报告里是否明说"测试未修/未验证"**；
   本轮 `p3-m2-dbdata` 的 `f6c7eb74` 自称"未验证"、`p3-dx-big` 自称"基线 3484 尚无法验证" ——
   **这两条自述本应直接阻止我并入**，是集成方的判断失误（代价：一次波次中止 + 两轮门禁时间）。

### 15.3 波次中止（abort）规程：失败的门禁对 main 零代价

本轮实际执行了一次完整中止，流程值得固化：

```
1. 合并 N 条车道到 par/integration
2. 跑全量门禁 → 失败
3. git -C .worktrees/integration reset --hard main     # 丢弃整波，main 从未被触碰
4. 只重新合并"自述已验证通过"的车道 → 再跑门禁
5. 绿 → ff-only 提升并校验树同一性
```
**价值实证**：本轮门禁一次性抓出**两个互相独立**的缺陷（`MirClientContextSeams.cs` 编译错误 +
`DxCtlDibCoreTests` 34 例失败），两者都会被直接带上 main。
**代价实证**：`main` 完全未被污染，中止只花了重置与重跑的时间。

### 15.4 提升竞态已成常态（呼应 §13.9）

顺序会话 A 现在**每隔几分钟**就往 main 提交一批（J193→J213 一路推进）。
本轮提升因此经历了"门禁绿 → main 已移动 → 重新吸收 → 再门禁"的完整循环。
**操作规程固化**：门禁一绿**立即**检查 `main` 是否仍等于吸收时的 SHA；不等则重吸收重门禁；
**永远不要复用移动前的门禁结论**。单次提升的预期成本已从 1 轮门禁升为 1~2 轮。

## 16. 第 20 轮集成与裁定

### 16.1 提升结果

| 提升 | 内容 | main | 全量测试 |
|---|---|---|---|
| 第 1 次 | RunGate `MagicIntervalUtils`（198 行 + 43 例）+ `ObjNpc` 骨架 | `74ca257f` | 12,148 / 0 |
| 第 2 次 | `p2-dxcontrols-rest` 3 切片（`AsphyreTimer`+`StreamClipbrd`+`DxControlClpbrd`，+216 例）+ `p2-rungate-impl` 续片 + `p4-m2-objnpc` 切片 | `7b851725` | **12,471 / 0** |

本轮起点 10,659 → **12,471 例**。本轮还执行了一次**波次中止**（见 §15.3），main 未被污染。

### 16.2 新增到"DxComponent ↔ LoadDx 去重批次"的两个**类型**（§14.4 的**枚举**之外）

`DxGroupAttackProgress.pas:123` 的 `TDxGroupAttackProgress` 与 `DxSwitchButton.pas:77` 的 `TDxSwitchButton`
与 `LoadDx/DxControlSeams.cs:447/473` 的接缝同名；且守卫测试
`LoadDxNamespaceCollisionTests.LoadDx_Own_Seams_Are_Still_Declared_Locally`
**反向要求**这两个名字留在 LoadDx → 直接落地必触发 CS0104 且守卫变红。

**裁定**：目标形态 = **原文名落 `DxComponent` + 删除两处 LoadDx 接缝 + 同步改守卫测试**（即车道提的方案 A）。
**明确否决方案 B**（用 `TDXGroupAttackProgress`/`TDXSwitchButton` 之类变通拼写）：
本工程已因"车道自行改名"吃过一次下游契约不一致的亏（`TDxImageButton` 那次，见 §12.11），
再引入第三套命名约定，将来还要改回来，纯属负债。
两类型并入 §14.4 同批，**待 `DxComponent` 目录不再有在飞车道时，由集成方一次性原子执行**。

### 16.3 登记：`DxControlClpbrd.pas` = **死代码，不移植**

可复核证据（车道 `p2-dxcontrols-rest` 提供）：接口段 `6-11` 与 `StreamClipbrd` 逐字相同；
实现段 `15-167 ≡ 12-164`（**153 行 0 差异**）；不在 `GuiEdit.dpr`；全树无任何 `uses`；
而 `StreamClipbrd` 被 `Main.pas:591/606/629/637/2085/2101`、`Structure.pas:243/253/270` 实际调用。
**处置**：保留 `DxControlClpbrd.cs` 的**转发实现**（保真且可追溯，便于比对），但**不再计为覆盖缺口**。

### 16.4 待裁定 → **已裁定**（**均不登记为"不移植"**，避免静默缩小"全量"目标）

集成方实测复核（在 `main` 上 grep `src/**/*.cs`）：

| 单元 | 行数 | 车道建议 | **裁定** |
|---|---|---|---|
| `GuiManage.pas` | 417 | 不移植 | **不是"不移植"，而是"被上游阻塞"** —— 顺延，不作为缺口登记 |
| `LoginDlg.pas` | 162 | 不移植 | **应移植**（转 WinForms + 接缝），顺延派发 |

**`GuiManage.pas` 复核证据**：
- 该单元在**整个 C# 侧无任何提及**（文件头 40 行内无、正文也无）→ 确实未移植，且**不存在"换名已移植"的别名**。
- 其声明的依赖逐个核对：`DxBackground` **连 `.pas` 文件都不存在**（该类型应定义在别处，待定位）；
  `DxPageControl`/`DxEdit`/`DxImageGrid`/`DxPopupMenu`/`DxComboBox` **均无同名 `.cs`**（E1 假），
  只在其它 `.cs` 的**文件头**被提及，因而被审计记为 MAPPED（**这正是 §11.2 警告过的 E2 假阳性类别**，
  值得单独复核：这几个单元很可能被高估了覆盖度）。
- 其核心方法 `NewDxControl`（:404，按 `GuiHeader` 分派构造控件）、`LoadComponent`(:83)、`LoadSubComponent`(:37)、
  `LoadFromStream`(:443) **本质就是构造上述那些控件** —— 在那些控件落地前无法保真实现。
- **裁定**：`GuiManage` 归为**下游阻塞项**（等 5+1 个控件单元落地后再做），**不写入"不移植"登记**；
  同时把它与那 5 个控件的**覆盖度假阳性**一起列为待复核项。
  （车道提的"与 `LoadDx/GuiComponentLoader.cs` 功能重复"**未被采信**：`GuiComponentLoader.cs` 不提及
  `GuiManage.pas`，两者关系需在控件落地后按原文逐方法比对，不能凭印象判重。）

**`LoginDlg.pas` 复核证据**：本工程对窗体的既定做法是**转 WinForms + 接缝**（`VirtualTrees`、`VirtualStringTree`
即按此处理，见 §6.1 与 `uFrmGameSpeedLogic.cs`），"用了第三方 VCL 控件"**不等于**整窗体不移植。
**裁定**：应移植，顺延派发。

**规程（重申并强化）**：车道**可以建议**"不移植"，但**结论必须由集成方下**；
结论下达前不得写入 `$VENDOR_UNITS` 一类登记。**"依赖未移植" ≠ "不该移植"，而是"顺延"** ——
把前者登记成后者会让覆盖率报表把真实缺口伪装成已完成，静默降低"全量"目标。

## 17. 第 8 次跨车道碰撞（**第三种形态**）与"Delphi 格式串"缺陷类

### 17.1 第 8 次碰撞：车道**看不到**另一条车道在它开工后落到 main 的类型

**现象**：合并 `p4-rungate-mirclient` 时门禁报 **CS0101/CS0111** ——
`MirClientContextSeams.cs` 里 4 个类型与 main 上已存在的定义重复：

| 类型 | main（先落地、已提升） | 车道分支 |
|---|---|---|
| `TAddressInfo` | `GXX.RunGate/GateShareContainers.cs:71` | `MirClientContextSeams.cs:297` |
| `TAddressListEx` | `GateShareContainers.cs:227` | `MirClientContextSeams.cs:310` |
| `TSafeStringList` | `GateShareContainers.cs:316` | `MirClientContextSeams.cs:123` |
| `TSafeMemoryStream` | `GateShareContainers.cs:407` | `MirClientContextSeams.cs:211` |

**与前两次的形态差异（重要）**：

| 次 | 形态 | 何时炸 |
|---|---|---|
| 第 5/6 次 | 同一命名空间的**重复类型定义** | 合并时 CS0101 |
| 第 7 次 | 接缝签名与真实实现**不一致**（`Find(int)` vs `Find(ushort)`） | 合并时 CS1503 |
| **第 8 次** | **车道基线早于另一车道的后续提交** → 它按"main 当时没有"造了替身，而该类型后来出现在 main | 合并时 CS0101 |

**根因**：车道的基线是**某个历史 main**（本例 `76eb613d`），而 `p2-rungate-impl` 在其后提交并提升了
`GateShareContainers.cs`。**两条分支各自都能编译** —— 车道无从感知"未来"会出现在 main 上的类型。

**裁定**：以**先落地并已提升者**为准（`GateShareContainers.cs`），车道删除自己的 4 个定义并适配调用点；
`GateShareContainers.cs` 属 `p2-rungate-impl` 分区，车道不得改（缺成员/签名不符则上报由集成方处理）。

**新规程（写进所有派发提示词）**：
> **凡新增一个类型（尤其是接缝/替身类型），提交前必须先查 main 是否已有**：
> `git grep -n "class <TypeName>" main -- '*.cs'`
> 车道**不 rebase**，但可以随时用 `git show main:<path>` 读 main 的当前版本 —— 这是"看到未来"的唯一途径。
> 本工程 8 次碰撞中，**6 次**都能被这一条命令提前发现。

**接缝层是重灾区**：一次性声明大量替身类型的文件（本例 `MirClientContextSeams.cs`）最容易撞车。
派发时若某车道要造接缝层，应在提示词里要求它**先枚举 main 上已有的同名类型**。

### 17.2 "Delphi 格式串"缺陷类：**编译期与普通断言都抓不到**的一类真缺陷

**发现来源链**：车道 `p3-dx-big` 纠正了集成方对 DIB 那 34 例失败的定性 ——
它们**不全是"测试期望写错"**，其中混着**真实现缺陷**：`DXConsts` 的常量是 **Delphi 格式串**
（`'...(%d)'`，`DXConsts.pas:46/107`），而托管侧用了 `.NET` 的 `string.Format`，
后者**不认 `%d`**，会把 `"%d"` 原样输出到消息里（车道改了 4 处为 `GXX.Core.Rtl.DelphiFormat.Format`）。

**集成方据此做了一次全仓排查**（`src/**/*.cs`）：
- 同行内 `string.Format(` 且含 `%[dsfuxX]` 的可疑点：**仅 1 处**；
- `DelphiFormat.Format`（正确助手）调用点 **13** 处，`string.Format(` 共 **221** 处（其余多为 .NET `{0}` 风格，正常）；
- 含 Delphi 风格 `%` 格式字面量的文件 **19** 个。
→ **该缺陷类目前是收敛的**，不是普遍性问题。

**但那 1 处可疑点确认是真缺陷**（已修）：

`ParadoxConv.cs` 的 `DelphiFormatString` 原实现：
```csharp
=> string.Format(fmt.Replace("%S", "{0}"), args);   // 错
```
而格式串是 `"Can't convert from %S to %S symbol %S"`（原文 `ParadoxConv.pas:71-80`）—— **三个**占位符。
全部 `Replace` 成 `{0}` 后，**三个位置都取第一个实参**：消息变成 `Can't convert from A to A symbol A`，
`dest`/`sym` 被**静默丢弃**。已改走 `GXX.Core.Rtl.DelphiFormat.Format`（按序消费实参、`%S` 与 `%s` 等价），
并补 **5 例回归测试**（`tests/GXX.RunGate.Tests/ParadoxConvFormatTests.cs`）锁定按序替换语义。

**为什么这类缺陷危险**：它**不会编译报错**，也**不会被普通断言发现**（要有断言比对完整输出文本才会暴露），
且已被合并进 main 很久（P1 波次的车道产出）。**这是"看起来一样、语义不同"的第 1 类高发缺陷。**

**新规程**：
1. 派发提示词中明确要求：**凡原文用 Delphi `Format`/`FormatFloat`/`IntToStr` 等格式化的地方，
   托管侧一律走 `GXX.Core.Rtl.DelphiFormat`，禁用 `string.Format`**（除非格式串确为 .NET `{0}` 风格）。
2. 车道报告里必须列出它发现的"**`.NET` 用法 ≠ Delphi 语义**"清单（不限格式化），供集成方扫其它车道。
3. 集成方定期（每 2~3 轮）跑一次本节的排查命令，确认该缺陷类未扩散。

## 18. 第 21 轮：30 单元甄别、两处 RTL 真缺陷、以及一次**我自己的流程事故**

### 18.1 提升记录

| 提升 | 内容 | main | 全量测试 |
|---|---|---|---|
| 第 1 次 | RTL 修复 `ParadoxConv`（Delphi 格式串误交 `string.Format`，三个 `%S` 全取首参） | `851f9e5f` | 12,508 / 0 |
| 第 2 次 | 4 车道波次：`p2-rungate-impl`(GateShare 全族) + `p2-dxcontrols-rest`(DxImageButtonEx) + `p4-m2-objnpc` + `p4-rungate-mirclient` | `128c1c11` | 13,115 / 0 |
| 第 3 次 | RTL 修复 `HUtil32.CompareLStr/CompareBackLStr`（见 18.3） | `6f438c5b` | 13,133 / 0 |

本会话起点 10,659 → **13,133 例**。

### 18.2 ⚠ 事故：**`reset --hard main` 吃掉了已提交但未提升的修复**（我自己的流程缺陷）

**经过**：我把 RTL 修复（补 `compn<=0` 守卫 + 逐字符 `UpCase`）提交在 `par/integration` 上并**跑绿了全量门禁**（12,658）。
正要去提升时发现 `main` 已被顺序会话推进（→ `603672af`），于是我按老习惯执行 `reset --hard main` 以"重新吸收"。
**这一步把那个已提交、已验证的修复连同它 135 行测试一起从分支上抹掉了**（`reset --hard` 把分支指针移回 main，未提升的提交变成悬挂对象）。

**为什么规程没拦住我**：§13.9/§15.3 写的是"门禁失败并中止后必须 `reset --hard main`"，那条的前提是**集成分支上没有任何值得保留的东西**；
而这次集成分支上有一个**已验证待提升**的提交 —— 前提不成立，我照搬了结论。

**抢救**：`git cat-file -t 972c4145` 确认悬挂提交仍在 → `git cherry-pick 972c4145` → 重新门禁（13,133 全绿）→ 提升为 `6f438c5b`。
**零损失，但纯属运气**（reflog 默认保留 90 天；若期间跑过 `gc --prune`，这次修复就永久丢了）。

**新规程（覆盖 §13.9/§15.3 的旧表述）**：
> 移动集成分支前**必须先看** `git log --oneline main..par/integration`。
> **非空 ⇒ 禁止 `reset --hard main`**；正确做法是 `git -C .worktrees/integration merge --no-ff main`（吸收），
> 或先把已验证的提交提升上去再动。**只有该区间为空时，`reset --hard main` 才是安全的。**
> 换言之：`reset --hard main` 是"丢弃暂存"操作，**不得用于"追赶 main"**。

### 18.3 第二个 RTL 真缺陷：`HUtil32.CompareLStr` / `CompareBackLStr`（车道 `p4-m2-objnpc` 报出）

回读原文 `HUtil32.pas:1981-1995` / `:2010-2032` 后确认**两个函数各有两处偏差**：

| 缺陷 | 原文 | 旧托管实现 | 后果 |
|---|---|---|---|
| `compn <= 0` 守卫 | 直接 `False` | **缺** | `compn==0`：`CompareOrdinal(...,0)==0` → **错判为相等**；`compn<0`：**抛** `ArgumentOutOfRangeException` |
| 比较方式 | 逐字符 `UpCase` → **大小写不敏感** | `string.CompareOrdinal` → **大小写敏感** | 一切大小写混排的比较结论相反 |

已改为逐字符 `char.ToUpperInvariant`（与 Engine 侧既有 1:1 实现 `MonGenParseCore.CompareLStr:116` **语义一致**，避免同一函数两种语义），
补 **18 例**回归测试（大小写不敏感 / `compn<=0` 不抛且 False / 长度守卫 / 末尾比较边界）。
**注**：`MonGenParseCore.cs` 属顺序会话常驻区，**统一两份实现的动作待其静默后再做**（现在两份都对，只是重复）。

### 18.4 30 个 CHECKLIST_ONLY 单元的逐单元甄别（只读车道，结论已采信）

**关键前提修正**：这 30 个单元在 `src` 里**一个 E2 命中都没有**（唯一表面命中的 `DES.pas` 是 `UnitDes.pas`/`DesUnit.pas` 的子串误配）
→ 它们**不是**"E2 假阳性"，而是**纯 E3** 条目。**这修正了 §16.6 里我的推断**（我曾怀疑 `DxPageControl` 等 5 个单元被 E2 高估，
那是**另一个**集合，仍需单独复核）。

**结果**：
- **(a) 真未移植 16 个 = 10,087 行**（这才是真实剩余量）：`ParadoxDataSet`1362 · `uFrmMainGamePets`1353 · `SelectClient`1248 ·
  `CustomActor`1130 · `uFrmDummySetting`1081 · `Login`957 · `HardInfo`898 · `uFrmCustomItemProperty`498 · `SellPlayer`307 ·
  `GateSet`302 · `GHeroDB`276 · `Objects`190 · `uFrmClientPlugManager`151 · `uFrmHumanExport`151 · `PlugIn`114 · `CreateChr`69。
- **(b) 别名已移植 0 个**（诚实结论：**没有一处**能靠"补文件头"一次闭合）。
  四处"像 (b) 但不是"（算 (b) 会掩盖真实工作）：`CustomActor` 在 `PlaySceneNewActor.cs:478` 只有**同名桩类**（仅 1 字段）；
  `SelectClient` 只有 4/7 条命令；`SellPlayer` 只有 `List<string>` + Search 桩；`EncodingHelper` 语义散落两处。
- **(c) 应从"缺口"剔除 14 个**：(c1) 死代码 3（`DES`402/`DragFromShell`95/`uFrmCustomMoney`24）；
  (c3) 被 .NET 原生取代 9（`SimpleClass`1236/`SHSocket`590/`FixedMemoryPool`486/`VersionHelper`406/`MemPool`299/`EncodingHelper`206/`MsCTF`139/`IOCPTypeDef`96/`SyncObj`41）；
  (c4) 与同类逐字节重复 2（`GameCenter\ParadoxDataSet` 与 RunGate 份 SHA256 全同；`FireDragon` 是 `ObjFireDragon` 的旧版，327/384 行逐字相同且不在任何 `.dpr`）。

### 18.5 对那 7 条"需裁定"的裁决

| # | 单元 | 裁决 |
|---|---|---|
| 1 | `Client-HGE\Login`(957) | **顺延**（登录器 EXE 是**独立交付物**，当前 C# 客户端 headless；不登记为"不移植"） |
| 2 | `DBServer\SelectClient`(1248) | **真未移植，派车道**；并记录 `Checklist.md:287` 的 ✅ 属**过度声明**（实测只落 4/7 条命令） |
| 3 | `GameCenter\GHeroDB`(276) | **登记为不移植**（BDE 别名/表/字段管理器，已被 Sqlite 架构取代）；`Checklist.md:289` 的"迁移工具窗体 ⬜"另计 |
| 4 | `DBServer\PlugIn`(114) | **顺延**（stdcall DLL 插件 ABI 是否保留属**插件体系架构决策**，归顺序会话/插件归属方） |
| 5 | `Common\EncodingHelper`(206) | **需移植**（`GetBufferEncoding` 的 BOM/UTF-16 嗅探语义影响文件解析，206 行成本低） |
| 6 | `SelGate` 的 `MemPool`+`FixedMemoryPool`+`SimpleClass`(2021) | **登记为"被 .NET 取代，不移植"** —— 它们是**真算法**，但托管侧已有等价设施（`GatewayProtocol.IocpManager`/`TcpLink`）；**要求在这些 `.cs` 文件头 40 行内注明源单元**以闭合 E2 证据（否则报表会一直显示为缺口） |
| 7 | `Client-HGE\Objects`(190) | **登记为不移植**（仅设计期工具 `GuiEdit.dpr` 引用，产品链路 `Client.dpr` 不含） |

**顺带发现 2 处文档缺陷**（`Checklist.md` 属顺序会话高频编辑文件，**未擅自改**，仅记录）：
`:273` 引用的 `GatewayKit/GateSession.cs` **不存在**（`GateSession` 实在 `GatewayProtocol.cs:84`）；`:287/:289` 的 ✅ 与实测不符。

### 18.6 待执行批次与新增待去重项

1. **`DxComponent ↔ LoadDx` 去重批次**（§14.4 的 3 个枚举 + §16.2 的 2 个类型）**仍未执行**。
   现计划**单开一条专用车道**（区域含 `LoadDx/**`、`DxComponent/**`、`tools/GenGuiRecords.ps1`、守卫测试），
   一次性完成"生成器 + 生成物 + 接缝 + 守卫测试"的原子搬迁，**并顺带落地 `DxGroupAttackProgress`/`TDxSwitchButton`**（`p2-dxcontrols-rest` 正在等这个通知）。
2. **新增两处待去重（均在顺序会话常驻区 `Engine/**`，暂缓）**：
   `Engine.TNormNpc`（`NpcScriptEngine.cs:10` 最小模型）vs `Npc.TNormNpc`（全量，`ObjNpc.pas:262` 正式归属）；
   `Engine.TCustomMagicConfig`（`MagicBatchH.cs:62` 子集）vs 完整面（`uCustomMagicUtils.pas:215` 正式归属）。
   **共同处置**：正式归属方拥有；待 `Engine/**` 静默后由集成方删除子集并改引用。
3. **(a) 类 16 个单元的派发路线**：已开 `p6-core-paradox`（`ParadoxDataSet`，**两副本逐字节相同 ⇒ 只移植一份进 `GXX.Core`**）
   与 `p6-m2-pets`（`uFrmMainGamePets`）；其余按 18.4 的规模降序排队。

### 18.7 提升记录（续）与**结构性瓶颈：`TPlayObject` 面**

| 提升 | 内容 | main | 全量测试 |
|---|---|---|---|
| 第 4 次 | `p3-dx-big`(DIB 34 例修绿 + `DxMemo.TDxScrollControl`) + `p5-m2-custommagic`(+903 例) + `p3-m2-dbdata`(+314 例) | `48ddc6a0` | **14,544 / 0** |

**结构性发现（车道 `p4-m2-objnpc` 第二轮，用脚本抽取成员逐个核对得出）**：
`ObjNpc.pas` 剩余的 **4 个优先方法全部不是被"那 6 个 Engine 成员"阻塞，而是被整片未移植的 `ObjPlayer.pas` / `TPlayObject` 面阻塞**：

| 方法 | 阻塞面（托管侧缺失成员） |
|---|---|
| `TMerchant.UserSelect`(814) | `m_nGameGold` `m_nBigStoragePage` `m_nDealGoldPose` `m_boWaitHeroDate` `m_sHeroName` `m_sDeputyHeroName` `m_sTempHeroName` `m_sAutoSendMsg` `GameGoldChanged()` `SysMsg()` + `m_sString` + `GetPoseCreate()` |
| `TMerchant.ClientBuyItem`(323) | `m_nGold` `AddItemToBag` `IsEnoughBag` `IsAddWeightAvailable` `SendAddItem`（`SendMsg` 签名不符） |
| `UpgradeWapon` 外层(72) | `m_nGold` `m_UseItems[U_WEAPON]`（**读写**）`CheckItems` `DecGold` `GoldChanged` `SendDelItem` `RecalcAbilitys` `FeatureChanged` + `GotoLable` + 4 个全局量 |
| `TNormNpc.GotoLable`(312) | **`m_nVal`（硬阻塞）** + `m_NPC` `m_Script` `m_ItemBoxNpc` `m_boOffLine` `m_boDummyObject` `m_dwLastGotoLabelTick` `m_nOneLabelGotoCount` 等 |

**结论**：`TPlayObject` 面是这四条的**共同硬前置**。车道建议排一条专门的 **`TPlayObject` 面拼接批次**（四片：变量容器 → 金额/物品容器 → NPC 会话与脚本标签 → 英雄/副将），
**可同时解锁 ObjNpc 剩余 56 条中的 20 条以上**。车道**未自行声明任何 `TPlayObject` 替身成员**（正确，符合 §14.2 的"不造第三份"原则）。
另新增 Engine 缺口：`TCreature.Initialize`（原文虚方法）、`TCreature.m_ActorIcons`（`@m_ActorIcons`）。

**⚠ 这是当前**最主要的系统性瓶颈**：多条车道（ObjNpc、MirClientContext、RunGate `uFrmMain` 的 ~90 例程）都卡在
顺序会话常驻区 `Engine/**` 的**核心对象模型缺成员**上。派发任何新车道前应先评估它是否需要 `TCreature`/`TPlayObject` 成员。
**计划处置**：单开一条 **Engine 面拼接车道**，给 `Engine/ObjBase.cs` 等**具体文件**的窄 `!` 授权，
要求"逐个成员先 `git grep` 查 main 是否已有，只补缺的、保持 1:1、最小面"，并跑整树门禁
（同类重复**成员**是 `CS0102`，任何构建都能立刻抓到，属"可检测失败"而非静默错误 —— 这是它与"重复类型"的关键区别）。

### 18.8 一条"接缝做过头"的教训（车道 `p4-m2-objnpc` 自我纠错）

切片 2 把 `TNormNpc.GetVariableText` 做成了**纯委托**；切片 7 发现原文**三个子类覆写都调 `inherited GetVariableText(...)`**，
且 `GetLineVariableText`(:6000) 对它做**虚调用** → 纯委托**破坏了虚分派链**。已改正为 `public virtual` 外壳 + 接缝，并加虚分派回归用例。
**规程**：接缝**太厚**（把原文 `virtual`/`override` 压成静态委托）与**太薄**（该接缝的却没接）都是缺陷，
但**前者更隐蔽** —— 它编译通过、单测也过，只在多态调用路径上出错。
**判定要求**：凡原文成员是 `virtual`/`override`/`abstract`，托管侧**必须保留虚分派**（`virtual`/`override`），
不得为了"先能编译"而把虚调用降级为直接委托。

## 19. 第 22 轮：第三个 RTL 真缺陷、一次流程事故、一条新的跨车道事故类型

### 19.1 提升记录

| 提升 | 内容 | main | 全量测试 |
|---|---|---|---|
| 第 1 次 | `p5-client-mirreturn` + `p4-m2-objnpc`（含复核轮） | `6eb34f18` | 14,590 / 0 |
| 第 2 次 | `p3-m2-sweep` + `p3-m2-dbdata`（含 UserShop 引号缺陷修复） | `b8fd1645` | 14,692 / 0 |
| 第 3 次 | `DelphiRTL.Trim` 系列修复 + ObjNpc 复核守卫同步 | `bc42db8f` | **14,736 / 0** |

### 19.2 第三个 `GXX.Core` RTL 真缺陷：`Trim` 系列（由车道的**复核轮**发现）

车道 `p4-m2-objnpc` 按裁定停止堆接缝、转为"已覆盖 57 条例程逐条回读原文复核"，**复核本身产出了真缺陷**：

**`DelphiRTL.Trim/TrimLeft/TrimRight` 比原文窄** —— 旧实现只去 6 个字符（`#9 #10 #11 #12 #13 #32`），
而原文判据是 `while (I <= L) and (S[I] <= ' ') do` → **所有 `#0..#32`**。
**可达后果**：`Upper(Trim("HERO\u0014"))` 原文 = `HERO` → `CMD_RACE_1`；旧托管 = `HERO\u0014` → 落 `else` = `CMD_RACE_5`
——**NPC 脚本的目标级别解析落错分支**。已按 `<= ' '` 双向扫描改正，并同步该车道那 2 条"差异锁定"守卫用例
（它们原本断言**旧的错误行为**，这正是守卫的设计意图：Core 一修就红，提醒重读原文）。

**这是本会话第 3 个"编译过、单测过、语义错"的共享 RTL 缺陷**（前两个：`HUtil32.CompareLStr` 大小写与守卫、`ParadoxConv` 的 `%S`）。
**规律**：三者**全部由车道的回读复核/交叉核对发现，无一由测试发现**。→ **"复核轮"是与"移植轮"同等重要的产出形态**，
不应被视为"没有新增覆盖"而降低优先级。

**⚠ 同批的第二个偏差（B）有意未修**：`StrToInt/StrToIntDef/StrToInt64Def` 先做 `Trim()`，
而原文走 `Val`（据称只跳**前导**空白，尾随空白应报错并返回 `Default`）。
**未修理由**：`Val` 是 Delphi RTL 函数、**本仓无其源码可回读**（不同于 `Trim` 的判据可从 `SysUtils` 语义确证），
而它影响**全仓数字解析**；在无法确证"`Val` 到底跳 `' '` 还是 `<= ' '`、尾随空白是否必然报错"之前不改。
车道的 6 条守卫用例**已把该偏差锁定并注明"Core 修好后本断言会失败"**，不会丢失。

### 19.3 一次流程事故：`WIP-不可合并` 的车道被并入波次

我按惯例把 4 条车道并入波次，**其中一条的 HEAD 明确标着 `WIP-不可合并`**（该车道遵守了规程，是我没查），
构建当场 `CS0246`，整波中止、白跑一轮门禁。

**已固化**：新增 `GXX.CSharp/tools/Check-LaneReady.ps1`（**ASCII-only**，已 `git add -f` 入库，因为 `tools/` 在 exclude 里）：
逐车道报 `ahead / dirty / HEAD 主题`，并**自动拦截 HEAD 含 `WIP` 的可合并车道**（退出码 1，可用于脚本门控）。
**规程**：**波次前必须先跑它**；已并入 §15.2 的"构建绿灯 ≠ 门禁绿灯"一族。

### 19.4 ★ 新事故类型：**车道用相对路径写文件 → 落到主工作树**（deliverable！）

车道 `p5-client-mirreturn` 报告：它的 `write` 调用最初用**相对路径**，
而 **harness 的工作目录是主工作树**（`D:\chuanqi\daima\GXX原版_Delphi7`，分支 `main`）——
于是 **7 个新文件一度落在主工作树**（也就是交付分支！）。
该车道在 **commit 之前**发现，用 `Get-FileHash` 逐一校验 7/7 一致后搬迁到自己的工作树、再删除主工作树副本，
并复核主工作树只剩开工前既有的改动。**本次零损害，但这是最危险的一类越权写入** ——
它绕过了 `verify-lanes.ps1`（那只检查车道工作树的 `status`），直接污染交付分支。

**已核实**：本轮提升后主工作树的 `git status --porcelain` 只有顺序会话自己的在飞文件（`ObjMonDamageSpellArmorCore*`），无该车道残留。

**新规程（写进后续所有派发提示词）**：
> **所有文件写入必须使用绝对路径**（`D:\chuanqi\daima\GXX原版_Delphi7\.worktrees\<lane>\...`）。
> 相对路径会被解析到**主工作树**，即交付分支 `main`。
> 集成方每轮提升后应顺带看一眼主工作树的 `git status --porcelain`，确认没有本不该在那里的新文件。

### 19.5 测试间**静态全局污染**：已派专车道治本

车道 `p3-m2-sweep` 定位了一起**真实的跨测试类污染**：`FormGeneralConfigTests.cs:488` 在 `[Fact]` 体内把
`M2Config.sEnvirDir` 设为 `"D:\Mir\Envir\"` **且不还原**，导致任何"按 `sEnvirDir` 拼路径"的测试在它之后运行时**必然错位**
（`TestConfig.cs` 已关并行 ⇒ 按类顺序执行 ⇒ 这是**确定性**失败，不是偶发）。
同类未还原赋值共 **9 处**（`CombatPowerTests:21`、`CombatPowerSettingTests:22`、`CustomHeroMagicTests:21`、
`CustomNpcUtilsTests:22`、`CustomNpcFormTests:23`、`HeroMagicFormsTests:22`、`FormJ56Tests:19`、`FormJ57Tests:22`、`FormGeneralConfigTests:488`）。
**已开车道 `p6-test-isolation`**，优先方案是**程序集级**快照/还原（xUnit 2.9.2 自定义 `TestFramework`），
这样**不必改动任何既有测试文件**、且未来任何新测试自动受保护；要求用反射枚举 `M2Config` 全部静态成员（禁止手写清单），
并**先复现污染、再证明修复后顺序无关**。

### 19.6 结构性瓶颈的解法：`partial` 让"补成员"不必碰别人的文件

`p6-m2-playersurface` 车道的分区**不含任何 `Engine/**` 既有文件** —— 因为已核实
`TCreature`/`TPlayObject` 在本工程**全是 `partial`**（`ObjBase.cs:13/155`、`CombatPower.cs:524/539`、`RecalcBonus.cs:242`、
`RecalcAbilitys.cs:144`、`ObjBase.OnlineMsg.cs:7/32`），
所以该车道可以在**自己的新文件**里 `public partial class TPlayObject { ... }` 补成员，
**完全不需要写权限去改顺序会话常驻的文件** —— 从根上消除了"两个写者"的风险（只剩"重复成员 ⇒ CS0102"这一**可检测**风险）。
**推广建议**：凡"需要给别人家的类补成员"的批次，先查该类是否 `partial`；是则一律走"自有新文件 + partial"，不要申请 `!`。

### 19.7 本轮新开车道
`p6-m2-playersurface`（`TPlayObject`/`TCreature` 成员面，解锁 ObjNpc 20+ 例程 + RunGate `uFrmMain` + MirClientContext）、
`p6-test-isolation`（19.5）。另 `p6-core-paradox`、`p6-m2-pets` 在飞。

## 20. 第 23 轮：一次提升 + **顺序会话静默回退了我的分区表**

### 20.1 提升
4 车道波次（`p3-m2-dbdata` + `p3-m2-sweep` + `p5-client-mirreturn` + `p6-m2-pets`）一次门禁全绿，
`main` → `2e872074`，全量测试 **14,736 → 15,161**（Client 3,970→4,166；M2Server 7,386→7,635）。

**本轮首次用上新工具**：`tools/Check-LaneReady.ps1` 在并波次前**当场拦下**了 HEAD 标着 `WIP-不可合并` 的 `p6-core-paradox`，
避免了上一轮那种"并进去才发现编译不过、整波中止"的浪费。**该工具已产生实际收益，务必每波先跑。**

### 20.2 ★ 新协调风险：**顺序会话会静默回退我的分区表**

**现象**：我要给新车道加分区行时发现 `lane-zones.tsv` **只有 34 行、结尾停在 `p6-m2-pets`** ——
我在上一轮加的 `p6-m2-playersurface`、`p6-test-isolation` 两行**不见了**。
并**不是**我漏加：两行都编辑成功过、也随 `1de469bf` 提交过。经核对，**更早的授权（`p3-dx-big` 的 `DxControls.cs` 窄授权、
`p3-m2-dbdata` 的 `DbLayerSqlFidelityTests.cs`、`p4-m2-objnpc` 的 `Npc*`、`p2-rungate-impl` 的 glob）都还在** ——
说明顺序会话提交的是**某个中间时刻**的副本，恰好覆盖掉我最后两次编辑。

**根因**：两个会话**共享同一个工作目录**（主工作树）。顺序会话按惯例 `git add -A` 提交时，
会把它当时看到的文件内容一起提交；若它在我的编辑落盘前就读过该文件（或用了自己缓存的副本），
提交结果就是**我的改动被静默回退**。这不是恶意，是共享工作目录的固有竞态。

**危害**：分区表是 `verify-lanes.ps1` 的唯一依据。一行被回退 ⇒ 车道会被误报"越区"⇒
集成方可能据此拒绝一条本来合法的产出，或反过来**纵容**真正的越区。

**新规程**：
1. **改完分区表后要复核落盘**（`Select-String "^<lane>\t"`），不能假设编辑一定生效。
2. **`verify-lanes.ps1` 报"越区"时，先回读分区表**确认该授权是否仍在 ——
   有可能是"授权被回退"而不是"车道越权"。本轮 3 处 `FAIL` 里就有属于前者的情况。
3. 分区表的修改**单独提交、信息写明**（便于事后对齐），不要与其它改动混在一起。
4. 长期方案（未做）：把分区表移到一个**只有调度方写**的位置，或让车道区从 `unit-map.tsv` 同源派生，
   减少"同文件双写者"。

### 20.3 本轮修掉的分区表缺口（都是我自己的规格遗漏）

| 车道 | 问题 | 处置 |
|---|---|---|
| `p3-m2-dbdata` | 测试区 `DbLayerM2Data*`/`DbLayerAuction*`/`DbLayerFingerprints*` 缺 `!`（车道需续改自建测试文件） | 加 `!` |
| `p6-m2-pets` | 源码区与测试区都缺 `!`（同上） | 加 `!` |
| `p2-rungate-impl` | 新建 `RunGateConfigLoader.cs` 不在任何 glob 内 | 加 `RunGateConfigLoader*`（源码 + 测试） |
| （仓库忽略） | `p6-core-paradox` 造出探针文件 `pxdiag-flds.DB` | 加窄前缀 `pxdiag-*`（**不用宽泛的 `*.DB`**，避免误伤真实数据文件） |

**这是"我自己的分区表规格反复滞后于车道实际产出"的第 N 次**（§13.x 已记过同类）。
**规律**：车道的**新文件命名无法被预先穷举**。缓解办法：派发提示词里要求车道
**在新增文件前先报备命名**（或让分区表给"自有新文件"一个以车道名为前缀的通用 glob）。

### 20.4 本轮新开/续跑的车道
- `p2-rungate-impl` **续跑**：按它自己的报告 §15 **S1→S8** 计划做 `uFrmMain.pas` 剩余 ~90 个例程；
  已重申"造抽象前先跑重名检查""绝对路径写入""WIP 标注""保留虚分派"四条纪律，并告知 `HUtil32.CompareLStr`/`DelphiRTL.Trim` 已修、
  `Pos("")` **尚未修**（它现有的 `PosDelphi` 补偿要继续保留）。
- `p6-client-actor` **新开**：`CustomActor.pas`(1,130 行) —— 托管侧只有 6 行桩（`PlaySceneNewActor.cs:477-482`），
  7 个覆写（`CalcActorFrame` 330 行 / `Run` 251 行 / `LoadSurface` / `GetDefaultFrame` / `DrawChr` / `RunActSound` / `RunSound`）全缺。
  已给它 `!` 授权删那 6 行桩，并特别要求"**基类缺虚成员时不许用 `new` 隐藏**，要给精确的补虚成员请求"。

## 21. 第 24 轮：报表终于说实话 + 一条可复用的窗体陷阱

### 21.1 提升
`p6-m2-pets`（`uFrmMainGamePets.pas` 1,353 行 + 同源 `.dfm` **全量移植完毕**，+234 例，51/51 过程、
48/48 DFM 事件绑定脚本逐条比对）并入 → `main` = `17befc81`，全量 **15,174 / 0**。

**一处有理由的例外（记录在案）**：本次门禁跑在 `b0fd98b9`，正要提升时 `main` 被我自己的
`audit-coverage.ps1` 那次提交推进了。按 §13.9/§20.2 的规矩本应重吸收重门禁，但我**先验证了吸收引入的增量**：
`git diff --name-only <gated> <after-absorb>` 只有 `tools/audit-coverage.ps1`、**编译产物 `.cs` 数量为 0**。
在该前提下复用门禁结论是**有依据的**，不是"省事"。
**规程细化**：`main` 移动后，若增量**不含任何被编译的文件**（`*.cs`/`*.csproj`/`props`/`targets`），可复用门禁结论并**在提交信息/台账里写明验证过程**；否则必须重门禁。

### 21.2 审计报表终于"说实话"：新增 not-ported 登记（15 个单元）

**问题**：§18.4 的甄别早已认定 15 个单元属于"死代码 / 被 .NET 取代 / 与已移植单元重复"，**该从剩余工作里剔除**，
但工具一直把它们继续算在 `checklist-only` 里 —— 也就是**每轮我读到的"剩余量"都被高估了约 6,000 行**。
（这正是 §13.7"四个巨型单元被 UNMAPPED 清单漏掉"的镜像错误：那次是**少报**，这次是**多报**。）

**处置**：把它们并入 `audit-coverage.ps1` 的 `$VENDOR_UNITS`（该桶的语义本就写成"registered as **not ported by design**"），
每条**注明理由分类**（c1 死代码 / c3 被 .NET 取代 / c4 重复），并把报表列名从 `vendor` 改为 **`not-ported`**。
**效果**：`checklist-only` **29 → 15**、`not-ported` 163 → 178。剩下的 15 个才是真正的未开工清单：
`ParadoxDataSet`×2(1362) · `CustomActor`(1130) · `SelectClient`(1248) · `uFrmDummySetting`(1081) · `HardInfo`(898) ·
`Login`(957，顺延) · `uFrmCustomItemProperty`(498) · `GateSet`(302) · `SellPlayer`(307) · `EncodingHelper`(206，需移植) ·
`uFrmClientPlugManager`(151) · `uFrmHumanExport`(151) · `PlugIn`(114，顺延)。

**注意一条实现约束**：登记表**按 basename 匹配**，所以"两个目录各一份、只有一份是非移植"的单元**不能登记**
（`GameCenter\ParadoxDataSet` 与 RunGate 份逐字节相同，但 RunGate 份正在移植 ⇒ 该 basename 必须保持可计数）。
已在脚本注释里写明，避免后人误加。

### 21.3 ★ 可复用的窗体移植陷阱：`TComboBox.ItemIndex` 越界

由 `p6-m2-pets` 车间的测试抓出（原文 `uFrmMainGamePets.pas:408/:415` 直接用 INI 值赋值）：
- **Delphi**：`ItemIndex := <越界值>` **静默置 -1**，不抛；
- **WinForms**：`SelectedIndex = <越界值>` **抛 `ArgumentOutOfRangeException`** → 必崩。

原文正是靠 Delphi 的静默容忍才没崩；托管直译就会崩。该车道加了 `SetItemIndex` 垫片复刻 Delphi 语义。
**已转告正在做 `uFrmMain`（含同类回填路径）的车道**，并要求一并排查
`TListBox/TCheckListBox/TRadioGroup.ItemIndex` 与 **`TStrings[Index]` 越界**
（后者两边都抛但**异常类型不同** —— 若原文用 `try/except` 吞异常，托管侧必须吞**对应类型**，否则行为不一致）。

### 21.4 同车道报出的一处"注释嵌在算式里"的陷阱

原文 `:941`：`4000000000 div (High(dwPetNeedExps) { div 2})` —— **`{ div 2}` 是 Pascal 注释**，
除数是 **1000** 而非直觉的 500（差一倍）。
**规程**：从原文复制算式时，注意 `{ }` 与 `(* *)` 注释**可能嵌在表达式中间**；
"逐字保留 + 注释 `// 原文如此`"时也要把这类注释一并标注，否则后人会按直觉"修正"它。

### 21.5 待办（由 `p6-m2-pets` 交给集成方）
该车道有 **3 处替身在只读区之外**（都在它自己独占区内，未越权），将来需按 §14.1/§17.1 去重：
1. `M2Config` 的 **32 个宠物字段**以 `partial` 分支落在 `Forms/GamePets/GamePetsConfig.cs`（namespace `GXX.M2Server.Engine`）
   —— 若顺序会话日后在 `M2Share.pas` 批次里落了正式定义，**必须删这份重复声明**（否则 CS0102）；
2. `TGamePetConfig`（41 字段）**必须保持 `class`**（`:629` 用 `ReferenceEquals` 做指针相等判定，改 struct/record 即破坏语义）；
3. `GamePetsState` 承载 `g_GamePetConfigList` + 4 个函数，其中 **`GetGamePetConfig` 被 `ObjBase.pas`/`Magic.pas`/`M2Share.pas` 使用**，
   迁移时须保留"大小写无关、首个命中、未命中返回 null"语义。

另：该车道报出一处原文真缺陷 `:363`（DC 勾选框回填读的是 **MC** 字段 → `boPetDCToMaster` 从未被回填、两框初始态恒相等），
已逐字保留 + 4 条测试锁定；但其**完整后果链无法稳定断言**（两次运行结果自相矛盾），已如实登记为**未覆盖**并建议在有显示环境时补测 —— **不假装覆盖，处置正确**。

## 22. 第 25 轮：结构性瓶颈**被解开**，集成方补三处 Engine 解锁点

### 22.1 提升
4 车道波次（`p2-rungate-impl` S1 + `p3-m2-dbdata` P4 + **`p6-m2-playersurface`** + `p6-m2-pets` 报告）
+ 集成方直接补的 3 处 Engine 编辑，一次门禁全绿 → `main` = `7dc7c13a`，全量 **15,174 → 15,685**。

**★ 本轮最重要的事：§18.7 登记的"结构性瓶颈（`TPlayObject` 面）"被解开了。**
车道 `p6-m2-playersurface` 用 §19.6 的 `partial` 手法，在**自己的新文件**里补齐了
**95 个成员/方法、153 例测试、且未改任何既有文件**（`git show --stat` 只有 12 个新增文件），
并且**对 71 个新成员名逐个 `git grep main` 做防复发核验**（只有 3 个命中，均为已复用项，无 CS0102）。

**下游解锁实测**（对照 §18.7 的四条阻塞清单）：

| ObjNpc 方法 | 阻塞解除 |
|---|---|
| `TMerchant.UserSelect`(814 行) | **11/12**（仅剩 `SendMsg` 命名） |
| `TMerchant.ClientBuyItem`(323) | **6/7**，且 **`ClientSellItem` 现只差 `SendMsg`** |
| `UpgradeWapon` 外层(72) | 该给的 **全解除** |
| `TNormNpc.GotoLable`(312) | **9/10** |

### 22.2 集成方补的三处 Engine 解锁点（车道做不了、我做）

| # | 位置 | 改动 | 理由 |
|---|---|---|---|
| 1 | `Engine/Castle.cs:406` | `TCastleManager` 改 **`partial`** 并补 `GetCastleNameList(TStringList)`（原文 `Castle.pas` 语义：按 `m_CastleList` 顺序 `Add(m_sName)`） | 该车道明确报它**不是 partial**，故无法用自有文件补；这是 ObjNpc 报的"**最高投产比补齐点**"（补上后 `NpcSeams.GetCastleNameList` 即可从空实现改为转调真实现，攻城列表路径真通） |
| 2 | `Engine/ObjBase.cs:166` | `m_ItemList` 由 `List<TUserItem>` 改 **`List<TUserItemView>`**（该字段**全仓零调用方**） | 车道指出其物品容器用 `List<TUserItemView>`（与既有 `m_UseItems` 一致），元素类型不同则**无法用 `override` 桥接** ⇒ 只能各持一份后备字段形成"双容器"。改类型后可桥接 |
| 3 | `Engine/ObjBase.cs:169` 构造函数 | 追加 `PlayerSurfaceVarDefaults.MigrateStringVarDefaults(this);` | 原文 `m_TVal`/`m_ZVal`/`m_sString` 元素默认值是 `''`，托管是 `null`；车道**无法自补**（该文件已有无参构造，它再声明一个会 **CS0111**） |

**同步**：一处"差异锁定"用例（`ZVal_ManagedElementDefaultIsNull_DiffersFromDelphiEmptyString`）原本**断言缺陷本身**（元素是 null），
随第 3 项修复改为断言**修正后的原文语义**（默认 `''`），并保留历史说明。**这是本工程第 N 次"守卫用例随 Core 修复而翻转"** ——
该模式运转正常：守卫的价值就在于**Core 一改就红，强迫复查**。

### 22.3 两条高价值的技术发现（都由车道产出）

1. **★ `GetScriptLabel` 在原文里就是坏的**（`ObjPlayer.pas:15216`）：
   `sLabel := GetValidStr3_Ex(sCmdStr, sCmdStr, '/')` —— Delphi 该函数把**分隔符之前**的部分写进 `Dest`（第二实参 `sCmdStr`）、
   **返回值**是分隔符**之后**的剩余串。于是 `sLabel` 拿错变量、真标签留在 `sCmdStr`，
   紧接着 `:15226` 的 `sLabel[1] = '@'` **对任何输入都不成立** → **`m_CanJmpScriptLableList` 恒空**。
   **影响**：`LableIsCanJmp` 只可能命中 `@main`/`@HeroMap`/Yes/No 四个硬编码项。
   **处置**：车道逐字照抄 + 单测锁死；我已**转告 ObjNpc 车道不要按"标签应该能跳转"写用例**（否则会去"修"一个原文就坏的东西）。
2. **`g_btShowLogLevel: Byte` 使 `<= 0` 不可达**（`uFrmMain.pas:1109-1113` + `GateShare.pas:1172`，车道编号 **C15**）：
   `ReadInteger` 结果**先截断成 Byte 再比较** ⇒ 对负值永不可达（Byte 无负值），实际等价于 `= 0`；
   INI 里写 `-5` 会变成 **251**，随后命中上界钳位得 **11**。
   **叠加**该车道自己登记的 C1（上界钳到 `Items.Count` 而合法下标少 1）→ 任何 >10 的值都会走到
   `cbbShowLogLevel.ItemIndex := 11` 的**越界**路径（Delphi 静默 -1 / WinForms 必崩，即 §21.3 那个陷阱）。
   **★ 直接印证了 §21.3**：车道已加 `DelphiItemIndex(value,itemCount)` 垫片，且**先用例断言 WinForms 原生赋值确实抛异常**、再断言垫片不抛 —— 否则垫片没有存在意义。

### 22.4 第 4 处"接缝臆造"被改正（同一模式的持续清剿）

`FormGlobals.nMaxClientMsgCount` 原为 **`const int = 5`**，注释还振振有词地解释"INI 里写的是这个看似同名实则不同的常量"——**是错的**：
`GateShare.pas:1226` 的真实声明是 **`nMaxClientMsgCount: Integer = 100;`（`var` 段、可变）**，
`uFrmMain:1207` 会从 INI 覆盖它、`uFrmSafeFilter.pas:740` 会把它写回 INI；被误当成它的是 `g_Config.nSumSpeedMaxCount`(=5)。
已改为 `static int = 100` + 复位，并订正 2 处测试期望。
**这是 §12.x/§20.x 反复出现的"接缝臆造"的第 4 例** —— 共同特征是：**接缝里写了一个"看起来像原文"的常量/默认值，但没有回读原文行**。
**派发提示词应继续强调**：接缝里的每一个常量、默认值、签名都必须在注释里带 `原文 文件:行`。

### 22.5 新增待办
- **命名裁定**：`TBaseObject.SendMsg(BaseObject, wIdent, wParam.., sMsg)`（原文 `ObjBase.pas:30339`）
  与既有 `TCreature.SendMsg(ushort,long,long,long,long,string)`（**入队版**）**同名不同义**。
  裁定：**新成员命名 `SendTo(...)`**，不得用 `override`/`new` 覆盖入队版；已在注释中说明原因。已转告 ObjNpc 车道。
- **`TSafeList`/`TSafeStringList`/`TM2CriticalSection` 等的"读锁不可重入"**（§23.3 式的原文缺陷，由 `p3-m2-sweep` 报出）：
  原文「掩码后 CAS」使第二位读者永不成功、自旋到 1000ms 超时 —— 后续所有锁调用方都要知悉（已记在 §19 相关条目）。
- **静态污染**：`FormGlobals.ResetForTest()` 不重置 `g_EatItemCDConfig` 的 42 个字段（`p2-rungate-impl` 报），正是 `p6-test-isolation` 车道在查的同类问题，已转告。

## 23. 第 26 轮：四项并轨 + 测试隔离机制落地

### 23.1 提升
4 车道波次一次门禁全绿 → `main` = `52d3e076`，全量 **15,685 → 15,828**。**`main` 已到 15,828 例**（本会话起点 10,659）。

| 车道 | 贡献 |
|---|---|
| `p2-rungate-impl` | `RunGateUiSeams`：复刻 Delphi `ItemIndex` 越界静默置 -1，堵住 §22.3 的 C1 崩溃路径（+14 例） |
| `p6-core-paradox` | `TParadoxDataSet` 全量测试 117 例（`GXX.Core.Tests` **550 → 667**）；并修正两处实现问题：记录布局改为 **`Explicit` FieldOffset**（`Pack=1` 对 `fixed buffer` 会**静默前移**）、接缝 `Eof`/`Value` 后备字段与 `GetFieldData` 的 `False` 传播 |
| `p6-test-isolation` | **步骤 2 落地**：程序集级静态全局快照/还原（xUnit `TestFramework` + `IXunitTestCase` 包装）—— 即 §19.5 那个"不改任何既有测试文件即可让每个测试自动隔离"的方案 |
| `p6-m2-playersurface` | 交付报告（成员添加表 / 71 个成员名防复发核验 / 四优先清单打勾 / 15 条原文缺陷 / 接缝精确签名） |

### 23.2 记一条值得注意的实现坑（`p6-core-paradox`）
**C# `Pack=1` 对 `fixed buffer` 字段会静默前移偏移** —— 该车道发现并改为**显式 `FieldOffset`** 才与 Delphi 的 `packed record` 逐字节一致。
这类"布局看起来对、实际偏移错了"的缺陷**不会被编译期发现**，且只在与真实二进制交互时暴露；
**规程**：凡 1:1 复刻 packed record / 二进制布局，**一律用显式 `FieldOffset` 并写"逐字段偏移"断言**，不要依赖 `Pack`。

### 23.3 本轮新开车道
`p6-core-hardinfo`：`Common\HardInfo.pas`（898 行，机器码/硬件信息采集；`Client.dpr:69` 与 `M2Share.pas:8446` 在用，C# 全树 0 命中）。
放 `GXX.Core/Hardware/**`（客户端与服务端的共同下位）。**派发时已明确取舍**：
所有 OS 调用（CPUID / `DeviceIoControl` 取 IDE 序列号 / `EnumDisplayDevices` / `GlobalMemoryStatusEx` / WMI / 注册表）**一律走接缝**，
**纯逻辑（`ChangeByteOrder`、`CountSetBits`、寄存器解读与字符串拼装、HWID 组合规则、数值→可读字符串、容错回退）必须高覆盖** ——
即"取原始数据"与"怎么处理数据"分开，前者接缝、后者带断言。

### 23.4 测试隔离机制交付（`p6-test-isolation`）与**两条新的跨车道规程**

**机制**：**程序集级自定义 xUnit `TestFramework`**（`XunitTestFramework.CreateExecutor` → 自定义
`XunitTestFrameworkExecutor.RunTestCases` → 包装 `IXunitTestCase.RunAsync`）。
**选它的理由（值得记，因为它否掉了一个更简单的方案）**：`BeforeAfterTestAttribute` 在 xUnit 2 里**只能逐方法/逐类标注、无法程序集级生效**，
而本缺陷的根因恰恰是"**没人记得还原**" —— 靠逐类标注必然随新测试漂移。程序集级钩子则**零改既有测试文件、未来新测试自动受保护**。
语义：快照点在内层用例之前（类构造器/`BeforeAfter` 之前），`finally` 还原；还原目标是"**本用例开始前的状态**"而非进程初始默认值。

**覆盖规模**：**23 个类型 / 1107 槽位（M2Config 占 1048）**，反射口径（`Public|NonPublic|Static|DeclaredOnly`），
排除 const 13 / 非数组 `static readonly` 36 / 手写 getter 的静态属性（怕副作用）；
**开销实测 0.164 ms/用例**（全量 ≈1.2 s，墙钟无可测差异）。

**前后对照（硬证据）**：复现用例两类两顺序（用类级 `ITestCaseOrderer` 显式钉死顺序）——
修复前"污染→受害"顺序 **1 failed / 1 passed**（Expected `.\Envir\` / Actual `D:\Mir\Envir\`）、反向顺序 2 passed ⇒ **顺序相关**；
修复后两顺序各 2 passed ⇒ **顺序无关**。（它另做了一次"装配加载时污染全局量"的量测：整工程 7288 passed / **只有那 2 个复现受害例失败**，
既有 7286 例全活 —— 说明**既有测试都自己钉了临时目录**，真正的受害者是尚未落地的 `SweepNationsTests`，其复现即它的替身。）

#### ★ 规程 A：**禁止用 `Set-Content`/`-replace` 改写 `.cs` 源文件**
该车道用 PowerShell `Set-Content` 重写 `TestConfig.cs` 时**损坏了文件编码**，把
`[assembly: CollectionBehavior(DisableTestParallelization = true)]` 注释掉了 ⇒ **套件随即并行执行并挂死 10 分钟以上**。
**规程**：改代码文件一律用编辑工具（或明确指定编码的写入），**不要用 `Set-Content`/`-replace` 批量改写 `.cs`**；
并且认识到 **`DisableTestParallelization` 是承重墙** —— 它一旦失效，表现是"挂死"而不是"失败"，极难归因。

#### ★ 规程 B：**xUnit 2 同类内方法顺序是 UniqueID 哈希序，不是声明顺序**
任何"靠 A 先于 B 执行"来构造的测试都**不可靠**（同一类内方法顺序由哈希决定）。
**写跨用例污染/时序类测试时，必须用类级 `ITestCaseOrderer` 显式钉死顺序**，否则测试会在换机器/改方法名后随机翻转。

**其它被它覆盖的真实风险**（比"路径泄漏"更危险）：`M2Forms` 的注入位（`MessageBoxHandler` 等）泄漏会**挂死 testhost**；
`DbLayerGlobals.GetTickCount` 若泄漏成常量，重试/超时循环可能**永不结束**；
`M2ShareState` 的 4 个 private INI 缓存会按被污染的 `g_sSelfFilePath` 建错路径。

**裁定（它留给我的一条）**：把**在飞车道的 src 侧静态接缝**（`NpcSeams`/`AuctionDbRunSeam`/`DbLayerRunSeam`）
**纳入隔离覆盖**（该机制按其说明只需在 `M2ConfigIsolationCoverage.Types` 加一行即生效）——
这几类"静态委托持有者"正是跨测试污染的典型载体，自动复位比依赖各车道自觉调用 `ResetDefaults()` 更可靠。测试侧的接缝不纳入（不归该程序集管）。
**已落实**（`e85e3394`）：纳入后 **23→26 类型、1107→1180 槽位**（`NpcSeams` 58 + `AuctionDbRunSeam` 8 + `DbLayerRunSeam` 7）；
门禁例数不变（7298/0），且**该三类接缝今后新增委托/标量字段会自动受"逐成员必须命中"守卫保护**，不靠谁记得改清单。

## 24. 第 27 轮：Client 侧移植 + 一条**必须补的虚分派缺口**（不会在 build/test 中暴露）

### 24.1 提升
4 车道波次 + 一次重吸收，门禁全绿 → `main` = `ed7e9c0b`，全量 **15,828 → 15,998**。

| 车道 | 贡献 |
|---|---|
| `p6-client-actor` | **`CustomActor.pas` 1,130 行 8/8 成员全量移植**（`CustomActor.cs` 1,989 行 + 153 例，含 10 组差异断言）；删除 `PlaySceneNewActor.cs` 里那 6 行桩 |
| `p6-core-paradox` | `TParadoxDataSet` 全量（30 方法）+ 120 例（`GXX.Core.Tests` 连跑 10 次稳定）；`PxLangTable` 118 项脚本抽取 + 双向回读比对 **118/118 identical** |
| `p4-m2-objnpc` | 切片14：**吸收 main 后去接缝化/去重**（删 8 个已落地委托、`GetStdItem`/`GetStdItemName` 转发单一存储、`GetCastleNameList` 接真实现、**`SendMsgToClient` 按裁定改名 `SendTo`**、`m_ZVal` 去掉冗余归一） |
| `p6-test-isolation` | §23.4 的接缝纳入（`e85e3394`） |

### 24.2 ★★ 待办（高优先，且**不会在 build/test 中暴露**）：Client 侧基类缺 `virtual`

车道 `p6-client-actor` 报出：原文 `CustomActor.pas` 的 **7 个方法全带 `override`**，而托管基类的情况是——
- `ActorCore.cs:247 CalcActorFrame()`、`ActorCore.cs:338 Run(uint now)`、`ActorMotion.cs:191 GetDefaultFrame(bool)` → **均非 `virtual`**；
- `LoadSurface` / `DrawChr` / `RunSound` / `RunActSound` → **基类根本没有同名成员**。

该车道**拒绝**用"非虚方法/静态委托"伪装覆写（§18.8 的事故形态），也**没有越界改基类**，而是把逻辑全抽成纯静态层锁死，
并给出**可直接套用的补虚清单**（**待集成方执行**）：

```
ActorCore.cs:247    public void CalcActorFrame()            -> public virtual void CalcActorFrame()
ActorCore.cs:338    public void Run(uint now)               -> public virtual void Run(uint now)
ActorMotion.cs:191  public int GetDefaultFrame(bool wmode)  -> public virtual int GetDefaultFrame(bool wmode)
// 并在 TActor 上新增 4 个虚成员（对应原文 TActor 的虚方法）：
public virtual void LoadSurface();
public virtual void DrawChr(int dx, int dy, bool blend, bool boFlag);
public virtual void RunSound();
public virtual void RunActSound(int frame);
```
补齐后：`CustomActor.cs` 里 3 处 `new` → `override`；`LoadSurface(object? sender)` → 无参并 `override`（原文 402-584 全文不用 `Sender`）；其余 3 处加 `override`。**调用点无需改动**（虚分派自动生效）。

**为什么必须做**：调用点全都是**基类静态类型**（`PlaySceneMessages.cs:729`、`ActorMessages.cs:277/287`、`ActorMotion.cs:290`），
非虚则"自定义怪的帧计算/默认帧/推进"被**通用动作表静默接管** —— **编译过、单测过，只在多态路径上出错**。
**这是本会话第 4 次遇到同一形态**（前三次：`TNormNpc.GetVariableText`、`TNormNpc.Click`、`TDxImageButton.DoDrawCaption`）。
**规程建议**：派发任何"移植 `override` 方法"的车道时，提示词里必须要求它**列出每个覆写对应的基类虚成员**；
基类缺失时给出补虚清单（本车道就是这么做的，做法正确）。

### 24.3 `TParadoxDataSet` 的三条原文缺陷与四个移植陷阱（车道实测）

**原文缺陷**：
- **D17**（`:797`+`:800-803`+`:805`）**字段名被读到 TableName 区之后**：`:797` 先加"字段信息区之后"尺寸，`:800-803` **又加** TableName 区尺寸（v7=261 / v3.5=79），`:805` 拿这个 P 当**字段名区起点** ⇒ 名字整体后移 261/79 字节（`:799` 的注释 `// TableName size` 恰好暴露了那个偏移本是给表名的）。
- **D19**（`:1005`+`:937-957`+`:1052`）**未初始化内存**：`GetMem` 不清零，而 `grBOF`/`grEOF` 分支**连 `RecordIndex` 都不写**就返回 ⇒ `GetRecNo` 读未初始化内存。
  表现为**门禁偶发失败**（约 1/6 概率，`RecNo` 得 `166957392`）。**车道没有当"环境抖动"重跑放过，而是连跑到复现并定位** —— 这是本轮最有价值的排障。托管侧已在 `AllocRecordBuffer` 显式清零（**有意偏离，已登记**）。
- **D18**（`:1281-1286`）**`FileLoc` 的文件偏移与块内索引共用 32 位**：最高位置位时 `and $FFFFFF00` 得**负数**、`Seek(Loc+9)` 被喂负偏移；低 8 位被 `Idx` 占用 ⇒ **`.mb` 偏移必须是 256 的倍数**，否则偏移被索引覆盖（车道自己踩了：偏移 `$80` ⇒ `FileLoc=$000000FF` ⇒ `Loc=0` ⇒ **静默读到文件头**）。

**移植陷阱（会坑人，但不是原文缺陷）**：
1. **`[StructLayout(Pack=1)]` 表达不了 Delphi `packed record`** —— CLR 只接受 `Pack ∈ {1,2,4,8,16,32,64,128}`，且 **`fixed byte[N]` 会前移**（实测 `TPxDataHeader` 得 32 应 40、`TPxBlobIdx` 得 5 应 6）⇒ **必须用 `LayoutKind.Explicit` + `FieldOffset`**。（与 §23.2 同一坑，现已有两处实测。）
2. **Delphi 7 枚举最小 2 字节** ⇒ `SizeOf(TPxRecordHeader)` = **6**，托管 `enum:int` 会得 8、使偏移偏 2 ⇒ 用 `ushort` 承载 + 访问器。
3. `public bool Eof { get; protected set; }` **会生成第二个后备字段** ⇒ `Next()` 写的 `FEof` 永远读不到 ⇒ 必须 `=> FEof`。
4. `TPxDataHeader` 的 `$006C/$0072` 是**文件绝对**偏移，本记录内应减 `$58`（首版照抄得 `Size=120`）。

### 24.4 `TParadoxDataSet` 的两条跨工程请求 + 裁定

1. **`ParadoxConv` 的方向问题**：`GXX.RunGate.csproj` 引用 `GXX.Core` ⇒ **`GXX.Core` 不能反向引用 `GXX.RunGate`**（循环）。
   故该车道在 `GXX.Core` 侧只做**形状声明 + 两个可注入钩子**（`TEncodingKind` 与 RunGate 同名枚举**整数值一致**可直转），
   依赖面实测仅 3 个入口 / 6 个调用点。**方向判断正确**。
2. **裁定**：**推荐把 `ParadoxConv.cs` + `ParadoxConv.Tables.g.cs` 从 `GXX.RunGate` 下沉到 `GXX.Core`**（跨工程搬迁，集成方任务）——
   这样接缝可删、真正实现"**只移植一份**"，也解开 `GXX.GameCenter` 拿不到 `ParadoxConv` 的死结（GameCenter 只引用 `GXX.Core`）。
   **未执行**（属跨工程搬迁 + 需改 `RunGate`/`GameCenter` 引用点），登记为待办。
3. `BDEToSqliteForm.cs:35` 的 `IGBDEDataSet` 接缝需要 ~25 行适配器指向 `TParadoxDataSet`（`FieldCount`/`Fields[i].AsString|AsInteger|AsFloat`/`Eof`/`First`/`Next` 语义等价）。
   **裁定：批准**，但**明确禁止在 `M2Server` 里重写解析逻辑**（解析已在 `GXX.Core`）。

### 24.5 又一条工具链规程（与 §23.4 规程 A 同源）

`p6-client-actor` 报告：该文件**混用行尾**，用 pwsh 的 `ReadAllText`/`WriteAllText` + here-string 替换会**静默失配**并**误删闭括号**，
而 **Roslyn 只在文件末行报 `CS1513`**（报错位置与故障位置相距极远，排查代价高）。
**规程**：**对 `.cs` 一律用编辑工具（`edit`/`write`），不要用脚本字符串替换** —— 与 §23.4 规程 A（禁用 `Set-Content`）合并为同一条纪律。
**另**：该文件混用行尾也说明"用脚本按 `\n` 切分算行号会漂移"，与 §13 的既有警告一致。

## 25. 第 28 轮：ObjNpc 去接缝化的实质收益 + 代补三个脚本字段

### 25.1 提升
3 车道报告/收尾提交 + 集成方代补的 `TPlayObject` 脚本字段，门禁全绿 → `main` = `3698e30d`，全量 **15,998 → 16,003**（补字段后 M2Server 8,131 全绿）。

### 25.2 ★ "去接缝化"的收益被量化了（`p4-m2-objnpc` 切片14）

该车道删掉了 8 个**"默认恒返回 null"的静态委托**，并把 `GetStdItem`/`GetStdItemName` 改成**三个名字共用一个后备存储**、
`GetCastleNameList` 从占位改为**转调真实现**、`SendCustemMsg` 委托删除（子类 `inherited` 直接走 `base`）、
去掉 `m_ZVal[n] ?? ""` 归一。

**★ 它把接缝的危害总结成一句话，值得记下来**：
> 原先 8 个"默认恒返回 null"的委托，会把**「字段语义错」伪装成「分支没命中」**。

这正是接缝最危险的地方：**接缝的默认实现让错误的字段访问看起来像正常的控制流**，从而不在任何断言里暴露。
删掉接缝后，`GetLevelBaseObject*` 的 **4 个目标级别分支第一次真正接到 Engine 字段**。
**规程**：接缝的默认实现**不得**是"静默返回中性值"；应当**抛异常或显式报未接线**，
否则它就成了掩盖缺陷的伪装层（本工程已多次吃亏：§22.4 的 `nMaxClientMsgCount const 5`、
§24.3 的"接缝 `Eof`/`Value` 后备字段"，以及本条）。

### 25.3 集成方代补：`TPlayObject` 的三个脚本会话字段

Ob 该车道报 `TNormNpc.Click`(ObjNpc.pas:4431) 被三个字段阻塞，而它们**只存在于 `Engine.TScriptPlayer`**
（`NpcScriptState.cs:31/32`，且第三个缺失），`TPlayObject` 上没有。**我回读原文核对后确认它们确实是 `TPlayObject` 的成员**：
```
ObjPlayer.pas:149  m_nScriptGotoCount: Integer;    （:1647/:2816/:2820 置 0）
ObjPlayer.pas:356  m_sRandomString: string;        （:1781 置 ''）
ObjPlayer.pas:357  m_sNpcSelectItemName: string;   （:1782 置 ''）
```
已用 `partial` 追加 `Engine/PlayerSurface/TPlayObject.PlayerSurface.ScriptFields.cs`（**零改既有文件**）。
**收益**：`TNormNpc.Click` 可从"虚外壳"升级为真实现 ⇒ `TMerchant.Click`/`TGuildOfficial.Click`/`TCastleOfficial.Click`
三处覆写的 `inherited` **才真正落到原文逻辑**（此前落在一个空壳上）。

### 25.4 ObjNpc 的剩余缺口清单（供后续批次，**车道未自行声明任何替身**，正确）

| 缺口 | 阻塞 |
|---|---|
| `TPlayObject.m_boObMode` + `FilterShowName` | `TNormNpc.GetShowName`(9609) |
| `TPlayObject.m_nWalkSpeed`/`m_nInitWalkSpeed` | `TNormNpc.Initialize`(9864) |
| `m_UseItems`（**读写**，非只读） | `UpgradeWapon` 外层体 |
| `m_boSendMsgFlag`/`boSendCustemMsg`/`g_sSendCustMsgCanNotUseNowMsg`/`g_FilterTexts` | 现以同名列接缝绕过 |
| **`List<TUserItemView>` vs `TUserItem` 异型** | `ClientBuyItem` 的物品构造桥（车道**主动报告自己产物里的这处不一致**，值得肯定） |

**下一轮起点已定**（按车道建议）：`ClientSellItem`(71 行，最小可验证闭环) → `ClientBuyItem`(323) →
`UserSelect`(814) 按 `@buy`/`@sell`/`@repair` **分片**。

### 25.5 ★ 待办（承 §24.2，仍未执行）：Client 侧基类缺 `virtual`

**本轮已复核确认该诊断正确**（排除了一处误报）：`ClEvent.cs:121` 的 `virtual void LoadSurface()` 属于
**另一个类**（`ClEvent` 与本继承链无关），不是 `TActor` 的成员；实测层级是
`TCustomActor : TActor`（`CustomActor.cs:1583`）`: TActorCore`（`PlaySceneNewActor.cs:466`），
而 `TActorCore` **只声明了** `CalcActorFrame`(`ActorCore.cs:247`) 与 `Run(uint)`(`ActorCore.cs:338`)，**两者都非 virtual**，
且**没有** `LoadSurface`/`DrawChr`/`RunSound`/`RunActSound` 四个成员。
⇒ §24.2 的补虚清单**成立且必要**（补 3 个修饰符 + 在 `TActor` 上新增 4 个虚成员 + 7 处 `new`→`override`）。
**未执行原因（诚实记录）**：这是一次 5 文件改动，需连带改 `CustomActor.cs` 的 7 处；
本轮集成方上下文已接近上限，**主动选择不在余量不足时开始易错的重构**，改为先落地安全的合并与代补。
**下次执行时注意**：`virtual` 化基类成员会让既有同签名子类产生 **CS0114（隐藏）警告**（非错误，本仓不禁警告），
但**必须同时**把子类改成 `override` 才能获得多态收益 —— **只改基类不改子类 = 完全无效果**。

## 26. 第 29 轮：物品种类表示裁定 + **一条我自己的推断错误被代码否掉**

### 26.1 提升
`p4-m2-objnpc` 切片 15/16 并入（`TNormNpc.Click` **收成真实现**、`ClientSellItem` 1:1）→ `main` = `2b6fdd52`，全量 **16,003 → 16,059**。

该车道同时确认了一件重要的事：`TNormNpc.Click` 收成真实现后，`TMerchant.Click`/`TGuildOfficial.Click`/`TCastleOfficial.Click`
三处覆写的 `inherited` **才真正落到原文逻辑**（此前落在一个虚外壳上）；并**复核了 `Click` 在托管侧确为 `public virtual`**
—— §24.2 那个"基类非虚 ⇒ 多态静默走错实现"的坑**没有在同型场景复发**。**说明该规程已被车道内化。**

### 26.2 ★★ 我的一次推断错误：**改一个"零调用方"字段的类型 ≠ 接上了容器**

两轮前我把 `Engine/ObjBase.cs:170` 的 `m_ItemList` 从 `List<TUserItem>` 改成 `List<TUserItemView>`，
理由是"元素类型一致后 `BagItems => m_ItemList` 才可能桥接"。**本轮读代码发现这个改动毫无效果**：
`TCreature.PlayerSurface.Items.cs:180` 实际是 `protected virtual List<TUserItemView> BagItems => **m_BagItems**;`
—— 它用**自己的后备字段**，而该文件 `:184`/`:362-363` 的注释**已自认**"`m_ItemList` **未接入**"。
⇒ **"双容器"（`m_BagItems` + `m_ItemList`）至今仍然存在**，我那次改动只是改了一个**至今零调用方**的字段的类型。

**教训（写进规程）**：
> **改变一个"零调用方"字段的类型，必须同时验证"谁将要消费它"真的接到了它。**
> 否则得到的是"看起来完成了整合、实际仍是双容器"的**假整合** —— 它比明确不做更糟，因为它会让后来人（包括我）以为已经接上了。
> **具体做法**：改完字段后，用 `git grep` 确认**没有任何地方仍指向旧的后备存储**，并把"谁 => 谁"的接线点写进提交信息。

### 26.3 裁定：物品表示 **方案 A**（并授权车道自行执行）

车道 `p4-m2-objnpc` 报出：`TUserItemView`（`AddAbility.cs:64`）**只有 3 个成员**（`wIndex`/`BtValue[14]`/`CustomProperties`），
而它需要 `Dura`/`DuraMax`(可写)/`MakeIndex`/`Name`/`boIsBind`/`btBindOption` 六个字段；
被迫新增的 4 个只读委托（`ItemMakeIndex`/`ItemName`/`ItemDura`/`ItemDuraMax`）**从类型上就无法承载 `DuraMax` 的写回**，
而那是 `UserSelect`/`ClientBuyItem`/`UpgradeWapon` 外层体的**共同硬阻塞**。

**证据链是"既有代码自证"的三条**（比我的推断可靠）：① 视图只有 3 成员；② 那 4 个委托的注释自认是权宜；
③ `GXX.Core.Protocol.TUserItem` 是**已 1:1 的权威记录**。

**裁定 方案 A**：`TUserItem` 是唯一**存储与权威**类型；`TUserItemView` 退化为 `GetAccessory.Apply` 的**按需视图**。具体：
1. `ObjBase.cs:170` `m_ItemList` **改回 `List<TUserItem>`**；
2. `BagItems` 改为 **`=> m_ItemList`**（**真正**接上单一容器）并**删除后备字段 `m_BagItems`**；
3. **删除那 4 个只读委托**，改直读 `TUserItem` 字段
   —— 依据 §25.2 立的规矩：**默认静默返回中性值（`_ => 0` / `_ => ""`）的委托会把「字段语义错」伪装成「分支没命中」**，删掉比留着安全；
4. `GetAccessory.Apply(...)` 的入参**保持 `TUserItemView`**（视图 ≠ 存储），调用处按需现造视图 ⇒ `RecalcChain`/`m_UseItems` 一行不改；
5. `AddItemToBag` 等因签名变化的连锁，按原文 `PTUserItem` 语义统一为 `TUserItem`。

**执行方式**：已把 `Engine/ObjBase.cs` 与 `Engine/PlayerSurface/TCreature.PlayerSurface.Items.cs`
**窄授权（`!`）给该车道自行改动** —— 它是唯一已经逐行逆向过 `GetUserItemPrice`/`ClientSellItem` 的车道，比新开车道重读更快，
且 `m_ItemList` 至今零调用方、风险面被约束在这两个文件内。

**同型问题一并登记（本次不改）**：`GetItemAddValue` 在托管侧有**两套标准物品表示**
（`GXX.Core.Protocol.TStdItem` 权威 vs `Engine.TStdItemView` 视图），且**字段名不一致**（`AniCount` vs `Anicount`、`Name` vs `NameStr`）。
裁定：车道的接缝按**权威侧**定名是正确的；两套命名的统一留待后续，先在报告里列对照表。

### 26.4 本轮新开车道与授权
- `p7-client-virtual`：执行 §24.2/§25.5 的**虚分派恢复**（补 3 个修饰符 + `TActor` 新增 4 个虚成员 + `CustomActor.cs` 7 处 `new`→`override`），
  并**要求它写一个"改前失败、改后通过"的多态验证测试**（这是本任务的核心证据）。
  已给它 `!` 授权：4 个源码文件 + `CustomActorTests.cs`（后者是它需要同步期望的既有文件）。
- `p4-m2-objnpc`：新增 `Engine/ObjBase.cs`、`Engine/PlayerSurface/TCreature.PlayerSurface.Items.cs` 两个窄 `!`（见 26.3）。

## 27. ⚠ **共享测试套件里的偶发失败**：`ParadoxDataSetCursorTests.EmptyTable_First_IsEof_NoRecords`

### 27.1 现象（由车道 `p6-core-hardinfo` 在**干净基线**上复现）
它把自己的 7 个新文件**全部移出树**后跑 `GXX.Core.Tests`，4 次运行：`0/667`、`1/666`、`1/666`、`0/667`；
恒失败的是 **`ParadoxDataSetCursorTests.EmptyTable_First_IsEof_NoRecords`**（`tests/GXX.Core.Tests/ParadoxDataSetTests.cs:779`，`Assert.Equal(0, ds.RecNo)`），
实测值**每次都不同**（`1701869908` = `0x656C6F74`、`41514896`），且**与并行度相关**（`-v n` 更慢时多次全绿，默认 verbosity 下失败率高）。
**它自己的 165 例 Hardware 测试单独跑 165/165 全绿** —— 即该失败与它无关。

### 27.2 为什么这必须优先修（不只是"某条车道的小问题"）
按本工程既定规程 —— **"车道门禁 = 构建 0 error **且** 该工程测试全绿"**、**"最后一次提交必须全绿"** ——
这条随机失败会：
1. **随机卡住任何一条车道的收尾**（`GXX.Core.Tests` 是全工程共享的下位测试工程）；
2. **让集成方误判**：要么错杀一条本来健康的分支，要么把偶发当成"基线漂移"而放行一条真坏的分支。
⇒ **必须修到确定性，不能靠"再跑一次就好了"。**

### 27.3 集成方的排查（排除了一次误判，缩小了根因）
该失败**不是**§25 那条 D19 的残留（车道 `p6-core-paradox` 曾为 D19 做过一次"有意偏离"修复），因为：
1. `ParadoxDataSet.cs:592` 的 `AllocRecordBuffer` **确实清零**（`Marshal.AllocHGlobal` 后 `PxBuffer.FillChar(p, size, 0)`）；
2. `PxBuffer.FillChar` 是**真实现**（`new Span<byte>((void*)ptr, count).Fill(value)`），**不是空壳接缝**；
3. 我在 `ParadoxDataSet.Seams.cs` 里**未发现**共享/静态缓冲。

**因此新的根因假设**：`ActiveBuffer` **指向一块已被释放、或从未属于本次 `First()` 的缓冲**（use-after-free / 陈旧指针）。
支持证据：观测到的值像**文本**（`0x656C6F74` → 字节 `74 6F 6C 65` = `"tole"`），
**像是别的测试写过的记录文本残留** ⇒ 读到了**其它数据集实例用过的同一块非托管内存**。

**处置**：已把该问题的**完整证据与排查结论**转给原属车道（`p6-core-paradox`，它写的 D19 修复、最了解该接缝），要求：
① 定位"grEOF 路径下 `ActiveBuffer` 到底是本次分配 / 上一实例遗留 / 已释放"；
② 按 D19 同一原则**修到确定性**（**并明确指出"这块缓冲在 grBOF/grEOF 路径下本就不该被解引用"或"必须指向本次已清零的缓冲"**，
不接受"在 `RecNo` 读取处包一层 `if` 返回 0"这种掩盖式修法）；
③ **连跑 10 次 `GXX.Core.Tests` 要求 10/10 全绿**，并解释"为什么此前只在默认 verbosity 下失败"。

### 27.4 给集成方自己的规程（在修复落地前）
只要该用例仍在，**每次门禁都必须用"失败用例名"核对**，而不是只看"failed>0"：
- 若**唯一**失败项是 `EmptyTable_First_IsEof_NoRecords` ⇒ 视为**已知偶发**，**重跑该工程一次**再判；
- 若失败项**还包含别的** ⇒ 按正常失败处理（不得用"偶发"掩盖）；
- 该豁免**只对这一个用例名有效**，且**必须在台账里可查**（即本条）—— 不允许口头/记忆式豁免。

## 28. 第 30 轮：虚分派恢复落地 + **我又一次侦察疏漏**（只搜了 `src` 没搜 `tests`）

### 28.1 提升
3 车道波次（`p4-m2-objnpc` 6 提交 + `p6-core-hardinfo` 2 提交 + `p7-client-virtual` 2 提交）一次门禁全绿
→ `main` = `1aa3fd65`，全量 **16,059 → 16,273**。已核实虚分派修复在 main 上生效
（`ActorCore.cs` 有 `public virtual void CalcActorFrame`、`CustomActor.cs` 有 `public override void CalcActorFrame`）。

### 28.2 ★ 虚分派恢复（§24.2/§25.5 那个"不会在 build/test 中暴露"的缺口）—— 已修，且**证据是"改前失败"**

车道 `p7-client-virtual` 完成 11 处改动（基类 3 处 `virtual` + `TActor` 新增 4 个虚成员 + `CustomActor.cs` 7 处 `new`/普通 → `override`），
**方法体零改动**，并给出本任务最关键的证据：
- **改动前**：3 例断言**失败**（`CalcActorFrame` 期望 112 实得 **20**、`GetDefaultFrame` 期望 120 实得 **32**、`Run` 的 `m_boCreateEffect` 未回落）
  —— 即**通用动作表 MA19 确实接管了子类实现**，把"多态静默走错实现"从推断变成了实测；
  另 4 个成员在改动前**基类上根本不存在**（`CS1061`），无法构造断言失败态 ⇒ 车道**以编译失败态作为改动前证据**（探针文件已删）。**这个处理是诚实且正确的。**
- **改动后**：7/7 全绿。
- **CS0114 清单：空**（全仓无其它 `TActor`/`TActorCore` 子类声明这 7 个同签名成员）；
- **警告 0 新增**：用 `--no-incremental` 全量构建 + 与基线警告清单 `Compare-Object` **双向差集为空**（150 条逐条相同）——
  这是"证明没引入新警告"的正确做法，值得其它车道效仿。

### 28.3 ★ 我的侦察疏漏：查调用点时**只搜了 `src`，漏了 `tests`**

我派发时断言"**全仓没有任何地方以带参形式调用 `LoadSurface(...)`**"，依据是我跑过 `LoadSurface\s*\(` 的 grep。
**该断言不成立**：`tests/GXX.Client.Tests/CustomActorTests.cs` 有 **3 处 `a.LoadSurface(null)`**（原 `:1634/:1648/:1809`）。
原因很具体：**我的 grep 只覆盖了 `GXX.CSharp\src`，没有覆盖 `tests`**。

**后果与处置**：收敛签名为无参会**编译失败**，车道做了 3 行最小机械改动（`a.LoadSurface();`）并**主动上报**、还给出"若认为越区可 revert"的选项。
**裁定：保留该改动**（它是编译的必要条件；revert 等于放弃第 6 项改动）。车道的处置正确 —— **它没有因为"任务书说没有调用点"就停下来，也没有默默改掉不说**。

**新规程**：
> **凡"查某成员的调用点/引用点"，必须**同时**搜 `src` 与 `tests`**（本工程的测试工程里有大量真实调用点，
> 且测试常以**基类静态类型**持有实例，正是多态相关改动的关键消费方）。
> 具体命令应包含两个根：`GXX.CSharp/src` 与 `GXX.CSharp/tests`。
> 本会话已多次栽在"只看了 src"上（本条是最典型的一次）。

### 28.4 遗留待办（由该车道指出，均已登记）
1. **基类那 4 个新成员是空实现** —— 本轮只补"分派槽位"，原文 `TActor.LoadSurface`/`DrawChr`/`RunSound`/`RunActSound` 的**本体仍未移植**；
   因此 `CustomActor` 里 7 处"前置门为真 → `inherited`"的分支目前只 `return`（因基类为空实现，**行为与改动前等价**，不构成回归）。
   **待基类本体落地后再接**。
2. **真正的待办不是"还有谁该 `new`→`override`"（0 个）**，而是 `Tail/HerbActor.cs` 记录的、**尚未写成类 override 的原文子类**：
   `TKillingHerb`/`TBeeQueen`/`TMineMon`/`TCentipedeKingMon`/`TCastleDoor`/`TWallStructure`/`TDragonBody`
   的 `CalcActorFrame`/`GetDefaultFrame`/`LoadSurface`/`DrawChr`/`Run` —— **基类虚化后已具备落 1:1 override 的条件**（这是本轮打开的下一批工作）。
3. `Tail/HerbActor.cs:11`（"`TActorCore` 的这两个方法是**非虚的**"）与 `:603`（"Actor 基类虚方法…受阻，本车道无权"）**两处注释已过期**，
   需相应车道更新（该文件不在 `p7-client-virtual` 的独占区）。
4. 生效调用点（`PlaySceneMessages.cs:729`、`ActorMessages.cs:277/287`、`ActorMotion.cs:290`）**此前无任何测试覆盖**，
   本车道那 7 例是第一批回归证据 —— 说明"多态路径"长期处于无测试状态，与 §24.2 的判断一致。

## 29. 第 31 轮：偶发失败（§27）**根因定案并修除** + 一起工作树误伤事故

### 29.1 提升
3 车道报告提交（含一处**同文件双侧新增冲突**，取作者版本解决）+ `p6-core-paradox` 的 D19 修复并入
→ `main` = `3176d555`，全量 **16,273 → 16,302**。

### 29.2 ★★ §27 那条偶发失败：**根因是 D19（未初始化内存）**，我提的 stale-pointer 假设**被排除**

车道 `p6-core-paradox` 用了**三段互证**，其中第三段是关键：

| # | 实验 | 结果 | 结论 |
|---|---|---|---|
| (a) | 列出 `FActiveBuffer` **全部**赋值/释放点 | 赋值仅 2 处（`Next()`/`Resync()`，都带 `== Zero` 判断）；清零点仅 1 处（`Close()`） | 空表用例**全生命周期只分配一次** ⇒ **不存在**"上一实例遗留/已释放"的指针 |
| (b) | grEOF 路径现场打印 `ActiveBuffer` 原始 6 字节 | `00-00-00-00-02-00` | 头部内容与布局都正确、未被覆写 ⇒ 不是"读到别人的缓冲" |
| (c) | **因果实验**：仅注释掉 `AllocRecordBuffer` 里那一行 `FillChar` | **第 2 次即复现**（`Actual: -35615349`）；恢复后 **31 次全绿** | 该行**既充分又必要** ⇒ 根因锁定 |

**★ 方法学价值（写进规程）**：
> **"根因定案"最有说服力的证据是"把嫌疑修复拿掉，看缺陷是否复现"的因果实验**，
> 而不是"加了修复之后不再出现"（后者与"偶发"无法区分）。
> 本轮 (c) 做到"去掉即复现、恢复即 31 连绿"，这才把"疑似"变成"定案"。

我提的 stale-pointer 假设**是错的**，且它给出的两个反证都有了机制解释（我**没有**继续坚持自己假设，这也是应有的态度）：
- **为什么值"像文本"**（`0x656C6F74` = `"tole"`）：`AllocHGlobal` 背后是**进程堆**，同尺寸块**常数级复用**；
  测试里大量含可打印字节的短命分配（`PxSyntheticDb` 的 `byte[]`、GBK 字段名 `MagName`/`Descr` 的往返）⇒ **复用巧合**，不是"别的数据集的记录文本"。
- **为什么与并行度相关**：唯一随机源就是**堆状态**；并行越杂越易"恰好非 0"。`-v n` 全绿是**概率性侥幸**，不是该配置无缺陷。

**修复方式也正确**：它**没有**采纳"只在 `RecNo` 读取处包一层 `if` 返回 0"（我在 §27.3 明确拒绝的那种掩盖式修法），
而是修**对象的不变量**：*"新分配的记录缓冲在返回时整块都已定义"*；并指出**同一块头部的另外 5 字节**（`BookmarkFlag` +4..+5）
与 `InternalSetToRecord`(:1030) 读的是**同一个头部** ⇒ 只兜底 `RecNo` 会漏掉它们。

**测试加固（值得效仿）**：① 原用例从"只断 `RecNo == 0`"加固为**断言整块 6 字节头部**；
② **新增独立证据用例**：先建含可打印文本 `"tole"` 的数据集并**释放**（故意在堆里留 ASCII 残留），再建空表走 grEOF，
断言 `RecNo == 0` **且 `NotEqual(0x656C6F74)`** —— **直接对"残留复用"这一根因机制下断言**，而不是只对表象下断言。
**稳定性证明**：31 次连跑全绿（默认 15 次 + `-v q/m/n/d` 各 3 = 12 + 强制 16 线程并行 4）。

### 29.3 ✅ 据此**撤销** §27.4 的暂时豁免
该偶发失败已修除，`GXX.Core.Tests` = **654/654**。
**规程更新**：§27.4 那条"唯一失败项为 `EmptyTable_First_IsEof_NoRecords` 时可重跑豁免"**即刻作废** ——
**门禁恢复为"任何失败都按正常失败处理"**。（保留此撤销记录，以免后来人继续沿用已失效的豁免。）

### 29.4 ⚠ 事故：一条车道的"干净基线"实验**误删了别人工作树里 64 个已跟踪文件**

`p6-core-hardinfo` 为做"把我的新文件移出树"的干净基线实验，实际造成
**`GXX.Core` 下 64 个已跟踪文件从磁盘消失**（覆盖 `Async/ Compress/ Crypto/ Data/ IO/ Launcher/ Protocol/ Rtl/ Stubs/ Util/` **以及 `Paradox/**` 全部 7 个文件**），目录里只剩 4 个 `.cs`。
`p6-core-paradox` 复核时发现并用 `git checkout HEAD -- GXX.CSharp/src/GXX.Core` 完整恢复（无内容损失）。
**连带影响**：我此前转述的"干净基线 4 次 2 红"是在**这棵被搬空的树**上得到的，**其代码状态与 HEAD 不一致**，不能直接对比
（不过 D19 的定案靠的是 (c) 因果实验，不依赖那组数据，故结论不受影响）。

**三条新规程（采纳该车道的建议）**：
1. **"移出 N 个文件"前后必须用 `git status --porcelain | Measure-Object` 数一遍数量**，确认移出的正好是预期的那 N 个；
2. **恢复只按路径白名单**：`git checkout HEAD -- <独占区路径>`；
   **绝不使用 `git restore .` / `git checkout .`** —— 那会**静默还原别的车道未提交的在飞产出**（比误删更隐蔽）；
3. **车道的"全绿"结论必须绑定"`git status` 为空"**：若工作树里有 ` D`（缺失）或他人的在飞改动，那条"全绿"就不可信。
   （本轮正是靠这条才发现：hardinfo 的干净基线实验期间，工作树根本不是它声明的那 7 个文件。）

## 30. 第 32 轮：物品表示方案 A 落地 + **两处"车道改进上级裁定"** + 一条必须登记的语义偏离

### 30.1 提升
`p4-m2-objnpc` 切片 22/23（方案 A 第②步）并入 → `main` = `5066c47c`，全量 **16,303 → 16,321**。
管线随后**再次排空**，本轮另开两条新车道（见 30.5）。

### 30.2 ★★ 车道两处**改进**了我的裁定（均采纳）

| 我的裁定 | 车道的改进 | 依据 |
|---|---|---|
| `m_ItemList` 改回 `List<TUserItem>` | 用 **`List<TUserItem?>`（可空）** | 原文槽位是 **`pTUserItem` 指针、允许 nil**，且原文多处**显式判空**（`ObjNpc.pas:1710`/`:4254`）；值类型元素**无法表达空槽** |
| （未提及归属层级） | **把 `m_ItemList` 从 `TPlayObject` 上移到 `TCreature`** | `BagItems`/`AddItemToBag`/`IsEnoughBag`/`CheckItems` **都在 `TCreature` 上**，够不到 `TPlayObject` 的字段 —— **这正是"双容器"产生的根因**；且原文 `ObjBase.pas:322` 声明在 **`TBaseObject`** 上 |

**规程含义**：车道在"照裁定执行"与"按原文纠正裁定"之间选择了后者，**并且给出了原文行号依据** —— 这正是要鼓励的行为。
对照 §26.2（我因未验证"谁消费它"而做出无效改动），这两处改进恰好补上了我当时缺的那一步。

### 30.3 ★ 方案 A 的**别名语义损失**：已要求登记为**正式偏差**（不是"注释说明"）

第②步完成后，`AddItemToBag` 的语义从原文的**指针别名共享**变成**值复制**：
原用例 `AddItemToBag_KeepsReferenceSemantics_LikeOriginalPointerList` 原本断言"改 `it.wIndex=999` 后背包里读到 999"，现在读到的仍是复制进去的值。
**这不是断言写法变化，而是真实语义偏离**：原文 `m_ItemList` 存**指针**，"改动物品对象后列表里那件也变"是**原文的真实行为**；方案 A 用值类型元素丢掉了它（代价是调用方**必须写回槽位**）。

**处置**：要求车道**登记为一条正式偏差（沿用 D 编号，如 D35）**，写清"偏离点 / 原文行为 / 托管行为 / 为什么必须偏离（`TUserItem` 是 struct）/ 若将来要恢复别名语义该怎么做（回方案 B：让视图包装权威记录）"，
**并在代码注释里同时写明"这是与原文的偏离"而不仅是"操作提示"**。
**理由**：这类"值语义 vs 指针语义"的差别**编译过、多数单测过**，只在"改了一处、另一处没变"时暴露 —— 与 §25.2/§26.2 那些"假完成"同族；
**只写在代码注释里不够，必须有台账条目 + 唯一编号**，否则后来人会把它当成 bug 去"修"。

### 30.4 ★ `TStdItemView.Anicount` / `AniCount`：确定性结论（**同一 Delphi 字段被拆成两个托管字段**）

车道按 §28.3 的新规程**同时搜了 `src` 与 `tests`**，给出全部读写点：
- **`Anicount`（小写 c）**：`AddAbility.cs:41` 声明；**被读**于 `RecalcChain.cs:55`（**特戒代码开关**，原文 `AddAbilitysByCode` 的 `IsShape` 双路之一）；无生产写点。
- **`AniCount`（大写 C）**：`AddAbility.cs:60` 声明；**被读**于 `GroupItems.cs:476`（**封号令**判定）；**被写**于 `tests/FormJ57Tests.cs:384/438/439`（对象初始化器）。

⇒ **两者都在用且用途不同** —— **不是"一活一死"，而是同一 Delphi 字段被拆成两个托管字段、各被一侧消费**。
**危害**：任何 `TStdItem`→`TStdItemView` 的构造若只填其一，**另一侧静默读到 0** —— 与刚删掉的那 4 个只读委托**同型的"沉默中性值"故障**。
**修法（一行）**：`AddAbility.cs:60` 改为转发属性 `public ushort AniCount { get => Anicount; set => Anicount = value; }`
（测试用对象初始化器，**属性初始化器同样合法 ⇒ 测试无需改**；`GroupItems` 自动与 `RecalcChain` 读同一份数据）。
**已授权该车道落地**（`AddAbility.cs` 加进其分区），并要求在提交信息里点明"这是 §29 那条沉默中性值故障的同族"。

### 30.5 本轮新开车道
- **`p7-dx-dibfusion`**：补齐 `DIB.pas` 原文 **4940-7930（约 3,000 行）** 的整段空洞
  （16 个 `Draw*` + 24 个 `TFColor.Do*` + 8 个重采样滤波器；由前序车道 `p3-dx-big` 的交付报告**明确记录为"从未产出"**）。
  派发提示词里逐条挂了**对本单元致命的既有规程**：`Pack=1` 表达不了 `packed record`（必须显式 `FieldOffset`）、Delphi 7 枚举 2 字节、
  `{ get; protected set; }` 生成第二个后备字段、Delphi 格式串必须走 `DelphiFormat`、**接缝不得静默返回中性值**、
  **查调用点必须同时搜 `src` 与 `tests`**、以及 §29.4 的工作树三条规程。
- **`p7-client-actor-family`**：工作树与分区已建（`ActorCore.cs`/`ActorMotion.cs`/`HerbActor.cs` + 新文件），
  待派发 —— 内容是 §28.4 登记的两项：**基类那 4 个空实现成员的原文本体**，以及**7 个原文 actor 子类**（`TKillingHerb`/`TBeeQueen`/`TMineMon`/`TCentipedeKingMon`/`TCastleDoor`/`TWallStructure`/`TDragonBody`）现在已具备的 1:1 `override`。

### 30.6 新登记缺口
`Bag` 目前是**只读视图**，**没有公开写回入口**（`AddToBag` 只能追加）。
车道提出：后续 `ClientBuyItem`/`UpgradeWapon`/`UserSelect` 分片若需"就地改背包里那件"，需补一个最小写入口（如 `SetBagItem`）。
**处置**：批准，但要求**先只申请这一个成员、并给出原文依据**，再动代码（车道自觉遵循了这一流程）。

## 31. 第 33 轮：D35 正式登记 + **我又一次"声称已做、实际未做"**

### 31.1 提升
`p4-m2-objnpc` 切片 24/25 并入 → `main` = `6bb213ea`，全量 **16,321 → 16,349**。两条新车道（`p7-dx-dibfusion`、`p7-client-actor-family`）均在飞。

### 31.2 ✅ D35 已按 D 编号惯例**正式登记**（含"恢复途径"）

车道的登记表覆盖了我要求的全部字段，摘其要点：
- **偏离点**：元素类型由原文 `pTUserItem` **指针** → `TUserItem?` **可空值类型**；
- **原文行为**：指针列表 ⇒ `BagItems.Items[I]^.X := v` 与"调用方手上那件"是**同一对象**（**别名共享**）；
- **托管行为**：`Add`/`SetBagItem` 都是**值复制** ⇒ 必须显式写回；
- **必须偏离的原因**：`TUserItem` 是 `struct`（1:1 wire/DB 权威布局），值类型**无法表达**"共享同一实例"；**可空保住的是"空槽"语义，不是"别名"**（这个区分很关键，它精确说明了可空与别名是两件不同的事）；
- **真实触发面**（不是猜测，是三个具体位置）：① `GetUserItemPrice(ref TUserItem,…)` 在 `StdMode=43` 就地改写 `DuraMax` → 传 `BagItems[i]` **编译不过**（`List<T>` 索引器不可 `ref`）；② `UpgradeWapon` 的 `m_UseItems[U_WEAPON].wIndex := 0`；③ `ClientBuyItem`/`UserSelect` 的"取出→改→放回"；
- **调用方契约（强制）**：`var t = BagItems[i]!.Value; …改 t…; SetBagItem(i, t);`
- **恢复途径**：唯一 = 回**方案 B**（视图持有权威记录引用）⇒ 届时 **D35 可注销**。

**唯一被迫改写的用例已改名并指明 D35**（不是静默适配）；两处代码注释现在都写明"这是与原文的偏离（D35）"，而非仅仅操作提示。
**这正是 §30.3 要求的形态** —— 本会话第二次把"车道发现/上级裁定"沉淀成**带编号、可检索、可注销**的正式条目。

### 31.3 ✅ `SetBagItem` 的**原文依据**（这条比实现本身更重要）
车道没有说"我需要一个写入口"，而是回读原文证明**原文本来就允许就地替换**：
Delphi `TList.Items[Index]` 是**可写属性**（`property Items[Index: Integer]: Pointer read Get write Put`）
⇒ 原文完全允许 `BagItems.Items[I] := UserItem;`；而托管 `Bag` 是 `IReadOnlyList`、`AddToBag` 只能追加
⇒ **必须**补等价写入口，否则 D35 契约里的"写回槽位"**无路可走**。
5 例覆盖：写回生效 / `-1` 越界 / 恰在 `Count` 越界 / 空背包任意下标 / 写 `null` 清空槽（对应原文槽可为 nil）。

### 31.4 ★★ 我的错误：**在消息里声称已授权，但实际没有改分区表**
我在上一轮回复里写了"**已把 `GXX.CSharp/src/GXX.M2Server/Engine/AddAbility.cs` 加进你的分区（`!`）**"，
**但我从未真正执行那次编辑**。车道通过 `git show main:GXX.CSharp/tools/lane-zones.tsv | grep AddAbility`（**空**）
核对 main 时发现了这个不一致，并**主动上报**（它按"直接授权"执行了那 1 行改动，且已验证零依赖，所以没有造成实际损害）。

**这是本会话第 3 次出现同一族的"假完成"**（§26.2 我改了零调用方字段却没验证接线；§27 的"修复未生效"；本条是**"声称做了却没做"**）。
**规程（新增，且是我给自己的约束）**：
> **绝不在消息里宣称"我已授权/我已修改/我已添加"** —— 除非**同一条回复里确实执行了那次编辑**。
> 更稳的做法：**在消息里直接给出让车道自查的命令**（如 `git show main:GXX.CSharp/tools/lane-zones.tsv | Select-String <路径>`），
> 让它能独立验证，而不是依赖我的陈述。
> **理由**：集成方是唯一能给授权的人；若授权"声称存在而实际不存在"，车道只有两条路——**越区执行**（违规）或**停摆**（低效），
> 而它无法自行区分"我漏做了"与"它记错了"。**让每一处授权都可通过仓库状态被验证**，是消除这种不确定性的唯一办法。

**车道做得对的地方**：它没有默默越区、也没有默默停摆，而是**执行 + 显著登记 + 要求补齐**，把选择权和事实都交回给我。
**已补**：`lane-zones.tsv` 的 `p4-m2-objnpc` 行加入 `!.../Engine/AddAbility.cs`（`6bb213ea`）。

## 32. 第 34–35 轮：管线加宽 + DIB 空洞开始填实

### 32.1 第 34 轮（无产出可合并，用于加宽管线）
两条在飞车道当时都在编辑阶段、无提交可并。故该轮用于**加宽**：
- 新开 **`p7-db-selectclient`**：`SelectClient.pas`（1,248 行，DBServer 选人会话层）——甄别清单里**最大的整单元缺口**。
  C# 侧实测只落了 4 条命令（100/101/102/103），缺 `RandomName`/`QueryDelChr`/`GetBackDelChr`/`KeepAlive`/`KickUser`/`ExecGateBuffers`/`DeCodeUserMsg` 与 **1000 槽会话表**；
  顺带记录 `Checklist.md:287` 把它标 ✅ 属**过度声明**。
- 分区表校验 30 行 / 0 处格式错误；`verify-lanes` **30/30 合规**。

### 32.2 第 35 轮提升：DIB 那 3,000 行空洞的第一个切片落地
`p7-dx-dibfusion` 切片 A 并入 → `main` = `0e84d569`，全量 **16,349 → 16,468**（`GXX.Client.Tests` 4,326 → 4,423）。

**落地内容**：`DIB.Fusion.cs` **1,418 行**（覆盖原文 `4940-5846` + `5917-5934`）+ `DxCtlDibFusionTests.cs` **1,587 行** = **3,005 行**。
这是 §30.5 登记的"**客户端最大的单块空洞**"（此前由 `p3-dx-big` 的报告记录为"`DIB.Fusion.cs` 从未产出"）的**第一个切片**。

**意义**：本会话的剩余量结构正在从"**整单元缺失**"转向"**巨型单元内部的未覆盖区**"——
DIB（8,747 行）、`MirClientContext`（11,125 行）、`ObjNpc`（10,546 行）、`uFrmMain`（4,216 行）、`DxMemo`（5,877 行）这类单元的**逐段收口**，
将成为后续轮次的主要形态。这类工作的特点：**单块体量更大、且常需先补依赖面**（如 `GateShare`/`ObjPlayer`/`TPlayObject`），
因此"先派哪一段"的排序比"开多少条车道"更关键。

### 32.3 第 36 轮提升：两条巨型单元同时收口
两条车道的切片同时并入 → `main` = `4d663e0c`，全量 **16,468 → 16,482**（`GXX.Client.Tests` 4,423 → 4,437）。落地 **1,201 行**：
| 车道 | 落地内容 |
|---|---|
| `p7-client-actor-family` | `TActor` **四个虚方法本体** 1:1（`ActorFamilyImpl.cs` 520 + `ActorFamilyEnv.cs` 308） —— 即 §28.4/§30.5 登记的"基类 4 个成员仍是空实现"那件事**已开始补实**（此前 `CustomActor` 的 `inherited` 分支落在一个空壳上） |
| `p7-dx-dibfusion` | 切片 B：`InitLight`/`DrawLights`/`Darkness`（原文 `5848-5966`） |

**两条车道仍在持续产出**（下轮待并）：
- `p7-client-actor-family` 切片 2：`ActorFamilyBaseTests.cs` **1,132 行 / 60 例**，并**更新了 `HerbActor.cs` 那两处过期注释**（`TActorCore` 的"这两个方法非虚"与"基类虚方法受阻"）—— 正是 §28.4 登记给后续车道的待办；
- `p7-dx-dibfusion` 切片 C1：`DoSmoothRotate` + 10 个 `Do*`（原文 `5975-6396`），562 行实现 + 486 行测试。

**观察（供后续判断节奏）**：DIB 空洞正以"每轮 500–1,000 行"的速度收口；按此节奏，`DIB.Fusion.cs` 那约 3,000 行还需 2–4 轮。这条经验可外推到其它巨型单元的收口估算。

### 32.4 第 37–38 轮：DIB 空洞接近填满，估算得到验证
- 第 37 轮：**两次提升**（16,482 → 16,593 → 16,593）。裁定并批准了 `p7-client-actor-family` 的三项越区请求
  （`TActor` 补 `partial` + 删 4 个空虚成员 / `CustomActor.cs` 的 7 处 `inherited` 跟上基类补实 / 修 `ActorSoundDispatch` 源头缺口），
  **并否决了它"在自己文件里重复补偿"的绕过方案**（那会留下两份同义但内容不同的派发实现 —— 属"假完成"族）。
- 第 38 轮：3 车道切片并入 → `main` = `59361243`，全量 **16,593 → 16,673**。
  **`DIB.Fusion.cs` 已达 2,754 行**（0 → 1,418 → 2,754）：§30.5 登记的那约 3,000 行空洞**接近填满**，
  §32.3 那句"每轮 500–1,000 行、还需 2–4 轮"的**估算得到验证**（实际 3 轮）。
  **规程含义**：对"有明确行号区间的空洞"，**按实测节奏外推**比凭直觉乐观估计可靠 —— 本会话早期多次低估巨型单元，
  原因都是忽略了"常需先补依赖面"。
- **一处观察已闭环**：第 37 轮我注意到 `p7-db-selectclient` 切片 1 落了 1,791 行源码**但无测试**；
  第 38 轮其切片 2 即补上 **27 例**（`TSelectChar` 1000 槽会话表，含 `Initialize`/`Finalize` 字段集差异断言）+ 测试替身 322 行。
  ⇒ **"大块源码 + 零验证"的切片在下一片补齐**是合理的推进方式，但**必须在报告里交代**；不能默认它会补。
- **管线再次排空**（无待并车道）⇒ 下一轮应继续**加宽**（§18.4 甄别清单里仍未开工的：`uFrmDummySetting`/`uFrmCustomItemProperty`/`SellPlayer`/`GateSet`/`uFrmHumanExport`/`uFrmClientPlugManager`/`EncodingHelper` 等，
  以及巨型单元的下一段：`MirClientContext` 的 `CheckUsePlugin` 六族、`ObjNpc` 的 43 例程、`uFrmMain` 的 ~90 例程、`DxMemo` 其余 7 族）。

## 33. 第 39–41 轮：**DIB 空洞完全填满** + `SelectClient` 完成（含"交付实现 ≠ 已接线服务"的边界）

### 33.1 提升
三轮连续提升 → `main` = `46543119`，全量 **16,673 → 16,913**。

### 33.2 ✅ `DIB.pas` 4940-7930 空洞**完全填满**（车道 `p7-dx-dibfusion`）
**覆盖表：无未覆盖区间**。新增 `DIB.Fusion.cs` **3,055 行** + `DIB.Fusion.Filters.cs` 617 行 + 3 个测试文件（2,474/347/366），**新增 220 例**；改动既有文件仅 1 处（`DIB.Effects.cs` 的 `DibRandom`）。
**这是 §30.5 登记的"客户端最大单块空洞"的收口**，也验证了 §32.3 那句"按实测节奏（每轮 500–1,000 行）外推"的估算 —— 实际 5 个切片。

**该车道最有价值的四条发现**：
1. ★ **`DoSpotLight`(6915-6958) 原文是空操作**：只画进临时 `z` 再 `z.Free`，**从不回写 `Src`**（实测像素不变）——
   **这类"整段写了但没有任何效果"的原文缺陷最容易被移植者"顺手修好"**，它逐字保留并锁定。
2. ★ **`DoResample` 的 USE_SCANLINE 路径 R/B 颠倒**（`DIB.pas:6` 定义了该开关）：源行按 `TColorRGB(R,G,B)` 解释，而内存实际是 `(B,G,R)` ⇒ 结果落在 `GetPixel` 的 **B 通道**（4×4→2×2 Box 实测 `[[10,16],[34,40]]`）。
3. ★ **四个滤波器（`ftrBell`/`ftrBSpline`/`ftrLanczos3`/`ftrMitchell`）在缩小时下标反射会给出负下标**（7388/7427/7537/7576）⇒ **读缓冲区之外**，同一输入两次运行结果不同。
   已建议登记为"**该四滤波器不可依赖**"——**注意这是"原文未知行为"，不是"我们没做"**。
4. ★ **`DibEffects` 的 `DibRandom` 恒返回 0**（`(int)r * 0` 占位残留）⇒ `Spray`/`AddMonoNoise`/`AddGradiantNoise` 退化为固定偏移。**已修 + 2 例回归断言**，
   但它指出该缺陷**原本零测试覆盖**，建议全族复核 —— 这是"**占位实现被当成正式实现**"的又一实例（与 §26.2/§31.4 同族）。

**接缝纪律**：4 个接缝（`IDibFusionCanvasSeam`/`IDibFusionPaintSeam`/`IDibFusionSpotSeam`/`IDibFusionColorizeSeam`）**全部"未装载即抛"**，**无静默中性值** —— 正确执行了 §25.2。

### 33.3 ✅ `SelectClient`（1,248 行）完成（车道 `p7-db-selectclient`）
**编译面 100% 移植**（`TSelectChar` 1000 槽会话表 + `TSelectClient` 全部 19 个方法），新增 **171 例**（槽位表 27 / 收包分发 71 / 角色族 71 / 生命周期 2），`GXX.DBServer.Tests` 371 → **542**，**连跑 3 次无偶发**。
未移植仅 `{$IF DBSUSETHREAD = 1}` 两段（`DBShare.pas:19 = 0` ⇒ **编译期不可达**）；
**任务书点名的 `ClientExecute`/`Close` 与 PChar 重载的唯一调用点都在这两段里 ⇒ 本配置下不可达**（该车道**主动纠正了任务书的假设**，正确）。

**`DeCodeUserMsg` 分派表**（命令 → 行 → 节流）：100→QueryChr(200ms，**无 CheckSession**)、106→RandomName(**无节流**)、101→NewChr(1000ms)、102→DelChr(1000ms)、103→SelectChr(`not boChrQueryed`)、105→QueryDelChr(200ms，返回值丢弃)、3006→GetBackDelChr(200ms)、其它→`SM_CHECKISMYSELFSERVER(8889)`。

**★ 该车道最要紧的原文缺陷**：
- **B1** `QueryChr`(:1182-1237) **恒返回 False** ⇒ `boChrQueryed` 永远为假 ⇒ CM_QUERYCHR 的 `not boChrQueryed` 门**永真**、CM_SELCHR 的 "Double send" 分支**不可达**；
- **B2** `GetBackDelChr`(:1043-1081) 恒返回 False（当前返回值被丢弃，故无行为差异）；
- **B3** 本端 keepalive `%++$`(:370) 在**收包侧是"未知命令"**（首字符 `+` 不在 :466 的 case 里）⇒ 对端若回同样字节，**两次后清空整个接收缓冲**；
- **B4** :484 的 `Continue` 绑定的是 **`for`** 而非"结束本条帧" ⇒ 同 ConnID 多槽时，**无 `!` 的载荷会被追加到每一个匹配槽**；
- 另有 `Random(Count-1)`(:895) 使**最后一项永远取不到**、`Finalize` 与 `Initialize` 的**字段集不同**(:197 vs :170)、`Add` 不写 Socket(:142) 等共 16 条。

**★ D-p7-1..9 偏差已登记**（该车道无权写本台账，**转录要点**）：构造收敛无参 / `SendUserSocket` 第二参 `string→byte[]` / 保留 `Finalize` 原名 / PChar 重载转调 string 重载 / `TUserInfo` record→class（指针语义）/ `TList`→`List<int>` / tick 源统一到可注入 `DelphiTick` / 局部 `TDeleteHumanInfo` 用 default /
**★ D-p7-9：整条解析链走 latin-1 字节串**。
**D-p7-9 值得单独记**：原文 `Length(sChrName) < 4` 与 `sChrName[I] in [#32..#255]` **都是按字节**的判据 ——
**两个汉字 = 4 字节应当通过**，若按 UTF-16 字符数判定会**误判为不合法**。这与本工程早先记录的"`AnsiString` 承载裸二进制"陷阱同源，
也再次说明**在 GBK 边界上必须显式选择字节口径还是字符口径**。

### 33.4 ⚠ **边界必须写清：交付的是"可接线的正确实现"，不是"已接线的服务"**
该车道**明确声明**：`DBServerService.cs` / `RoleDatabase.cs` 的接线**它没有做**（只读文件），
⇒ **当前跑起来的 DBServer 仍然只有那 4 条命令**。
并给出精确接线说明（它未越区改）：
- `DBServerService.cs:102-108` 按 `uFrmMain.pas:303-306/340-353` 为每个 TcpLink 建 `TSelectClient` 并把 `Send` 接到 `SendTextSink`、收包喂 `ExecGateBuffers`；
- `:114-181 ProcessGateData` **应整体删除**（由 `DeCodeUserMsg` 分派取代）；
- **★ `:135` 协议字段位置与原文不符**：现在把角色数放在别的字段，原文是 `MakeDefaultMsg(SM_QUERYCHR, nChrCount, 0, 1, 0)` ⇒ **角色数在 `Recog`、`Tag=1`**；
- `RoleDatabase.cs:46/75/88/100/125` 与原文的 nCode 语义/**Human+Hero 双库查重**/`DelChr` 的 `Level>45 → -3` 不一致。
**这是一条重要的进度诚实度标记**：本车道的产出**不会自动让 DBServer 多支持 5 条命令**，必须有人接线。
**待办**：接线 + 协议字段修正（`DBServerService.cs:135` 若与既有集成测试冲突，需裁定改哪侧）。

### 33.5 又一次 `.cs` 被脚本改坏 —— **而这次规程生效了**
该车道自报：切片 2 中它用 `Get-Content -replace | Set-Content` 改 `.cs`（**违反 §24.5**），把中文注释按 ANSI 读入写回导致整个文件乱码；
随后按 **§29.4 规程 2**（**恢复只用路径白名单** `git checkout HEAD -- <单文件>`）恢复，再用编辑工具重做，**未波及其它文件**。
⇒ **该规程经一次真实事故验证有效**（上一次事故是"移出文件误伤 64 个文件"）。
**注意**：这已是本会话**第 3 次**由不同车道违反"`.cs` 一律用编辑工具"，说明**光写在派发词里不够**；
建议后续派发词把这条**前置到"纪律"第一条并给出反例**（"PS 5.1 会按 ANSI 读入写回，中文注释整文件乱码"），而不只是列为一条编号。
（该车道另记一条工具链规程：**PS 5.1 按 GBK 读 UTF-8 的 `.ps1` 会乱码并使解析失败**，临时脚本须纯 ASCII 或用 `$PSScriptRoot` —— 与 §8.1/§24.5 同源。）

## 34. 第 42 轮：actor 家族收口 + **两处"沉默"缺陷（由先前分步推进的方式引入）**

### 34.1 提升
4 车道切片并入 → `main` = `00776abb`，全量 **16,913 → 17,030**。`TActor` 已确认为 `partial`（那 4 个空虚成员已删除、实体移入 `ActorFamilyBase.cs`）。

### 34.2 ★★ 发现一：`CustomActor` 有 **11 个与基类同名的字段**（**字段隐藏 ⇒ 同一状态两份存储**）
`CustomActor.cs:1614-1644` 声明了 `m_nSpellFrame`/`m_nCurEffFrame`/`m_nEffectFrame`/`m_nEffectEnd`/`m_nStruckWeaponSound`/`m_BodySurface`/`m_dwLoadSurfaceTime`/`m_boLoadSurface`/`m_boRunSound`/`m_dwEffectFrameTime`/`m_dwEffectStartTime`，
而基类也有同名成员 ⇒ **C# 字段隐藏**：基类本体读写的是**继承的那一份**，子类读写自己的那一份，**两边永不相等**。
> **编译不报、单测不报** —— 只有"接通虚槽位后，行为却不生效/不一致"时才暴露。

车道已**全部删除并改用继承字段**；`m_ColorEffect` 改为覆写虚属性 `ActorColorEffect`（基类存储名为 `m_BaseColorEffect`）；`SetSound_()` 由恒空替身改为转调 `base.SetSound()`。

**★ 这是本会话"假完成"族的又一形态，且是"分步推进"这个方式本身引入的**：
§28.2 先让基类虚化（补槽位）→ §30.5/§36 后补基类本体 → 而 `CustomActor` 的字段是**在基类还没有那些成员时**按原文落下的；
基类成员一落地，同名字段立刻变成隐藏。**责任在推进方式，不在车道。**
**新规程（写进"基类补本体"类任务的派发词）**：
> **给基类补成员/本体前，必须先 `git grep` 所有子类里是否有同名字段/成员**；
> 有则**一并删除并改用继承成员**，否则同一状态会静默变成两份。
> 顺带：`public` 字段同名是"隐藏"，`virtual`/`override` 才是指派 —— **同名不等于接上**（与 §24.2/§28.2 同源）。

### 34.3 ★★ 发现二：子类**基类关系**原先平铺错了（`inherited` 会整段绕过中间层）
原文层级：`TKillingHerb : TActor`；而 `TMineMon`/`TCentipedeKingMon`/`TBigHeartMon`/`TSpiderHouseMon`/`TDragonBody` **都 `: TKillingHerb`**；
`TBeeQueen` 才是 `: TActor`（**不是** `TKillingHerb`）。
托管侧原先**把它们全部平铺为 `: TActor`** ⇒ 这些子类的 `inherited` 会**整段绕过 `TKillingHerb` 的覆写**（而它们本该继承那层语义）。
已按原文更正，并有 `SubclassHierarchyMatchesOriginal` 锁定。
**规程**：移植"一族子类"时，**基类关系是语义的一部分**（决定 `inherited` 落到谁）；平铺成同一基类虽然能编译，但**改变 `inherited` 的落点**。

### 34.4 我对 `FormJ94Tests` 那个问题的裁定得到执行，且**结论是"实现缺失"而非"测试期望错"**
车道的结论与依据：**回读原文确认 7063-7072 与 7076-7092 两支确实存在** ⇒ 属**实现缺失**；
那 8 处既有断言只覆盖 7051-7062 段，**无需改期望值**，只需补两个中性实参（`0, -1`）即可编译；另**新增 4 例**锁定补齐的两支（含边界 `201/202/209/210`）。
⇒ 这与本工程"测试期望写错远多于实现写错"的常态**相反**，说明**"先回读原文判定哪侧错"这一步不能省**：若按常态先入为主地改测试，就会把正确的期望改坏。
另有两处既有断言**按新事实反向更正**（`CoverageTable_IsSelfConsistent` 由"必须存在未落地条目"→"不再有任何未落地条目"；
`UncoveredRanges_AreOrderedAndInBounds` 由"必须非空"→"为空时须由 `AllRangesCovered` 显式声明，**不得静默为空**"）—— 都注明了依据，**没有两边都留**。

### 34.5 裁定：该车道的**警告证据强度已足够**，不需要重跑基线
它报告 `--no-incremental` 全量构建的 `Warning(s)` 计数在 **157/163 之间波动**（同一条警告因多项目构建被**重复计入**），
因此**没能**复现早先那种"150 条逐条比对完全一致"的证据；但它已核实
**`ActorFamily*`/`CustomActorTests`/`FormJ94Tests`/`TailHerbActorTests` 四个文件的贡献警告为 0 条**。
**裁定：这已足够，不需要为它去 main 上跑同口径基线。**
**理由**：要证明的是"**我没引入新警告**"，而"**我改动的文件贡献 0 条警告**"是**更强且更直接**的命题 ——
它不依赖聚合计数这个**已知会被多项目重复计入**的不稳定指标。
**规程**：验证"无新增警告"时，**优先用"我改动的文件贡献了多少条"**，而不是"全量计数是否与基线相等"（后者受构建拓扑影响，本身不是稳定量）。

### 34.6 又一次：已知 flaky 集成测试在门禁里触发（协议执行）
`GXX.Integration.Tests.LoginSrvIntegrationTests.Client_ThroughGate_ToLoginSrv_RegisterAndLogin` 在全量门禁中失败（耗时 46 s ⇒ 重试耗尽）；
按既有协议**单独复跑 3 次全部通过（各 ~130 ms）** ⇒ 判定为**已知 TOCTOU 端口竞态**，非回归，**照常提升**。
**待办（值得单列）**：这是**第 2 条**反复在门禁里触发、消耗门禁周期的 flaky（第 1 条 D19 已修除）。
它每次都让我多跑一轮复验；建议**单开一条小车道**按 §29 的"D19 模式"处置它
（**先做因果实验**：去掉重试/超时后是否稳定复现 → 再定修法），而不是长期依赖"复跑一次"。

## 35. 第 43 轮：三单元清扫完成 + **"接缝臆造"第 5 例** + 一条语言优先级陷阱

### 35.1 提升
2 车道切片并入 → `main` = `dbbfbb5d`，全量 **17,030 → 17,230**。

**`p8-m2-itemprop-misc` 三单元全部完成**（12 个路径全新增、+4,517 行、+222 例）：
| 单元 | 行数 | 结果 |
|---|---|---|
| `uFrmCustomItemProperty.pas` | 498 | 全树 1:1；唯一未覆盖 `:163 {$R *.dfm}`，事件布线另以 `WireDfmEvents()` 1:1 复刻并断言（60 chk→`chk01Click` / 60 edt→`edtShowName01Change` / 6 处按钮与 memo，**与 DFM 实测一致**） |
| `SellPlayer.pas` | 307 | 全类 1:1，`:1-307` 无未覆盖行 |
| `EncodingHelper.pas` | 206 | 全单元 1:1，`:1-206` 无未覆盖行（仅 `:199-201` AUTOREFCOUNT 段不适用） |

### 35.2 ★ "接缝臆造"第 5 例：`g_SellPlayerList` 的类型错（已裁定修正）
原文 `M2Share.pas:8416` 是 **`g_SellPlayerList: TSellPlayerList`**（`svMain.pas:1636-1637` 创建、`:1414` AutoLoad、`:3180` Free）；
而托管侧 `M2ShareFuncs.cs:56` 被造成 **`List<string> g_SellPlayerList` + `SearchSellPlayer`（IndexOf 语义）** ——
**从类型上无法表达**原文的 `Items[I]` 记录访问 / `DeleteByIndex` / `AddSellPlayer` / `SaveConfig`。
**裁定**：按 §12.8「**正式归属落地后立即去掉替身**」改用**已移植的 `TSellPlayerList`**，删 `SearchSellPlayer`，同步 3 处调用点（并后续承接 `UsrEngn.pas` 的 12 处调用）。
**这是本会话第 5 例"接缝臆造"**（前四：§22.4 `nMaxClientMsgCount const 5`、§26.2 字段消费未验证、§31.4 声称已授权未做、§30.4 `Anicount/AniCount` 拆字段）。
**共同特征**：**替身/接缝里放了一个"看起来像原文"的东西，却没有回读原文的类型/常量/默认值**。
**规程（第 N 次重申，但这次给出可操作判据）**：
> 当你要给某成员写替身时，**唯一可接受的依据是"原文该成员的声明行"**（类型 + 初值）。
> 凡写不出 `原文 文件:行` 的替身，一律视为**待查**，不得先写后补。

### 35.3 ★ 一条语言优先级陷阱：`shr` 与 `>>` 的结合性**相反**
原文 `SellPlayer.pas:129`：**`L + (H - L) shr 1`** ——
**Delphi 的 `shr` 优先级高于 `+`**（等价 `L + ((H-L) shr 1)`），而 **C# 的 `>>` 优先级低于 `+`**（会算成 `(L + (H-L)) >> 1`）⇒ **必须显式加括号**。
该车道被单测抓出并锁定。**这是"逐字抄对了、语义却不同"的典型**，与 §17.2 的 `%d`、§34.2 的字段隐藏同族。
**规程**：移植含**位运算与算术混用**的表达式时，**一律显式加括号**，并在注释里写出原文的结合方式。

### 35.4 ★ "已移植但零生产调用方" = 没接上（与 §33.4 同源）
该车道指出：**`EncodingHelper` 目前零 C# 调用点** —— 它的真实消费者 `M2Engine/StringListHelper.pas`（53 行）尚未移植。
**裁定**：批准移植 `StringListHelper`，并把 `TFastIniFile.Load` / `TStringList.LoadFromFile` 接到 `TEncodingHelper.GetBufferEncoding`。
**这是本会话第 2 次出现"交付的是可接线的正确实现、而不是已接线的能力"**（第 1 次是 §33.4 的 `SelectClient` ⇒ DBServer 仍只有 4 条命令）。
**规程（建议加入交付报告的固定栏目）**：
> 每个单元除"覆盖行号"外，**必须回答"它现在被谁调用"**。
> 若答案是"零生产调用方"，就**必须显式登记**（"已移植未接线"），**否则会被误读为"功能已完成"**。

### 35.5 其它已登记的原文缺陷与规避判断（该车道）
- `EncodingHelper.pas:68-69` **纯 ASCII 缓冲返回 False**（注释与代码不符）→ 回落 ANSI；
  `:147-157` 空签名恒真（当前不可观测，**如实标注未伪造断言**）；`:176-179` 回落分支提前 `Exit` 恒返回 0。
- `FastIniFile.pas:2967-2968` **坏时间被静默归零**（`Default=0` 但判据 `T <> -1`）；
  `:2077-2085` **`Destroy→FlushBuffers` 自动落盘** vs 托管 `Dispose()` 空实现（已登记 D-P8-3）。
- ★ **命名空间取 `GXX.Core.EncodingHelper` 而非 `GXX.Core.Encoding`** —— 后者会让既有文件的 `Encoding.Xxx` **命中命名空间** ⇒ 一片 **CS0118**，
  而那些文件在他人常驻区不可改。**这个规避判断很关键**，已要求它写成"为什么不能叫 `GXX.Core.Encoding`"的明确条目，以免后人"顺手改名"。
- 自我纠错：`uFrmCustomItemProperty.pas:169` 的 `Exit` 首版被译成空方法 ⇒ **门控失效**，被 `ShowFrm_WhenNotStartReady_DoesNothing` 抓出后改为 `return` —— **单测抓出实现错**的正例（与 §34.4"实现缺失而非测试期望错"遥相呼应）。

## 36. 第 44–45 轮：**又一次双车道被杀 —— 而"抢救规程"第二次救回实质产出**

### 36.1 提升
`p8-m2-itemprop-misc` 报告 + 抢救产出并入 → `main` = `f65aa9a2`，全量 **17,230 → 17,258**。

### 36.2 事故：两条车道被宿主同时杀死
`p8-m2-dummysetting`（最后一句"Now removing all diagnostics from production code"）与
`p8-m2-itemprop-misc`（正在做我批准的 4 条请求）**双双无收尾消息地失败**。

### 36.3 ★ 规程再次生效：**产出没丢**（§13.2 的第 2 次实战）
按 §13.2「**禁止用 `git log` 判断车道有没有干活**」，我逐工作树看 `status --porcelain`，发现**两条都有实质未提交产出**：
| 车道 | 未提交产出 |
|---|---|
| `p8-m2-itemprop-misc` | **正好是我批准的那 4 个文件**（`M2ShareFuncs.cs`/`ViewOnlineHumanForm.cs`/`FormViewOnlineHumanTests.cs`/`SellPlayerTests.cs`）—— 即请求 #1 的落地 |
| `p8-m2-dummysetting` | 1 个改动的 handler + **6 个新测试文件**（切片 5 的测试） |

**处置（按 §13.3 抢救流程，逐条验证后原样固化，不改车道一行）**：
1. **`p8-m2-itemprop-misc` 的产出经门禁验证为绿**（build 0 error / `GXX.M2Server.Tests` **8,534 全绿**）⇒
   以 `3f71fdf9` **代其提交**，并并入 main ⇒ **第五条"接缝臆造"（`g_SellPlayerList` 类型错）就此修正在 main 上**
   （已核实 `M2ShareFuncs.cs:70` 现为 `public static GXX.M2Server.Misc.TSellPlayerList g_SellPlayerList`，与原文 `M2Share.pas:8416` 一致）。
2. **`p8-m2-dummysetting` 的产出不全绿**（5 例红，且**全在它自己的 `Diagnostic_*` 探针用例里**）⇒
   按 §15.2 立下的 `WIP-不可合并` 机制**原样固化**（`2eed010f`），**不并入**，并**重启该车道**继续排查。
   ⇒ **`Check-LaneReady.ps1` 随即把它挡在波次之外**（`ahead=1` 且标 WIP）—— **保护机制链条完整生效**（标签 → 工具拦截 → 未污染 main）。

### 36.4 对本轮两条 WIP/红测的裁定
`p8-m2-dummysetting` 那 5 条 `Diagnostic_*` 是**排查用临时用例，不应作为交付**。已要求它：
- **先回读原文判定哪侧错**：实现错 ⇒ 修实现并把用例改成断言**正确**行为；
  原文就是那个行为（例如"取消勾选会级联清掉从属标志"这类原文怪癖）⇒ 保留为**差异锁定**用例，**改名去掉 `Diagnostic_` 前缀**并注明 `// 原文如此（uFrmDummySetting.pas:<行>）` + 依据；
- **既不许留一堆 `Diagnostic_` 名字的正式用例，也不许把红测删掉了事**；
- 并清干净生产代码里的诊断残留（它自己说的"removing all diagnostics"）。

### 36.5 两次事故的对比（抢救规程的价值已被量化）
| | §13（第 1 次，8 条车道被杀） | §36（第 2 次，2 条） |
|---|---|---|
| 发现方式 | 逐工作树 `status`（**不是** `git log`） | 同 |
| 抢救结果 | `p2-rungate-impl` 60 文件全部保全并并入（1,206 例） | `g_SellPlayerList` 修正保全并并入；`dummysetting` 7 文件保全待续 |
| 结论 | **未提交 ≠ 没做** | 同（且这次连"被批准后正在做的修复"也救回来了） |

**这正是把 §13.2 那条写进规程的价值**：若当时按 `git log` 判断"车道没产出"，这两次的产出都会**被当成不存在**而重做一遍。
**唯一仍会丢的情形**：宿主在车道**尚未落盘任何文件**时杀死它 —— 这是无法用规程消除的，只能靠"小切片立刻提交"把窗口压到最小（本轮 `itemprop-misc` 若早提交一次，我就完全不必代提交）。

## 37. 第 48–49 轮：两条单元 100% 完成 + **"否定性断言必须计数取证"** + 一条会被忠实继承的编码缺陷

### 37.1 提升
`p8-m2-itemprop-misc`（请求 #3/#4）与 `p8-m2-dummysetting`（切片 5/6）并入 → `main` = `0d35402e`，全量 **17,320 → 17,743**（`M2Server.Tests` 8,608 → **9,058**）。

### 37.2 ✅ `uFrmDummySetting.pas`（1,081 行）**100% 完成**，且 WIP 抢救兑现
`p8-m2-dummysetting` **复活后全绿**：**66/66 过程、122/122 DFM 控件、62/62 事件绑定**，**+343 例**。
⇒ §36.3 那次"以 `WIP-不可合并` 原样固化"的抢救**完整兑现**：车道回来把 5 条红测修绿，并把 `WIP` 挡在波次外的保护链走完（标签 → 工具拦截 → 未污染 main）。

**那 5 条 `Diagnostic_*` 的处置完全符合裁定**：**不是删掉了事，而是回读原文后改写成正式断言**（并按"原文就是那个行为"分支处理，改名去掉 `Diagnostic_` 前缀 + 注明 `// 原文如此`）。
查清的三条机制都是**原文行为**而非移植缺陷（已 1:1 保留 + 锁定）：
1. `FormCreate:285` 的 `Checked := not boDiableDummyRun` **只有产生状态变化才触发 OnClick** ⇒ 默认值下**级联根本不跑**（8 个从属不被清空/禁用）；
2. `:291` 触发 `:958-963` 会用**尚未回填的**子框状态覆写 `g_Config.boDummyWarHreoRun` ⇒ 得 False；
3. `ButtonDummySaveClick` 的 `:514-530`/`:532-547` 两个脏标志块**不清标志** ⇒ **第二次保存仍会再写盘**。

### 37.3 ★★ 新规程：**"否定性断言"必须用计数取证**（该车道自己踩出并建议）
它曾在 3 个文件注释里断言「**`edtDummyHomeMapChange` 在 DFM 里没有绑定 `OnChange` ⇒ 永不触发**」——**该结论是错的**。
它是靠**对账**发现的：做"**DFM 绑定数（62）vs 托管 `+=` 数（61）**"核对时对不上，回读 `uFrmDummySetting.dfm:162` 确认**确有** `OnChange`；随后补绑定 + 补全该控件 DFM 属性（`Top`/`Width`/`Text` 三处都错）+ 3 处注释改勘误 + **删掉基于错误结论的用例、改为 3 条正确用例**。

**规程（采纳）**：
> 「**DFM 未绑定某事件**」「**原文没有这一支**」这类**否定性断言**，**必须用计数对账取证**
> （如：DFM 绑定总数 vs 托管 `+=` 总数、原文过程总数 vs 实现总数、控件声明数 vs 实例化数），
> **不能靠"逐行扫一眼觉得没有"**。
> **理由**：肯定性断言有一条证据就成立；**否定性断言需要对整个集合负责**，而人眼扫描对"集合是否完整"恰恰最不可靠。
> 这与 §34.4（"回读原文判定哪侧错"）同源：**先取证，再断言**。

该车道给的三组对账数字值得作为范例：原文过程 **66** → 实现 **66**；DFM 控件 **122** → 字段 **122** → 实例化 **122**；DFM 绑定 **62** → `+=` **62**。

### 37.4 ★ 一条**会被忠实继承**的编码缺陷：GBK 文本被误判为"无 BOM 的 UTF-8"
由请求 #4（把 `TFastIniFile.Load` 接到 `GetBufferEncoding` 嗅探链）**直接暴露**，且**原文亦然**：
- 「值」的 GBK 字节 **`D6 B5`** 恰是**合法 UTF-8 两字节序列**（`D6 ∈ $C0..$DF`、`B5 ∈ $80..$BF`）⇒ `IsBufferUTF8` 为真 ⇒ **整份文件按 UTF-8 解码 ⇒ 乱码**；
- **触发面**：第一个非 ASCII 字节起到文件尾**全部**构成合法 UTF-8 序列（典型 `Key=值`、单个/少量汉字 + 其余全 ASCII）；对照组「测试」`B2 E2 CA D4` 不触发；
- **调用链**（该车道逐段核实）：`FastIniFile.pas:2432-2445 → :1743-1753 → :1757-1760 → :1762-1773 → EncodingHelper.pas:167-179`；
  `StringListHelper.pas:45` 是**同一条链**（它本是"修 UTF-8 乱码"的补丁，**反方向的 GBK 误判对它同样成立**）。

**该车道的处置正确**：**没有擅自加"GBK 排他性"保险**（那会偏离 1:1），改为**两支用例锁死**该行为
（`..._IsMisdetected_OriginalFlaw` + `..._UnsafeLeadByte_DecodesAsGbk`）+ 登记（偏离 D-P8-16）+ 给出最小改法。
**我的裁定**：
1. **保持 1:1**（不加保险）—— 与本工程"原文缺陷照抄 + 注释 + 差异断言"的一贯处置一致；
2. **但这条与"渲染错"不同，后果是「真实配置文件被静默改写/读乱」** ⇒ 必须**显著登记**（本条）并**保留最小改法**，供将来若判定"保真代价不可接受"时**一处开关**切换；
3. **它的冒烟建议采纳为后续动作**：用真实配置（如 `SellPlayer.ini` 里出现 `Player=值` 这类单汉字值即命中）验证一次。
   ⚠ 我本轮两次尝试自动冒烟**均超时**（逐字节 UTF-8 校验与全仓递归扫描太慢，120 s 上限）⇒ **未完成，留给后续**（应改为在**已知配置目录**内、只扫 `.ini`、用整段 UTF-8 解码而非逐字节）。
4. **D-P8-15 的不对称要记住**：`TFastIniFile.Load` 走嗅探、而 **`Save` 仍固定 GBK** ⇒ **UTF-8 载入的 INI 回写会变回 GBK**。

### 37.5 该车道另交付的两份清单（均已按其要求登记在报告，此处只记存在）
- ★ **「为什么不能叫 `GXX.Core.Encoding`」独立条目**：命名空间与类型**共用声明空间** ⇒ `EncodingInit.cs:22/28/31/36`、`LauncherSettings.cs:186/327`、`ParadoxDataSet.cs:288-296` 的 `Encoding.Xxx` 会解析到**命名空间** → 一片 **CS0118**；那些文件在他方常驻区不可改 ⇒ 目录名与命名空间**有意不一致**（D-P8-7），并写明"**后人不要顺手改名**"。
- **14 处"固定 GBK/936 读文本"的读取点清单**（`IniFiles.cs:50`、`TxtEditorForm.cs:86/171`（文本编辑器最典型）、`ClientModuleList.cs`、`ViewFormsData.cs`、`Boxs.cs:228`、`MissionPageState.cs:48`、`LogDataService.cs:105`、`PluginAssemblyLoader.cs:140`、`PluginHostRuntime.cs:101/174`、`SelGateConfig.cs:297`、`LauncherSettings.cs:188`）——**一处未改**；
  并**正确排除了**二进制格式内的 `GBK.GetString`（Pak/Wis/Uib/GuiRecords/DesUnit）——**GBK 在那里是格式契约，不是猜测**。这个区分（"契约" vs "猜测"）是采用嗅探与否的判据。

### 37.6 `DummySetting` 留给集成方的 4 条跨区事项（如实登记）
1. `DummySettingConfig.cs` 的 43 个 `M2Config` 假人字段：`M2ShareGlobals` 在托管侧**并不存在**（0 命中）⇒ 待 `M2Share.pas` 落地后**必须删掉重复声明**（否则 CS0102）；
2. `DummySettingGlobals.cs` 的三个 `TGStringList` 同理；
3. 三个 `Load*`（`M2Share.pas:16607/:12890/:12936`）**未移植**（**不在本单元内**，原件只有 `Save*` —— 按"不顺手移植依赖"处理，正确）；
4. `svMain.pas:3241` 调用点接入只需一行 `TFrmDummySetting.ShowFrmDummySetting();`；**建议**把 `DummySettingState` 加进 `M2ConfigIsolationCoverage.cs` 快照清单（一行，双保险；它未越区改）。

### 37.7 `p8-m2-itemprop-misc` 请求 #1–#4 **全部落地**
#1 `g_SellPlayerList` 改 `TSellPlayerList` + 删替身（第 5 例"接缝臆造"修正）；#2 `FastIniFile` 固定日期时间整体回收进 `GXX.Core`（**方向约束已满足**）；#3 `TStringList.Text` + 编码状态 + `LoadFrom/SaveTo` 嗅探（**副作用已量化**：对"未载入过 / GBK 载入"的列表**字节完全一致**，仅 UTF-8/16 载入过的列表改变）；
#4 `StringListHelper.pas`（53 行）1:1 + `TFastIniFile.Load` 接入嗅探。
**该车道自己撤掉了先前的权宜实现**（`CustomItemPropertyLogic` 降级为纯转调、窗体改回 1:1 的 `.Text`）—— 在**同一条车道、同一个单元内**完成了"正式归属落地后去掉替身"的闭环，**没有等我发现**。这条原则被内化，比修掉任何单个缺陷更有价值。

### 16.6 覆盖度复核（`main`，本轮实测）

| units | mapped | assigned | checklist-only | unmapped | vendor | non-unit |
|---|---|---|---|---|---|---|
| 566 | **301** | 34 | 30 | **0** | 163 | 1 |

mapped 从本会话初期的 169 提升到 **301**（+132），unmapped 归零。
**但 `unmapped=0` 不等于完工**：剩余量在 `assigned`(34) 与 `checklist-only`(30) 里，
且本轮已发现 **E2 假阳性**（`DxPageControl` 等 5 个单元无同名 `.cs`，仅因他文件头提及即计入 MAPPED）。
**待办**：对 `checklist-only` 30 个与这批"仅靠头提及"的单元做一次**逐单元复核**，
确认是"真已移植"还是"被高估"（这正是 §13.7 四个巨型单元当初的发现方式）。

### 16.5 本轮车道产出的高价值原文缺陷（逐字保留 + 差异断言锁定）

1. **`StreamClipbrd.pas:78` 形参窄化静默丢数据**：`SaveClipboardFormat(fmt: Word)` 而 `:137` 传 `Cardinal`
   → Delphi 静默截断 → **格式 id ≥ 65536 的数据在 `SaveClipboard` 时被完全丢弃**（本轮最有价值的发现之一）。
2. `AsphyreTimer.pas:238` **`DeltaLimit = 32*FixedHigh`** 上限：60fps 下延迟 >533ms 只算 32 帧。
3. `AsphyreTimer.pas:204-205` Cardinal 回绕 + 提升 Int64 后截断回 Integer：时钟倒退 100ms → **-100.0ms 负延迟**；
   Δ=2048ms → `int.MinValue`。
4. `AsphyreTimer.pas:264-280` **`Start` 卸 OnIdle 钩子 / `Stop` 反而装上**（反直觉，逐字保留）。
5. `AsphyreTimer.pas:322` **`Reset` 丢弃 `RetreiveLatency` 返回值** → `LatencyFP` 在 Reset 后不变；
   原文拼写错误 `Retreive` 逐字保留。
6. `StreamClipbrd.pas:174-193` vs `17-51` 三处不对称（不 Open/Close、不 `Position := 0`、异常时 GlobalFree）；
   `:186` 用 `Write` 而 `:210` 用 `WriteBuffer`（同单元内不一致）。

---

## 38. 第 50 轮：会话交接 + 把"已关闭车道的陈旧占位行"变成真实 P9 波次

> **本轮执行者**：并行调度会话的**新会话**（旧调度会话的 goal 已 `paused`，其 226 KB 台账上下文不再续用）。
> **干线会话 A 现状**：其 goal 已 `blocked`（`Goal reached its configured limit of 256 rounds`），
> 最后产出 `批次J255`（09-21 20:11）。故本轮 **M2Engine 实际无人推进** —— 这是下面重新派发的直接原因。

### 38.1 提升（第 1 次）
`par/integration`（`b48e4c43` = 车道 `p4-m2-objnpc` 切片53 `MakeHeroName`/`MakeDeputyHeroName`，+466 行）
已 fast-forward 并入 `main`。
门禁：`dotnet build GXX.slnx -c Debug` → **0 error**（165 warning）；全量 `dotnet test` → **18,576 通过 / 0 失败**。

### 38.2 ★ 发现：**三条已关闭车道的 `unit-map.tsv` 行仍在"占位"**，掩盖了 22 个真实缺口
`tools/audit-coverage.ps1` 复测（`main @ b48e4c43`）：
`mapped=325 / assigned=22 / checklist-only=6 / unmapped=0 / not-ported=178 / non-unit=1`。
逐条核对 `assigned` 那 22 行后发现：

- `par/p3-m2-sweep`、`par/p2-dxcontrols-rest`、`par/p2-rungate-impl` **都已全量并入 main 且无未发布提交**
  （`git merge-base --is-ancestor <branch> main` 全部为真，`git log main..<branch>` 为空）；
- 但这三条车道**并未完成**它们名下的全部单元：其报告已"如实登记未完成项"，
  而 `unit-map.tsv` 的行仍指向它们 ⇒ 这 22 个单元**既没人在做、又不会出现在 UNMAPPED 里**。

> **规程（本轮固化）**：**合并车道 ≠ 关闭车道**。车道并入 main 之后，必须回头核对
> "`unit-map.tsv` 里指向该车道的行"：已完成的删行（E1/E2 会接管），未完成的**改派新车道**。
> 否则那一行会变成"永久占位"，把真实缺口从报表里藏起来 ——
> 这正是 §16.6「`unmapped=0` 不等于完工」的**第二种成因**（第一种是 E2 假阳性）。

### 38.3 P9 波次（本轮新开 5 条车道，全部是**新文件独占区**）
分区已写入 `tools/lane-zones.tsv`（`verify-lanes.ps1` 复核输出 `ALL LANES WITHIN THEIR ZONES`），
归属已写入 `tools/unit-map.tsv`（22 行改派，现已**没有任何**指向已关闭车道的行）。

| 车道 | 分支 | 承接单元（Delphi 行数） | 目标目录 |
|---|---|---|---|
| `p9-m2-monsters` | `par/p9-m2-monsters` | ObjDummy 1245 · ObjRobot 500 · ObjFireDragon 531 | `src/GXX.M2Server/Sweep9/Monsters/**` |
| `p9-m2-pathfind` | `par/p9-m2-pathfind` | PathFindClient 811 · PathFind_Hero 693 · ClientPickItemsCfg 192 · StruckDamageAbsorbUtils 194 | `src/GXX.M2Server/Sweep9/PathFind/**` |
| `p9-m2-datalayer` | `par/p9-m2-datalayer` | UserShopDB_Old 1073 · ItemEvent 389 · DataManage 278 | `src/GXX.M2Server/Sweep9/DataLayer/**` |
| `p9-m2-forms` | `par/p9-m2-forms` | ConfigMerchant 654 · ViewHeroRcd 453 · uAliyunSendSMSThread 320 · ViewKernelInfo 198 · uFrmClientPlugManager 151 · NoticeM 119 · ConfigMonGen 60 | `src/GXX.M2Server/Sweep9/Forms/**` |
| `p9-rungate-rest` | `par/p9-rungate-rest` | IODataPool 586 · IocpTcpClient 340 · Qos 293 · DllUpdateCommon 10 | `src/GXX.RunGate/Rest9/**` |

合计 **21 个单元 / ≈9,090 行**，全部是 `git grep` 实测"零命中"的真缺口（不是名字匹配误判）。

### 38.4 与顺序会话 A 的分工（回应其 §1 第 3 条"不要碰 M2Server 区"）
本次 5 条车道中 4 条落在 `src/GXX.M2Server/**`（会话 A 的常驻区）。**这是有意的越区派发**，依据两条：

1. **会话 A 的 goal 已 `blocked`（256 轮上限）**：`批次J255`（20:11）之后其区域**零推进**；
2. 5 条车道**只新建 `Sweep9/**` 新目录**：分区表里**不带 `!`**（即没有"改既有文件"授权），
   与 A 的任何既有文件**物理不相交** ⇒ 即使 A 复活，也不会与这 5 条车道产生文件冲突。

**给 A 的提示**：上述 21 个单元已改派车道，请勿重复开工；若要接手，请改做 `Source/M2Engine` 里
**仍无车道归属**的单元（如 `AxeMon.pas` 9456 行、`MirConfigDlg.pas` 7442 行、`ObjPlayer.pas` 面），
并在 §5 待认领池加行。

### 38.5 本轮未做的事（如实登记）
- `checklist-only` 6 个单元（Client-HGE `Login` 957 · LoginSrv `GateSet` 302 · DBServer `uFrmHumanExport` 151 ·
  M2Engine `uFrmClientPlugManager` 151〔本轮已并入 p9-m2-forms〕· DBServer `PlugIn` 114 · DBServer `CreateChr` 69）
  中，仅 `uFrmClientPlugManager` 本轮派发；**其余 5 个留待下一波**
  （含 `Login` 那条需要"独立登录器交付物"的架构裁定，见 §18.5 第 1 条"顺延"）。
- `Client-HGE` 的 `GuiManage` / `LoginDlg` 两条（原 `par/p2-dxcontrols-rest` 判定"不值得移植"但**未获采纳**）
  本轮**未改派**，待裁定后并入下一波。
- `DxComponent ↔ LoadDx` 去重批次（§14.4 / §16.2）**仍未执行**。

### 38.6 ★★ §16.6 那条待办（"仅靠头提及"单元的逐单元复核）已执行 —— 结果**推翻了"剩余不多"的印象**

**口径（本轮新写、可复跑）**：WEAK = 无同名 `.cs`（E1 不成立）+ 任一 `src` 文件**头 40 行内**无 `<unit>.pas` 提及（E2 不成立）
+ **正文里**有提及。`audit-coverage.ps1` 报 **weak=34**（含 `AliyunSDK\superobject`、`FireDragon` 这两个已登记的"不移植"，
故其余 **32 条**需要逐条判定）。
**判定手段**：从 UTF-8 镜像的 `.pas` 里用 `^\s*(T\w+)\s*=\s*class` 抽类声明，
再到**全部 1,289 个 `src`+`tests` 的 `.cs`**（排除 `bin/obj`）里用
`\b(class|record|struct|interface|enum)\s+<类名>\b` 找**声明**。
> ⚠ **不能用"名字出现"判定**：第一版我用 `IndexOf` 子串匹配，`AxeMon.pas` 的 52 个类**全部**"命中"——
> 但那是被同类文件的注释引用命中的，**不是实现**。改成"声明匹配"后才得到真结果。
> 这与 §37.3「否定性断言必须计数取证」同源：**"类名出现" ≠ "类被声明" ≠ "类被移植"**。

#### (a) 真已移植、**只是缺 E2 头部证据**（报表把它们误报成缺口；7 条）
| 单元 | 行数 | 实际声明位置 |
|---|---|---|
| `AxeMon` | **10,228** | `src/GXX.Client/Scenes/PlaySceneNewActor.cs`（52/52 个类都在，客户端怪物演员族） |
| `DxEdit` | 1,545 | `src/GXX.Client/GUI/DxComponent/DxControls.cs` |
| `DxPageControl` | 1,086 | 同上 |
| `DxComboBox` | 327 | 同上 |
| `DxImageGrid` | 275 | 同上 |
| `ItemRules` | 411 | `src/GXX.M2Server/Engine/Items.cs`（`TItemRule`；`TRuleList` 无声明，仍需复核） |
| `FireDragon` | 447 | 已登记 c4（`ObjFireDragon.pas` 的旧版重复），**非缺口** |
> **处置（低成本、可回收 13,872 行的假缺口）**：给上表左侧文件的**头部文档注释**补一行 `// 源单元：<unit>.pas`，
> E2 即成立、报表自动转 MAPPED。**这是"报表缺陷"而非"移植缺陷"**，下一波用一个"证据补全"小切片做完。
> ⚠ **但必须同时做方法级抽检**：本轮的判据只证明"**类被声明**"，**不证明"方法全都移植了"**。
> 补 E2 头注释与"抽检若干方法 vs 原文"必须在**同一个切片**里完成 ——
> 否则就是把"名字匹配"升级成"头部匹配"，重复 §16.6 已经踩过的"高估"错误（方向相反而已）。

#### (b) ★ 真未移植（同名类**声明不存在**）—— 报表此前**完全看不到**的真缺口，**≈31,668 行**
| 模块 | 单元 | 行数 | 类证据 |
|---|---|---|---|
| Client-HGE | **MirConfigDlg** | **8,355** | `TMirConfigDlg` 无声明（只在 `GameConfigDlgs.cs` / `MiniMapRender.cs` 里被提及） |
| Client-HGE | **JSYConfigDlg** | **6,395** | `TFrmJSYDlg` / `TTrackBar` 无声明 |
| Client-HGE | **DrawScrn** | **5,542** | 28 个类里 **24 个**无声明（`THintMessage` `TDrawSysMsg` `TScreenMoveMsgList` `TCountdownText` … 实测全为 `<none>`） |
| Client-HGE | DxImageEdit | 1,978 | `TDxBackgroundImage` 无声明 |
| Client-HGE | DxMainBottomForm | 1,664 | `TMainBottonAnimation` / `TDxMainBottomCenter` 无声明 |
| Client-HGE | DxGroupAttackProgress | 572 | `TContinueAttackSetting` / `TGroupAttackSetting` 无声明（**§16.2 早已登记待去重批次的阻塞项**） |
| Client-HGE | DropItemsMgr | 570 | `TDropItemsMgr` / `TPointDropItemList` 无声明（整单元） |
| Client-HGE | DxSwitchButton | 474 | `TButtonSetting` 无声明（同上，§16.2） |
| Client-HGE | DxPopupMenu | 467 | `TDxItemMenuList` 无声明 |
| Client-HGE | DxSexPanel | 328 | `TSexImageSetting` 无声明 |
| LoginSrv | MasSock | 1,017 | `TFrmMasSoc` 无声明（整单元） |
| LoginSrv | GrobalSession | 93 | `TfrmGrobalSession` 无声明 |
| DBServer | uFrmRoleDataEdit | 974 | `TFrmRoleDataEdit` 无声明（整单元） |
| GameCenter | GHeroDBConfig | 565 | `TFrmHeroDB` 无声明（整单元） |
| GameCenter | GLoginServerRouteSet | 35 | `TfrmLoginServerRouteSet` 无声明 |
| Common | MemoryIniFiles | 888 | `TMemoryIniFile` / `TIniValueList` / `TQuickSortList` 无声明（整单元；`inDpr=False`，需先判"是否死代码"） |
| LogDataServer | FileSearchPool | 558 | `TSearchThread` / `TSearchManager` 无声明 |
| LogDataServer | ThreadPool | 445 | `TPoolManager` / `TPoolThread` 无声明 |
| M2Engine | ObjAxeMon | 290 | `TDualAxeMonster` / `TThornDarkMonster` / `TArcherMonster` 无声明（整单元） |
| M2Engine | uFrmGlobalVarEdit | 233 | `TFrmGlobalVarEdit` 无声明 |
| M2Engine | dlgReplaceText | 121 | `TTextReplaceDialog` 无声明 |
| M2Engine | dlgConfirmReplace | 104 | `TConfirmReplaceDialog` 无声明 |

#### (c) 需"函数级"复核（无类声明，本轮**未定性**，不冒充结论）
`HandleNpcCmds`(731，NPC 脚本命令库) · `imm`(891，输入法 P/Invoke，§18.5 归 c3) ·
`CheckCrc`(159，CRC 函数) · `SelGate`: `ThreadPool`(1127)/`SendQueue`(566)/`IOCPManager`(285)
（§18.5 第 6 条已裁定"被 `GatewayKit` 取代、登记为不移植"，**但正式登记尚未落到代码头注释** ⇒ 报表一直显示为缺口）。

### 38.7 结论：**真实剩余量 = 31,668（本次新发现）+ 9,090（P9 在飞）+ ≈1,593（checklist-only 余量）≈ 42,000 行**
这解释了 §16.6 的疑问："`unmapped=0` 不等于完工"——**第三种成因**是本次这条：
**正文提及（E2w）把整单元的缺口降级成了一条"弱提示"，从此既不进 UNMAPPED、也不进任何车道**。
> **规程（本轮固化）**：`WEAK` **不是**一个"无需处理"的类别。每次波次收口时，
> 必须把 `WEAK` 里的每一条按 **(a) 补 E2 证据 / (b) 改派车道 / (c) 函数级复核** 三选一落地，
> 不得让它在两次审计之间"静默留存"。

### 38.8 下一波（P10）预案（**本轮不派发** —— 宿主内存只剩 1.9 GB / 15.7 GB，5 条车道在飞，先不超订）
| 预案车道 | 承接 | 规模 |
|---|---|---|
| `p10-client-scrn` | `DrawScrn`(5,542) + `DropItemsMgr`(570) | ≈6,100 |
| `p10-client-mirconfig` | `MirConfigDlg`(8,355) + `JSYConfigDlg`(6,395) | ≈14,750 |
| `p10-client-dxrest` | `DxImageEdit` + `DxMainBottomForm` + `DxGroupAttackProgress` + `DxSwitchButton` + `DxPopupMenu` + `DxSexPanel` | ≈5,483 |
| `p10-db-login-forms` | DBServer `uFrmRoleDataEdit` + LoginSrv `MasSock`/`GrobalSession` + GameCenter `GHeroDBConfig` + LogDataServer `ThreadPool`/`FileSearchPool` | ≈4,100 |
| `p10-m2-misc` | `ObjAxeMon` + `uFrmGlobalVarEdit` + `dlgReplaceText` + `dlgConfirmReplace` + Common `MemoryIniFiles` | ≈1,636 |
| （小切片）`p10-evidence` | §38.6(a) 的 6 处头部注释补全（**只改注释、不改逻辑**） | ≈0 |
> **`DxComponent ↔ LoadDx` 去重批次（§14.4/§16.2）必须排在 `p10-client-dxrest` 之前或同时裁定**，
> 否则该车道的 4 个单元会与"枚举/类型重名的根因去重"撞车（这是 §16.2 登记的同一个阻塞）。

---

## 39. 第 51 轮：P10 巨型车道开工 + §16.6 待办**收口** + 一条会"静默闭合同名缺口"的工具盲区

### 39.1 P10 波次（本轮新开 2 条巨型车道）
| 车道 | 分支 | 承接 | 规模 | 为什么是它 |
|---|---|---|---|---|
| `p10-client-mirconfig` | `par/p10-client-mirconfig` | `MirConfigDlg` 8,355 + `JSYConfigDlg` 6,395 | **≈14,750 行** | 二者都是 §38.6(b) 复核出的**此前不可见**缺口；且 `GameConfigDlgs.cs:301-302` 早已留下**等待接线的接缝** `CreateMirConfigDlg`、`:373` 已有 `TJSYConfigDlg` **桩** |
| `p10-client-scrn` | `par/p10-client-scrn` | `DrawScrn` 5,542 + `DropItemsMgr` 570 | ≈6,112 行 | DrawScrn 28 个类里 **24 个无声明**；DropItemsMgr 整单元未移植（`Scenes/DropItemFx.cs` 目前只是等价物） |

- 分区已入 `lane-zones.tsv`，`verify-lanes.ps1` → `ALL LANES WITHIN THEIR ZONES`（含新 2 条）；
  归属已入 `unit-map.tsv`。两条车道的目标目录都是**新建目录**，只对**接线所需的那 1 个文件**给了 `!` 授权
  （`GameConfigDlgs.cs` / `DropItemFx.cs`）——这正是"新文件独占区"规矩的用法：**接缝所在文件是唯一例外，且写进分区表**。
- 派发时已内置**内存协调**指令（`-m:1 -p:BuildInParallel=false`、只跑本车道测试、失败先 `dotnet build-server shutdown`）：
  本轮全机空闲内存一度只有 1.1 GB / 15.7 GB，而**历史上已有两次"宿主杀死车道"事故（§13、§36）**。
  同一指令已同时发给 5 条 P9 在飞车道。

### 39.2 ✅ §16.6 那条待办（34 条 WEAK 的逐单元复核）**本轮收口**
§38.6 已判定"有类声明的"（35 条，其中 7 条 (a) 类 + 28 条 (b) 类）。本轮补完**(c) 无类声明族**（函数级判据：
从 `.pas` 抽**单元级** `function/procedure` 名，再在全部 `src`+`tests` C# 里找同名符号）：

| 单元 | 行数 | 裁决 | 依据 |
|---|---|---|---|
| `imm` | 891 | **不移植（c3）** | 76 个例程 **0 命中**；§18.5#5 已裁定输入法走 WinForms 原生 IME |
| SelGate `ThreadPool` | 1,127 | **不移植（c3）** | 44 例程 33 缺；被 `GatewayKit` 取代（§18.5#6） |
| SelGate `SendQueue` | 566 | **不移植（c3）** | 12 例程 7 缺（`GetSendQueue`/`AddBuffer2`…） |
| SelGate `IOCPManager` | 285 | **不移植（c3）** | 12 例程 8 缺（`CompPortInit`/`InitServer`…） |
| `CheckCrc` | 159 | **已移植，缺 E2 证据** | 2/2 例程在册 |
| `dlgConfirmReplace` | 104 | **已移植，缺 E2 证据** | 3/3 例程在册 |
| `HandleNpcCmds` | 731 | **部分移植**：缺 `ClearBatchList` `QuestCheckConditionOr` `ProcessParams` | 5 例程缺 3 |
| `dlgReplaceText` | 121 | **部分移植**：缺 `GetReplaceText` `SetReplaceText` `GetReplaceTextHistory` `SetReplaceTextHistory` | 5 例程缺 4 |
| `MemoryIniFiles` | 888 | **真未移植** | 3 个类全无声明（25 例程的"命中"是常见名假阳性） |
| LogDataServer `ThreadPool` | 445 | **真未移植** | `TPoolManager`/`TPoolThread` 无声明；28 例程缺 18 |
| LogDataServer `FileSearchPool` | 558 | **真未移植** | `TSearchThread`/`TSearchManager` 无声明；24 例程缺 8 |
| `uFrmGlobalVarEdit` | 233 | **真未移植** | `TFrmGlobalVarEdit` 无声明 |
| `GLoginServerRouteSet` | 35 | 小缺口（1 例程，类无声明） | 但 `inDpr=False`，需先判死代码 |

> **方法论再确认**：本轮第一版结果里 `MemoryIniFiles` 显示"25 例程 24 命中"——**几乎全绿**，
> 但它的**三个类一个声明都没有**。⇒ **函数名匹配对"常见命名"（`AssignTo`/`Add`/`Clear`）会产生大量假阳性**，
> 只有"**类声明** + **整单元例程缺少数**"两条一起看才可信。这与 §38.6 的"不能用名字出现判定"是同一条教训的第二次出现。

### 39.3 ★ 新工具盲区：**重复 basename 会让"关闭一份"静默闭合另一份**
本轮给审计报表新增 `## DUPLICATE-BASENAME units` 分节（`audit-coverage.ps1`），实测出 **30+ 组**同名单元。
其中一条**正好命中我们自己的待办**：

| 源 | 行数 | 真状态 |
|---|---|---|
| `SelGate\ThreadPool.pas` | 1,127 | **不移植**（被 GatewayKit 取代） |
| `LogDataServer\ThreadPool.pas` | 445 | **真缺口**（`TPoolManager`/`TPoolThread` 都没移植） |

而 §18.5#6 的处置口径是"**在这些 `.cs` 文件头 40 行内注明源单元以闭合 E2 证据**"——
**若照做写一句 `ThreadPool.pas`，E1/E2 是按 basename 匹配的 ⇒ LogDataServer 那份的真缺口会被一起标成 MAPPED、从此消失。**
> **规程（本轮固化）**：§18.5#6 那种"补头注释闭合 E2"的处置，**必须先按 basename 是否重复分流**：
> - **basename 唯一**（如 `imm` / `SendQueue` / `IOCPManager`）⇒ 可以在 `.cs` 头注明，安全闭合；
> - **basename 重复**（如 `ThreadPool` / `AsyncCalls` / `ParadoxDataSet` / `WinSock2` / `uFrmMain` / `SDK` / `Share` / `PathFind`）
>   ⇒ **禁止**写头注释、**禁止**加入 not-ported 登记表，只能**逐副本**判定（用 `unit-map.tsv` 的显式行 + 真移植后的 E1）。
> 报表里那 30+ 组已带此告警，任何人引用 MAPPED 结论前必须先看这一节。

### 39.4 本轮未做（如实登记）
- **P9 五条车道仍在飞**（`p9-m2-monsters`/`pathfind`/`datalayer`/`forms`/`rungate-rest`），**尚未合并入 main**；
  本轮只做"保护在飞产出 + 加宽管线"，未做提升。
- §18.5#6 的 E2 补注解经 §39.3 分流后只剩 `imm`/`SendQueue`/`IOCPManager` 三条，**已在 §39.5 执行完毕**（但执行方式改了）。
- P10 其余预案车道（`dxrest` / `db-login-forms` / `m2-misc`）**未派发** —— 内存紧张，且 `dxrest` 须先有
  `DxComponent ↔ LoadDx` 去重裁定（§14.4/§16.2）。

### 39.5 §18.5#6 的"补头注释"**改判为"登记进 not-ported 注册表"并已执行**
§18.5#6 原口径是"在这些 `.cs` 文件头 40 行内注明源单元以闭合 E2 证据"。按 §39.3 的分流复核后**改为**：
把 `imm` / `SendQueue` / `IOCPManager` 三条加进 `tools/audit-coverage.ps1` 的 `$VENDOR_UNITS`（"不移植"注册表）。

**为什么改口径**（这是本轮的第二条方法论修正）：
- 补 `.cs` 头注释会让 E2 成立 ⇒ 报表把它们记成 **MAPPED**，**等于宣称"移植过了"** ——
  而它们**根本没移植**（`imm` 76/76 例程缺失、SelGate `SendQueue` 7 缺、`IOCPManager` 8 缺）。
  §18.5#6 的原意是"别让报表一直显示为缺口"，但**"消失"与"已移植"是两件事**：
  正确去处是 **not-ported 桶**（与 `MemPool`/`FixedMemoryPool`/`SyncObj`/`IOCPTypeDef` 同类），不是 MAPPED 桶。
- 实测效果：`weak 34 → 31`、`not-ported 178 → 181`，`mapped` **不变**（325）—— 正是"只改口径、不虚增完成度"。

**并且**：`ThreadPool` 因 §39.3 的重复 basename 风险**被明确排除**（源码注释里写明了理由），
LogDataServer 的那份仍留在缺口里（下一波 `db-login-forms` 车道）。
> **规程（本轮固化）**：**"让报表不再报缺口"有三种去处 —— MAPPED（真移植）/ not-ported（按设计不移植）/
> ASSIGNED（在飞）—— 必须先判定是哪一种，绝不能图省事都塞进 MAPPED。**

---

## 40. 第 52 轮：P9 首条车道收口（含一次"证据文件自己会把结论记成 MAPPED"的拦截）

### 40.1 提升（第 2 次）
`par/p9-rungate-rest` 并入 `main` = **`aa5477aa`**（车道 5 个切片，3 个新文件：证据数据 + 979 行取证测试 + 报告）。
门禁：`dotnet build GXX.slnx -c Debug` → **0 error / 165 warning**；
全量 `dotnet test` → **18,607 通过 / 0 失败**（`18,576 → 18,607`，即 +31 条本车道用例）。
合并前已用 `git merge-tree --write-tree` 对**全部在飞车道**做过 dry-run：7 条全部 `CLEAN`（分区纪律成立）。

### 40.2 裁定：`IODataPool`/`IocpTcpClient`/`Qos`/`DllUpdateCommon` 四单元 = **不移植 + 证据**（0 个 1:1 移植）
| 单元 | LF | 已移植/总 | 核心证据（该车道逐调用点取证） |
|---|---|---|---|
| `Common/IODataPool.pas` | 586 | **0/11** | 14 处 `GetMem/FreeMem`、装 `POVERLAPPEDEx` 原始指针、41 档×1024 预分配；6 个引用点**全在已裁"不移植"的 IOCP 管道内** + `uFrmMain` 的 6 个统计标签；`GatewayKit` 23 项公开 API 中 **0 项**含池 API |
| `Common/IocpTcpClient.pas` | 340 | **0/19** | 唯一调用方 `RunGateUtils.pas`（6 条调用点逐行核对）；`SetActive` 单方法内含 **20 处** Win32 IOCP 机械；活设施是 `TcpLink`（≥3 个生产调用方） |
| `Common/Qos.pas` | 293 | **0/0** | 纯头文件翻译（27 常量 + 22 类型 + 40 `EXTERNALSYM`），`implementation` 段**为空**；C# 侧 49 名字里 7 条 `SERVICETYPE_*` 已由 Client 落进 `WinSock2Constants`，**42 条 0 命中** |
| `DllUpdateCommon.pas` | 10 | **0/0** | 空壳：0 声明；`.dpr/.dproj/.dpk` 0 命中、任何 `uses` 0 命中 |

**该车道自我纠错 4 处**（全部由取证用例当场否掉初版臆断，值得进下一波提示词）：
`Qos` **确实**被 `IocpWinsock2.pas` 使用（`FLOWSPEC` 7 处 / `SERVICETYPE` 2 处）；`Qos` 的 `const` 段是 **4** 不是 5；
C# 侧 0 命中是 **42** 不是 32；`DivMod` **不是**死代码（被调用 2 次），真缺陷是负数路径会 `#DE`。
另固化两条脚本陷阱：`Regex.Matches(s, @"^type\s*$", Multiline)` **恒返回 0**（`\s` 跨行吞掉空行使 `$` 无法定位）；
剥注释时若连换行一起删，会把两行粘成一行、行首标记随之丢失。

### 40.3 ★ 集成方拦下一次"**证据文件自己把结论记反**"
该车道的证据文件 `Rest9/Rest9NotPortedEvidence.cs` **头部就写着 `Source/RunGate/Common/IODataPool.pas` 等 4 条路径**
—— 而 E2 判据是"某 `.cs` **头 40 行内**出现 `<unit>.pas`"⇒ **这 4 个"明确不移植"的单元会被记成 MAPPED**，
报表上等于宣称"移植过了"。**这正是 §39.5 那条规程的实战：** 处置不是去改这个文件，而是把 4 个 basename
加进 `audit-coverage.ps1` 的 not-ported 注册表（**注册表优先级高于 E2**，且 4 个 basename 已实测**唯一**，无 §39.3 的同名风险）。
实测效果：`assigned 23 → 19`、`not-ported 181 → 185`、`mapped 不变（325）`、`weak 不变（31）`。
> **规程（第 3 次同源，本轮补充可执行判据）**：车道交付"**不移植 + 证据**"时，**证据文件里不要用 `<unit>.pas` 字面量**；
> 若已经写了，**集成方必须在合并后立刻把它登记进 not-ported 注册表**，否则"证据"会变成"过度声明"。
> 判据固定为：**"这个单元到底移植了没有？"** —— 没移植就绝不能出现在 MAPPED 桶里。

### 40.4 车道交回的 3 条阻塞项的处置
| # | 内容 | 处置 |
|---|---|---|
| B1 | §18.5#6 要求"在这些 `.cs` 头注明源单元以闭合 E2"，但 `GatewayKit/GatewayProtocol.cs`、`TcpLink.cs` 在该车道分区外 | **按 §39.5 改判**：不补头注释（那会记成 MAPPED），改为 not-ported 注册表登记 —— 已在 §40.3 执行完毕 |
| B2 | `Qos.pas` 的 7 条 `SERVICETYPE_*` 已落在 `src/GXX.Client/Tail/WinSock2Constants.cs`（同源不同单元），报表无法自动判"部分已移植" | 同上：`Qos` 整单元按 not-ported 登记，**部分覆盖写在报告与本节**，不制造第二个 MAPPED |
| B3 | **`uFrmMain.pas:2240-2245` 的 6 个池统计标签数据源消失（D-P9-02）** —— 属 `uFrmMain`（已由 `p2-rungate-impl` 移植）的**开放项** | **登记为开放项**，需主窗体归属方决定"改语义 or 隐藏"；**本轮不擅自改**（越区） |

### 40.5 本轮的台账卫生
按 §38.2 那条规程，**车道关闭时同步清理 `unit-map.tsv`**：已删除 `IODataPool`/`Qos`/`IocpTcpClient`/`DllUpdateCommon`
指向 `par/p9-rungate-rest` 的 4 行（车道已并入并关闭；留着就是"永久占位行"），并在原处留注释指向注册表。

### 40.6 在飞状态
`p9-m2-monsters`/`p9-m2-pathfind`/`p9-m2-datalayer`/`p9-m2-forms`/`p10-client-mirconfig`/`p10-client-scrn`
**仍在飞**（前四条各有 2–4 个已提交切片）；dry-run 合并全部 `CLEAN`。





